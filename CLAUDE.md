# FFC

**Status:** Phase 1 Steps 1, 2, 3 of V2.8 **COMPLETE**; Step 4 **slices 1–8 COMPLETE** at S028; **S029** landed §3.20 Matches flashcard + AdminSeasons; **S030** landed §3.19 Formation **Slices D+E** (notes + share + realtime + entry links); **S031** landed **§3.1-v2 Captain Helper Slices A+B** (new `/matchday/:id/captains` admin screen, Formula + Randomizer modes, suggested pair cards, confirmation sheet with White=weaker auto-assignment, guests-on-roster subsection with S007 stats, rank-gap >5 advisory sub-modal) — Slice A `0f2b820`, Slice B `a689ba3`. S031 also handed off a 21-item live acceptance checklist ([sessions/S031/acceptance-checklist.md](sessions/S031/acceptance-checklist.md)) covering S030+S029+S028+S026 scope — user testing in parallel to the build work. No migrations; backend was pre-built at Step 2 (`v_captain_eligibility` view + `suggest_captain_pairs` / `pick_captains_random` / `set_matchday_captains` RPCs, migrations 0006 + 0008). S030 commits: `32dd8d9` (migration 0024 `formations.notes` + `upsert_formation` 7-arg + realtime publication + FormationPlanner notes/share/realtime UI) · `b20befe` (Poll State 8 CTA + AdminMatches card Formation action + MatchDetailSheet footer link). S029 commits: `977a9b8` (migration 0022 `seasons.planned_games` + RPCs) · `f1f6d0d` (AdminSeasons list+create+edit) · `f723794` (route + AdminHome link) · `25ef97e` (Matches query scorers + planned_games) · `46e08b1` (flashcard markup + matches.css + migration 0023 RPC DEFAULT NULL fix). **Migrations on live DB: 24 (0001 → 0024_formations_slice_d).** Slice 7 (S026) landed §3.7 Poll Depth-B + Leaderboard gate + edit_match_players + Phase 5.5 card + friendly auto-flag (`45383bc`, migrations 0017+0018+0019). **Slice 8 (S028)** = S027 backend carry-over + **§3.19 Formation Planner Slices A+B+C**: migration 0020 (`v_match_commitments.guest_id` extension · Poll.tsx pk-lookup refactor — `8a753cd`) + migration 0021 (`admin_draft_abandon` + `admin_draft_force_complete` RPCs unblocking Phase 5.5 buttons — `c6bfb89`) + §3.19 Slice A scaffold + 9 pattern presets + captain route `/match/:id/formation` (`03bbcf5`) + §3.19 Slice B drag-drop tokens + custom pattern (`e78ee99`) + §3.19 Slice C rotating GK + starting-GK picker + rotation_order persistence (`f489c14`) + deploy fix for 4 strict-build TS errors (`7e9a890`). §3.19 backend pre-built at Step 2 (`formations` table + `upsert_formation` + `share_formation`); S028 landed UI only. Slices D (realtime + share_formation + notes) + E (entry links) deferred. Live at https://ffc-gilt.vercel.app. **⚠ Migration 0020 collision RESOLVED** — S027 plan renumbered to 0022 (executed in S029). **Dropdown pattern** (`feedback_anchored_dropdowns.md`). **Match Detail pattern** — portal-rendered bottom sheet with optional W/D/L chip via `profileId`. **Admin direct-submit pattern (S025)** — `admin_submit_match_result` creates `matches` + N `match_players` + approves + audits in one tx. **Poll state-machine pattern (S026)** — all 9 Poll states derive from `(now vs poll windows) × myVote × roster_locked_at × draft.status × commitments[].team`; no separate phase enum. **Realtime-capable screen pattern (S026)** — `loadData(mode: 'initial' | 'refresh')` callback shared by initial mount + realtime subscribe + pull-to-refresh; `mode` gates skeleton-clear vs refresh-spinner. **Phase-2 seed pattern (S026)** — low-blast-radius admin affordances (audited, whitelisted, strictly additive) can land ahead of schedule during Phase 1. **Slice-size discipline pattern (S028)** — multi-session screens split into A/B/C/D/E with deferred-items documented in the file header JSDoc. **Ref-based drag state pattern (S028)** — pointermove mutates `useRef` not `useState` to avoid per-frame re-renders. **Conditional-spread for optional RPC args (S028/S029)** — `...(x ? { p_field: x } : {})` matches generated types' `T | undefined` without `as`-cast escape hatches; RPC args that are nullable must have `DEFAULT NULL` in PL/pgSQL or Supabase type generator marks them required. `supabase/migrations/0012_grants.sql` applied live — future table-creating migrations MUST rely on 0012's DEFAULT PRIVILEGES or emit own GRANTs; RLS alone does not grant access (Supabase "auto table exposure" OFF). All SECURITY DEFINER RPCs need explicit `GRANT EXECUTE ... TO authenticated` (DEFAULT PRIVILEGES from 0012 don't cover functions). `ffc/vercel.json` SPA catch-all rewrite load-bearing. **Vercel builds with `tsc -b` (project-refs) — stricter than local `tsc --noEmit` (S028 lesson).** Run `node ./node_modules/typescript/bin/tsc -b` before pushing. **Schema-drift pattern** — query `information_schema.columns` / `pg_proc.pg_get_function_arguments(oid)` / `pg_type` / `information_schema.views` BEFORE writing PostgREST queries, PL/pgSQL function calls, enum casts, or view-based caller logic. S024 drifts: `matchdays.venue` (not `venue_label`), no `matchday_number`, no `match_players.late_cancel_*`. S025 drifts: `log_admin_action` 4-arg (admin derived via `current_profile_id()`), `team_color` not `team_colour` (plpgsql lazy-parses so CREATE passes but first call fails), `seasons.starts_on` not `started_on`. **S026 drifts:** `profiles.banned_until_matchday_id` doesn't exist (bans live on `public.player_bans` with `starts_at`/`ends_at`/`revoked_at`); `v_match_commitments` exposed only `guest_display_name` — **fixed in S028 migration 0020** which appends `guest_id uuid` for pk lookup. **S028 RPC-arg typing** — Supabase generated optional args are `T | undefined`, not `T | null`; omit the field via conditional spread rather than passing null. `formations.starting_gk_profile_id` FKs profiles so guests are always outfield. Position enum UPPERCASE (`GK`/`DEF`/`CDM`/`W`/`ST`); leaderboard sort `motm` singular; `edit_match_result` narrow (score/MOTM/notes only, requires `approved_at IS NOT NULL`, no nested `players` — use `edit_match_players` for per-player post-approval corrections). `cast_poll_vote(matchday_id, choice)` accepts `'yes'|'no'|'maybe'|'cancel'`; re-voting yes after cancel resets `committed_at` (anti-hoarding per §3.6). `invite_guest` is confirmed-only + 8-arg (display_name + primary + secondary + stamina + accuracy + rating + description); raises `FFC_INVITER_NOT_CONFIRMED` / `FFC_NO_GUEST_SLOT`. `admin_draft_force_complete` raises `FFC_ALREADY_AT_CAP` if invoked at roster cap. Google OAuth in Testing mode — consent shows `hylarwwsedjxwavuwjrn.supabase.co` (Supabase platform limit). **S030 patterns:** CREATE OR REPLACE FUNCTION cannot add parameters — DROP + CREATE required (re-GRANT after). Supabase realtime requires explicit `ALTER PUBLICATION supabase_realtime ADD TABLE` — verify via `pg_publication_tables` before wiring `postgres_changes`. `postgres_changes` filter supports ONE column — secondary filters must be applied client-side. **S031 patterns:** git-worktree `core.worktree` misconfiguration — when `git rev-parse --show-toplevel` returns the main tree's path from inside a worktree, run `git config --worktree core.worktree <abs-worktree-path>` to fix; `git status` will then see your edits. Windows `cmd //c mklink /J` fails on paths containing `&` — use PowerShell `New-Item -ItemType Junction` instead. Bash heredoc with CSS content trips on `$` — use Edit tool for appending CSS blocks. `guest_rating` enum values are `weak | average | strong` (not `avg` — caught by `tsc -b` strict). Preview browser sandboxed to localhost — external URLs don't navigate, so auth-gated production flows must be hand-off. **S032 patterns:** Nested-button a11y workaround — when an inner element needs tap behaviour distinct from its outer `<button>` parent, use `<span>` with `onClick={(e) => { e.stopPropagation(); innerAction() }}` instead of nesting `<button>` (invalid HTML). Trade-off: keyboard users still Enter-activate the parent; the inner tap is pointer-only — acceptable for polish where the inner affordance is non-critical. **Pre-commit concurrent-write pattern (S032)** — when realtime subscription is unavailable for a table (no `ALTER PUBLICATION`), snapshot relevant state on load + re-read on commit + diff; surface advisory modal with last-write actor + timestamp via `admin_audit_log`. Phase-1 alternative to live realtime conflict detection. **S033 — data + palette:** DB row renamed `Season 1 → Season 11`, `planned_games=35 → 40` via `UPDATE seasons ...` (user's real-world league is on season 11 match 33/40). **Captain Helper palette re-theme (S033)** — CaptainHelper-only brand re-theme via 18 custom properties declared on `.ch-root` (--ch-paper `#0e1826` · --ch-ink `#f2ead6` cream · --ch-accent `#e63349` red · --ch-gold `#e5ba5b` · --ch-success · --ch-warn). Every rule re-pointed from hardcoded rgba/hex to `var(--ch-*)`. Use-this-pair & Roll buttons → accent red; primary pair card highlight → gold; concurrent-admin modal → gold (not cyan). Shared `auth-btn--approve` overridden scoped inside `.ch-root .ch-sheet-actions` so login/signup unaffected. Scope kept narrow — rest of app (Leaderboard/Poll/Profile/Admin) still blue/slate. **Single-screen tokenisation pattern (S033)** — when re-theming one screen without disturbing the app: declare 18-ish `--*` custom properties at that screen's root element, then point every descendant `rgba()` / `#hex` through `var()`. Rules stay structurally unchanged; future palette tweaks touch only the token declarations. Reusable playbook for Poll/Leaderboard if/when user greenlights propagation. **S034 — Admin IA restructure + AdminSeasons redesign + migration 0025.** Removed conditional 5th Admin bottom-nav tab (`RoleLayout` now consistent 5 tabs for everyone). Admin entry point moved to Settings → red `🛠 Admin platform` row, admin-gated. `AdminHome` rewritten stub → 3-card hub (Season / Player / Matches management). **Migration 0025** adds `update_season` (full edit · `COALESCE`/NULL optional args · `p_clear_ends_on` boolean signal for explicit unset · auto-stamp `ended_at` when `ends_on` moves into the past) + `delete_season` (guarded by `FFC_SEASON_HAS_MATCHDAYS` check · audits BEFORE DELETE so log survives) + DROP+CREATE `create_season` with `p_planned_games` now **required**. **§3 AdminSeasons** full rewrite (~380 LOC) — red `+ New season` pill CTA · bottom sheet (shared create/edit) · labelled meta rows with DD/MMM/YYYY dates via inline string-split (no `new Date(iso)` TZ shift) · format chip + status pill · edit + delete icons (delete disabled with dashed placeholder when `matchday_count > 0`). **S034 patterns:** (a) **"Leave alone vs explicitly clear" = 3-state signal** — use a `p_clear_FIELD boolean` alongside `p_FIELD TYPE DEFAULT NULL` for nullable columns; NULL = leave, set = assign, `clear=true` = erase. Avoids sentinel hacks, maps cleanly to TS conditional spread. (b) **CREATE OR REPLACE FUNCTION cannot change arg defaults** — DROP + CREATE required when making an optional param required. Re-GRANT EXECUTE after. (c) **DATE columns need string-split, not `new Date(iso)`** — `new Date('2026-04-21')` parses as UTC midnight, renders as 20/APR on negative-offset TZs. Inline `fmtDate(iso)` splits and uses components directly. (d) **Audit BEFORE DELETE** — `admin_audit_log.target_id` column holds the deleted row's UUID for postmortem trail. **S035 — Poll brand palette propagation.** User approved S034 live and asked to propagate the CaptainHelper palette to Poll first at full brand strength. 10-line scope-override added at the top of `.po-screen` — declares `--bg/--surface/--surface-2/--text/--text-muted/--accent/--success/--warn/--danger/--skel-a/--skel-b` to brand tokens (cream ink on brand navy paper, gold accents, red CTA). **Semantic-intent fix:** Poll `--accent` overridden to gold `#e5ba5b` (root has it red, but Poll's `.po-me-tag`/`.po-avatar--self`/`.po-guest-rating--strong`/`.po-status--novote`/`.po-team-header--active` fallbacks were gold — restoring the original design intent). **`--warn` alias** added (root defines `--warning` but Poll uses `--warn` throughout). Zero changes to the ~95 `.po-*` downstream rules — every `var(--*)` lookup resolves to brand values. Rest of app (Leaderboard/Profile/Matches/Admin) untouched. **Tokenisation playbook now validated at scale** — CaptainHelper needed rule-by-rule edits (rules had hardcoded rgba); Poll needed only the 10-line root block (rules were already var()-driven). Lesson: **when existing CSS is already var()-based with fallbacks, scope-override at the screen root is a 20× better ROI than rule-by-rule editing**. When root vs per-screen fallback intent diverges (root `--accent=red` vs Poll fallback `#c49a4b gold`), trust the per-screen fallback hint and override at screen scope. **S036 — BOIL THE OCEAN: brand palette propagated across 8 remaining screens in one sweep.** Leaderboard `.lb-screen`, Profile `.pf-screen`, Matches `.mt-screen`, Rules `.lr-screen`, Settings `.st-screen`, AdminPlayers `.admin-players`, AdminMatches `.admin-matches`, AdminSeasons `.as-root` + AdminHome `.ah-root` — all got the 12-token brand block (`--bg:#0e1826 / --surface:rgba(20,34,52,.68) / --text:#f2ead6 cream / --accent:#e5ba5b gold / --danger:#e63349 red / --success:#4fbf93 / --warn+--warning:#d7a04a`) declared at each root + `background:var(--bg);color:var(--text);` anchor. With CaptainHelper (S033) + Poll (S035), all 10 in-app screens now share one brand. Auth screens `.auth-screen` (Login/Signup/PendingApproval) + global `:root` defaults + portal overlays intentionally untouched. **Per-root token declaration (vs shared helper class) pattern (S036)** — considered abstracting into `.brand-theme` mixin, rejected: different layout properties don't cleanly merge + explicit per-root declaration keeps screen-level themeability obvious at edit-time + zero abstraction tax. ~100 LOC of duplication is the correct trade-off. **`--warn` AND `--warning` dual-alias pattern (S036)** — screens use different naming conventions; defining both at scope-root means no app-wide rename needed. **Preview harness quirk (S036)** — `window.location.href=X` destroys the JS execution context, so batched-visit scripts fail on second iteration; use sequential `navigate→wait→inspect` per route. Authoritative plan: `planning/FFC-masterplan-V2.8.md`. **S037 — Season 11 roster import + §3.15 captain reroll + logo rollout (boil-the-ocean).** Migration 0026 `season_seed_stats` + `v_season_standings` `CREATE OR REPLACE VIEW` UNIONing live aggregates with seeds (preserves view signature → zero downstream TS changes; required because `v_captain_eligibility` depends on the view). Migration 0027 archived Test Player + `TRUNCATE ... RESTART IDENTITY CASCADE` across 15 transactional tables + inserted 39 ghost profiles (35 player + 4 admin: Ahmed Saleh, Rawad, Abood, Barhoom; `auth_user_id NULL`) + 40 Season 11 seed rows derived from external sheet. §3.15 captain reroll modal Poll.tsx (gold-tinted attention-register card — accept_substitute / request_reroll RPCs). Logo rollout from `shared/FFC Logo Transparent Background.png`: PWA icons + new `og-image.png` 1200×630 + OG/Twitter meta tags. Patterns: seed-aggregate table + UNION view (re-usable); `CREATE OR REPLACE VIEW` when dependents exist; notifications payload client-side filter (PostgREST `.contains()` unreliable); TRUNCATE...CASCADE multi-table atomic; ghost profiles as first-class entities (leaderboard-visible + rosterable but can't log in). **Migrations on live DB: 27.** Live: https://ffc-gilt.vercel.app. **S038 — Cream-bg PWA + share-preview polish (P1) + ghost-claim routing fix (P2, Barhoom unblock).** Two commits, no migration. **P1 `a44f1fb`** regenerated PWA + OG share assets with cream `#f2ead6` brand bg (was dark navy with cropped shield in S037): `og-image.png` 1200×630/80% inset · `ffc-logo-180.png` Apple-touch 78% inset · `ffc-logo-maskable-512.png` 60% safe-zone. Generator script `_wip/gen_cream_assets_s038.py` (PIL bbox+LANCZOS) — re-usable pattern. `index.html` meta updates: `og:description` + `twitter:description` → "The official Home of the FFC." + `?v=2` cache-buster on og:image (forces WhatsApp scrape cache to refresh). `PublicLayout.tsx` topbar stripped (auth screens render own crest; `3573761` topbar redundant on Login/Signup/PendingApproval). Transparent `any`-purpose icons untouched. **P2 `4de4b05`** unblocked Barhoom (admin) Google-OAuth sign-in. Diagnosis during code-read: full ghost-claim infra ALREADY EXISTED (`Signup.tsx` Stage 2 ghost-picker + `pending_signups.claim_profile_hint` + migration 0008 `approve_signup(p_pending_id, p_claim_profile_id)`). Bug was purely a routing dead-end — `Login.tsx` navigates to `/`, HomeRoute rendered `<PendingApproval />` which had no path forward AND for OAuth users no `pending_signups` row gets created so admins never saw them in the queue. **3-line fix:** `router.tsx` HomeRoute → `<Navigate to="/signup" replace />` (Signup self-derives Stage 2 ghost-picker if no pending row, or Stage 3 waiting if one exists); `RoleLayout.tsx` defensive `!session || !role` redirect to `/`; `AdminPlayers.tsx` ApproveSheet adds green ✓ "Email matches" / amber ⚠ "Expected X, got Y" sanity banner when `ghost.email` set. **Pre-claim email seed** via `DO $$ BEGIN ... END $$` block (multi-statement queries fail on CLI): Barhoom→`ahmed.abdallahh@hotmail.com`, Abood→`amakkawi89@gmail.com`, Ahmed Saleh→`ahmed_msaleh@hotmail.com`, Rawad→`rawadbn@gmail.com`. **Patterns (additive):** (a) **"Read existing code before designing the fix" (S038)** — initial P2 plan was full feature build with new migration + new `claim_requests` table + 3 RPCs + new `Claim.tsx` page + AdminPlayers tab augmentation. Reading codebase end-to-end revealed 80% already shipped; final fix was 3 routing edits + 4-row UPDATE. Lesson: when a user reports a bug in complex code you haven't touched in days, read the relevant files end-to-end before sketching the fix. (b) **Multi-statement SQL via Supabase CLI requires DO block** — `db query --linked "UPDATE ...; UPDATE ...;"` errors out; wrap in `DO $$ BEGIN ... END $$`. (c) **PIL bbox+LANCZOS asset-regen pattern** — `getbbox()` first, scale preserving aspect, centre on canvas; inset percentages stay meaningful regardless of source padding. (d) **OG image cache-buster `?v=N`** is the safest way to force WhatsApp scraper to re-fetch (it caches by URL). (e) **ApproveSheet email-match sanity banner** for destructive admin bind UIs — surfaces both identity sources (auth email vs ghost expected email) with colour-coded match indicator. **Migrations on live DB: 27 (unchanged).** PendingApproval.tsx kept on disk but dead code (import removed from router). Both deploys READY (P1 ~17s, P2 ~22s). Next: **S039 — live verification of Barhoom OAuth → ghost-pick → Mohammed-approves flow end-to-end + WhatsApp/iOS Add-to-Home acceptance + carry-over checklists (S031–S035)**. **S039 — Signup/login redirect-race fix + forgot-password + Phase 2 design.** Commit `677b1ed` (`Signup.tsx` stage-derivation useEffect now waits for `profileLoading` before running — prevents race where pending row reads `approved` but role is still null on refresh; on `role` detected, `navigate('/poll')` instead of dead `return`; Stage 3 polls every 5s for `profile.role` and fires `window.location.replace('/')` on approval so AppContext re-initialises; `Login.tsx` forgot-password wired to `supabase.auth.resetPasswordForEmail` — was dead stub; new `ResetPassword.tsx` page handles Supabase recovery-link callback at `/reset-password`; `BannerKind` extended with `'success'` variant). Commit `1d3f78e` (Phase 2 brainstorm closed: masterplan V3.0 written + Phase 2 design spec + ref-console mockup. Two tracks locked: 2A = Poll→Lock→Captain automation bundle (Web Push + pg_cron reminders + auto-lock + captain auto-pick); 2B = Live Match Console (admin URL→ref entry → events log → approve flow with timing). Build-order locked 2B FIRST). No code-only commit for Phase 2; backend foundation deferred to S040 slice. **S040 — Phase 2 Slice 2B-A: Live Match Console backend foundation.** 4 commits, migration 0028 applied to live. **Plan + design (`0feb349`)** — slice plan documenting migration 0028 scope. **Migration 0028 (`a8c23b5`)** — `match_event_type` enum (8 values: goal · own_goal · yellow_card · red_card · halftime · fulltime · pause · resume); new `pending_match_events` + `match_events` tables with participant XOR + minute non-negative checks + ordinal index; 5 timing columns added to `pending_match_entries` + `matches` (kickoff_at, halftime_at, second_half_kickoff_at, fulltime_at, total_stoppage_seconds — nullable on `matches`, default 0 stoppage on pending); RLS policies (pending events admin-select; permanent events authenticated-select USING true); `regenerate_ref_token(matchday_id)` admin RPC mints fresh 6h base64url tokens (24 random bytes via `gen_random_bytes` → 3-replace base64url chars); `submit_ref_entry` rewritten DROP+CREATE to read `events` + `timing` payload keys (backwards-compatible); `approve_match_entry` rewritten with sentinel-guarded timing copy + event-log promotion. **Code-review fixes (`08ddfbf`)** caught 2 real bugs before going live: (1) `approve_match_entry` timing-overwrite guard `CASE WHEN v_pme.kickoff_at IS NOT NULL` prevents nulling existing `matches.*` timing on admin-direct approval (admin path doesn't carry timing); (2) `regenerate_ref_token` advisory lock `pg_advisory_xact_lock(hashtext('regenerate_ref_token:' || p_matchday_id::text))` prevents dual-token mint under concurrent admin calls (two-statement burn-then-mint under snapshot isolation could otherwise produce two active tokens). **Apply + types regen + close (`7af341d`)** — types regenerated 1916 → 2183 lines; build clean (`tsc -b` EXIT 0 + `vite build` EXIT 0); no UI changes (RefEntry.tsx still stub — slice 2B-B opens that work). **Patterns (additive):** (a) base64url tokens via `gen_random_bytes` + 3 `replace()` calls (URL-safe vs Postgres `encode(..., 'base64')` which produces `+`/`/`/`=`); (b) two-statement burn-then-mint under snapshot isolation always needs an advisory lock to serialise concurrent calls; (c) timing-copy paths need a sentinel column (`kickoff_at IS NOT NULL`) to distinguish "no data" from "0"; (d) when a spec hardcodes a migration number, confirm build-order before applying — Phase 2 spec hardcoded "0029" for Track 2B but build-order locked 2B FIRST so it claimed 0028 instead. Live: https://ffc-gilt.vercel.app (no UI delta). **Migrations on live DB: 28 (0001 → 0028).** **S041 — Phase 2 Slice 2B-B: admin "Generate ref link" UI.** 4 commits, no migration. Plan `600ab99` + Task 1 data layer `c6add71` (extends `MatchdayWithMatch` with optional `activeToken: { expires_at }`; `loadAll` adds a fourth Promise.all branch fetching `ref_tokens WHERE consumed_at IS NULL AND expires_at > now()`; new `formatExpiresIn(iso)` helper) + Task 2 main `b2788c9` (new `RefLinkSection` rendered conditionally on `roster_locked_at` inside MatchdayCard — dashed `+ Generate ref link` CTA when no active token, gold-tinted "expires in Xh Ym" + 🔄 button when one exists; new `RefLinkSheet` portal showing read-only URL once with 📋 Copy via `navigator.clipboard.writeText` + 💬 WhatsApp share via `https://wa.me/?text=` deep link; single `handleMintRefLink(md)` handler calls `regenerate_ref_token(p_matchday_id)` RPC, captures raw token from return value, opens sheet, then re-runs `loadAll()` so the card chip shows fresh expiry). Two code-review fixes mid-task: `2fa75b5` reuses the existing `.sheet*` base classes already used by `<ConfirmSheet>` (initial pass introduced parallel `.admin-sheet*` naming — code review caught the drift risk before push); `1d22d8e` adds `mintBusy: string | null` per-row in-flight guard preventing double-tap race where a fast second tap could mint two tokens before `loadAll()` refreshes the card chip. **Patterns (additive):** (a) **one-shot raw token UX** — RPC returns plaintext only at mint (Postgres stores `sha256(token)` only); sheet captures the URL the moment RPC resolves, matchday-row reload only fetches `expires_at` afterwards (the chip-driving fact), token is never persisted; (b) **`loadAll` fan-out for derived per-row state** — auxiliary table data (here `ref_tokens` per matchday) fetched as a Promise.all branch in the same load function and merged via Map lookup, avoiding per-card useEffect queries that would N+1; (c) **reuse base sheet classes — don't invent parallel naming** — when a portal-sheet already exists in the same file (`<ConfirmSheet>` with `.sheet*`), reuse the foundation; only truly-novel bits (`.admin-ref-link-*` for the section + URL input) earn custom classes; (d) **`mintBusy: string | null` per-row in-flight guard** — storing the in-flight matchday id (`null = idle | <md.id> = busy on this row`) lets disabled-state + click-handler short-circuit synchronously; cleared in `finally` so a thrown RPC doesn't permanently lock the row; (e) **WhatsApp deep link via `wa.me`** — `https://wa.me/?text=<encoded>` opens the app on mobile + Web WhatsApp on desktop; phone omitted so user picks recipient; `target="_blank"` + `rel="noreferrer noopener"` safe default. Build clean (`tsc -b` EXIT 0 + `vite build` EXIT 0). **Migrations on live DB: 28 (unchanged).** Live: https://ffc-gilt.vercel.app. **S042 — slice 2B-C (RefEntry pre-match):** Migration 0029 anon `get_ref_matchday` RPC + 0030 pgcrypto search_path hotfix (3 RPCs needed schema-qualification — caught by anon-key curl, missed by db-query verification). `useMatchSession` hook + RefEntry stub-to-full rewrite. **Migrations on live DB: 30 (0001 → 0030).** **S043 — critical security hotfix:** Migration 0031 fixes a NULL-3VL bug in `is_admin()`/`is_super_admin()` that allowed anon-key callers to bypass `IF NOT is_admin() THEN RAISE` guards on every SECURITY DEFINER admin RPC since Phase 1 (migration 0007). Body wrapped in `COALESCE(..., false)`. Verified by anon-key curl: `regenerate_ref_token` / `reject_signup` / `approve_signup` all now correctly return `42501 Admin role required`. RLS-safe (RLS treated NULL = false already). **Migrations on live DB: 31 (0001 → 0031).** **S044 — slice 2B-D: RefEntry live match console.** 8 commits, no migration (client-only). New files: `ffc/src/lib/refConsoleConstants.ts` (5 named tunables — `REGULATION_HALF_MINUTES` 7v7=35/5v5=25, `HALFTIME_BREAK_SECONDS=300`, `MAX_STOPPAGE_SOFT_LIMIT_SECONDS=180`, `UNDO_WINDOW_MS=15000`, `HALFTIME_ADD_MIN_SECONDS=60`); `ffc/src/lib/useMatchClock.ts` (~545 LOC client-authoritative match clock + event log + score + MOTM, persisted to sibling localStorage key `<sessionStorageKey>:clock` with hydration-effect-then-1Hz-tick render cycle, 8 mutators); `ffc/src/pages/RefEntryPickers.tsx` (5 bottom-sheet pickers extracted: ScorerPicker with Goal/Own-Goal toggle, PauseReasonPicker, two-stage CardPlayerPicker→CardKindPicker, MotmPicker — all `role="dialog"` + `aria-modal`). Modified: `ffc/src/pages/RefEntry.tsx` (LiveConsole shell with LiveHeader / HalfStrip / big M:SS clock / Score block / 3-row action grid Pause+Card · Undo+MOTM · End-Half+End-Match / EventStrip / HalftimeView with Skip-Break + Add-Min); `ffc/src/lib/useMatchSession.ts` (one-line return-shape extension exposing `sessionStorageKey: storageKey` + retrofitted `eslint-disable react-hooks/set-state-in-effect` on async-hydration setStates); `ffc/src/styles/ref-entry.css` (4 new brand tokens `--rf-success/--rf-warn/--rf-pause/--rf-danger` + ~292 LOC of live-mode rules). Commits: `9a30297` (plan) · `50b026c` (hook + tunables) · `07c8595` (undoLast half-routing fix) · `cd91483` (code-quality pass) · `f4c85ca` (LiveConsole shell) · `df189c2` (MatchEvent half field) · `fef1a0a` (scorer picker) · `a840476` (pause/card/MOTM/undo) · `9d93527` (extract pickers to sibling file). **Patterns (additive):** (a) **`Date.now()` at render scope defeats `useMemo`** — move INSIDE memo body, depend on a `tick` counter, suppress `react-hooks/exhaustive-deps` with intent comment. ESLint `react-hooks/purity` catches the bug. Same fix any time a `setInterval`-driven re-render reads wall-clock state. (b) **`setState` in `useEffect` for async-arrival hydration** is a real lint error but unavoidable when the dep is async (Web Crypto digest resolves later). Fix: `// eslint-disable-next-line react-hooks/set-state-in-effect -- intentional async hydration` with intent comment. (c) **`undoLast(): boolean` was a setState-async race trap** — flag set inside `setState((prev)=>...)` updater is unreliable when read after the function returns. Change signature to `() => void`; callers use `canUndo` derived boolean for button-disabled state. (d) **Sibling-key persistence pattern** — when one hook owns a localStorage key, a downstream hook can write to a sibling key (`<parent>:clock`) instead of forcing the parent to know about clock-state shape. Keeps responsibilities clean and hydration races local. (e) **`MatchEvent` needs a `half: 1 | 2` discriminator** — without it the formatter can't tell 1st-half-stoppage (match_minute=36 → render `35+1'`) from 2nd-half-regulation (match_minute=36 → render `36'`). Populate at every event-construction site; downstream readers (Pickers, future submit RPC) inherit the right semantics for free. (f) **Pause + score-cell disable invariant** — disabling score buttons while paused prevents goals at stale match-minutes (clock is frozen, stamp would be wrong). UX nudge: resume first, then score. CARD also disables; PAUSE/RESUME stays enabled because the only way out of pause is to resume. (g) **Two-stage card flow > one-shot grid** — picker → kind sheet sequentially, mutually exclusive via `cardPickerOpen && !cardStage` for player-picker, `cardStage` truthy for kind-picker. Avoids cramming an 8-row × 2-team × 2-colour grid into one view. (h) **Own-goal flips the displayed roster** — picker title flips, roster array swaps, but `clock.addGoal(team, participant, isOwnGoal=true)` receives the SCORING team (the one whose score increments), NOT the rendered roster's team. Critical correctness invariant: `team` prop tracks the SCORING team independently of which roster is shown. (i) **Soft-limit stoppage alarm** — when stoppage_h1/h2_seconds exceeds `MAX_STOPPAGE_SOFT_LIMIT_SECONDS` (180 s), chip turns red and pulses (doesn't auto-end the half — just nudges). Driven from the same hook display-memo so per-tick fresh. (j) **800-line file rule is a guardrail, not a target** — Slice 2B-D's first finished implementation hit 828 LOC in RefEntry.tsx; extracted the 5 picker components + helpers into `RefEntryPickers.tsx` as a single refactor commit before close-out. Both files dropped well into typical range (RefEntry 555 / Pickers 293). Doing extraction at slice-close (rather than after slice 2B-E grows it further) was lower-friction. (k) **YAGNI > over-engineering for tunables** — REGULATION_HALF_MINUTES etc. ship as TS constants; deferred app_settings plumbing until a second league configures differently. **Migrations on live DB: 31 (unchanged).** Live: https://ffc-gilt.vercel.app. **S045 — slice 2B-E: RefEntry post-match review + submit_ref_entry wiring.** 4 commits, no migration (client-only). Replaces the `mode === 'post'` placeholder + the disabled `END MATCH` stub with a real flow. Adds `'review'` mode to `useMatchSession`'s `MatchMode` (between `'live'` and `'post'`); new `endMatch()` / `confirmSubmit()` / `reopenLive()` actions. `useMatchClock` gains `fulltime_at: string | null` on `ClockState` plus three mutators: `endMatch()` (captures `fulltime_at`, finalises any open pause into the current half's stoppage, emits a `'fulltime'` event with stamp computed pre-pause-finalisation), `reopen()` (drops trailing fulltime event for BACK TO LIVE), `deleteEvent(ordinal)` (refuses pause/resume/halftime/fulltime events; reverses score for goal/own_goal). `useMatchClock(...)` call lifted from inside `<LiveConsole>` to `RefEntry()` top-level so Live + Review share the same hook instance (the hook already tolerates null sessionStorageKey + null kickoffIso so calling it before mode flips to live is a no-op). Defensive `??`-normalisation in `readClockState` so legacy persisted state without `fulltime_at` hydrates as `null` not `undefined` (the latter would have spuriously tripped the "already ended" guard). New `<ReviewConsole>` (final-score block with winner-highlight or DRAW badge, MOTM card with change/clear, optional notes textarea max 500 chars, full chronological event log with `<EventDeletePicker>` confirm sheet for goal/own_goal/yellow_card/red_card rows only, BACK TO LIVE + SUBMIT TO ADMIN action row, error banner). New `<PostSubmittedView>` (green checkmark + final-score readout + MOTM line + "close this tab" copy). `buildSubmitPayload(state, payload, notes, winner)` aggregates per-player goals (only `'goal'` events with matching `profile_id`/`guest_id` AND matching roster team — own_goal events do NOT credit the participant) + cards + is_motm, derives `result` from scores, strips client-only `committed_at`/`half` fields, includes timing block, sends as `as unknown as Json` (Supabase generated `Json` carries a structural index signature that hand-written interfaces lack — precedent in AdminMatches.tsx). On submit success: token server-burned, mode flips to `'post'`, `:clock` localStorage cleared (best-effort try/catch). On error: surface message in banner, allow retry. Files: `useMatchSession.ts` extended with mode + actions (~190 LOC); `useMatchClock.ts` extended with `fulltime_at` + 3 new mutators + defensive normalisation (~640 LOC); `RefEntry.tsx` rewritten with hook lifted to top + ReviewConsole + PostSubmittedView (~750 LOC); `RefEntryPickers.tsx` adds EventDeletePicker (~340 LOC); `ref-entry.css` appends ~250 LOC of review + post styles. Commits: plan `5dd1be8` · hooks `6c0b4aa` · review console + post view + submit wiring `0348ce4` · close-out. **Patterns (additive):** (a) **Lifting a stateful hook to share between sibling routes** — when two siblings need the same authoritative state and the hook tolerates null inputs, lift the call to the common parent; cleaner than threading through context. (b) **Defensive `??`-normalisation on hydrate when persisted shape evolves** — adding a field to a persisted-state interface deserialises legacy state with `undefined`; normalise every field in the read path rather than version-bumping. Cheap and forward-compatible. (c) **`result` is derivable from scores** — `submit_ref_entry` accepts both but client computes via `useMemo` so they can't drift. (d) **Own-goal score-reversal mirror invariant** — `addGoal(team, ..., isOwnGoal=true)` records `event.team` = SCORING team (which for own_goal is OPPOSITE of participant's roster team), so `deleteEvent` for own_goal decrements the OPPOSITE of `event.team` to undo cleanly. Subtle; comment at every callsite. (e) **Pause/resume/halftime/fulltime events are non-deletable in review** — clock-machine transitions; deleting them corrupts stoppage/break/fulltime state. UI hides the affordance; `deleteEvent` returns `prev` unchanged for these types as a belt-and-braces defence. (f) **`as unknown as Json` for jsonb RPC args** is the established pattern for hand-written interfaces that lack the structural index signature Supabase's generated `Json` type carries. (g) **Top-level hook needs fallback param** — `useMatchClock` reads `format` from `payload.matchday.effective_format` (null in loading/invalid modes); pass `'7v7'` default safely (no-op state ignores it). End-to-end live → review → submit needs a real token + Thursday matchday to test (carry-over to S046). **Migrations on live DB: 31 (unchanged).** Live: https://ffc-gilt.vercel.app.

FFC is a mobile-first PWA for managing a weekly 7v7 friends football league: Monday poll → Thursday game cycle, with match history, leaderboard, seasons, awards, and WhatsApp share integration.

## Stack
- **Frontend:** React 19 + Vite 8 + TypeScript 6 (PWA via `vite-plugin-pwa`)
- **Backend:** Supabase (Postgres + RLS + Auth + Edge Functions + Storage)
- **Auth:** email/password + Google OAuth
- **Deploy:** Vercel (GitHub auto-deploy from `main`, Root Directory = `ffc`)
- **Repo:** `github.com/mmuwahid/FFC` (private)
- **Supabase project:** `hylarwwsedjxwavuwjrn` (`ffc` on new FFC org, region `ap-south-1` Mumbai, Free tier)
- **Vercel project:** `prj_2NszuyOepArCTUAJCOxH8NsAAeSv` (`ffc` on `team_HYo81T72HYGzt54bLoeLYkZx`)
- **Live URL:** https://ffc-gilt.vercel.app

## Philosophy
- **Reuse PadelHub patterns** — every critical rule from `Padel Battle APP/tasks/lessons.md` applies here too. READ IT BEFORE WRITING CODE.
- **Mockup-first workflow:** HTML mockup → user review → finalize → implement. Never skip.
- **Phased rollout:** Phase 1 (Core Weekly Cycle) ships before anything else.
- **UI conventions:** all app-wide design rules live in `docs/ui-conventions.md`. Read before building or reviewing any screen.

## Folder layout
```
FFC/
├── CLAUDE.md                  ← you are here
├── _wip/                      ← scratch / draft files (clean weekly)
├── archive/                   ← retired docs / code
├── docs/
│   ├── ui-conventions.md      ← app-wide UI rules (dates, colour, layout, naming)
│   ├── platform/              ← platform-specific implementation notes
│   └── superpowers/specs/     ← design specs (YYYY-MM-DD-*.md)
├── ffc/                       ← Vite app
├── mockups/                   ← HTML mockups before implementing screens
├── planning/                  ← masterplan V1, V2, V3... (version always, never overwrite)
├── sessions/                  ← per-session logs (S001, S002, ...)
│   └── INDEX.md               ← session index (authoritative history)
├── shared/                    ← shared assets between sub-projects (logos, icons)
├── supabase/                  ← SQL migrations, Edge Function source
└── tasks/
    ├── todo.md                ← running todo (NEXT SESSION section at top)
    └── lessons.md             ← FFC-specific lessons (inherits PadelHub's too)
```

## Operating rules (inherited from PadelHub)
1. **Mockup-first.** No screen gets built without an approved mockup in `mockups/`.
2. **Plan docs versioned.** `planning/FFC-masterplan-V1.0.md`, never overwrite — create V2.0 for revisions.
3. **No files at repo root** except CLAUDE.md and this top-level readme.
4. **Temp/draft files go in `_wip/`**. Clean weekly.
5. **Session log every working session.** `sessions/S###/session-log.md` + entry in `INDEX.md`.
6. **NEVER regenerate user documents** — edit originals.
7. **Verify DB columns before writes** (Critical Rule #12 from PadelHub lessons).
8. **Plain-object React Context** — no useMemo cascades (Rule #13).
9. **No hooks after conditional returns** (Rule #14).
10. **Safe-area insets** on all fixed-position mobile elements. Implementation: `docs/platform/iphone-safe-area.md`. Mockup review checklist: `docs/ui-conventions.md` ("Mockup review checklist" section).
11. **Supabase CLI:** `npx supabase` (same as PadelHub — global install is broken on work PC).
12. **Git repo temp clone path on Windows:** full path `C:/Users/UNHOEC03/AppData/Local/Temp/FFC`. Never use `/tmp/` inside Node `fs` calls — it resolves to `C:\tmp\` incorrectly.

## Cross-PC protocol
Working across work PC (`UNHOEC03`) and home PC (`User`).
- **OneDrive IS the main working tree** (reverted at S018 from the S015 migration-out plan). Path: `C:/Users/UNHOEC03/OneDrive - United Engineering Construction/11 - AI & Digital/Works In Progress/FFC/`. Edit files here directly.
- **Separate-git-dir architecture** — the `.git/` lives OUTSIDE OneDrive, one per PC, so OneDrive sync never touches git internals. Work PC: `C:/Users/UNHOEC03/FFC-git/`. Home PC: `C:/Users/User/FFC-git/`. The OneDrive `.git` file is a text pointer (`gitdir: <path>`) that must be rewritten on each PC to point at its local git dir — OneDrive syncs the pointer across PCs so it needs updating whenever you switch machines.
- **Sync via git, not OneDrive file sync.** OneDrive syncs the working tree across PCs as a convenience, but the authoritative state is `origin/main`.
- **Git identity for FFC commits:** `git -c user.name="Mohammed Muwahid" -c user.email="m.muwahid@gmail.com"` (repo-local config already set on both PCs).
- **Windows `gitdir:` pointer must use forward slashes.** Example on work PC: `gitdir: C:/Users/UNHOEC03/FFC-git`.

### Session-start sync protocol (MANDATORY — run before any work)
Every session must begin with this check to detect and resolve cross-PC lag, because OneDrive may have synced working-tree files from the other PC while this PC's local `.git/` stayed at an older HEAD. Symptoms: `git status` shows dozens of "modifications" or "untracked" files that are actually already-pushed commits from the other PC.

1. **Detect which PC you're on** — `echo $USERNAME` (or check `C:/Users/<name>/`). Work PC = `UNHOEC03`, Home PC = `User`.
2. **Fix the `.git` pointer** — read `FFC/.git`; it must say `gitdir: C:/Users/<this-pc-username>/FFC-git`. If it points at the other PC, rewrite the single line. Forward slashes only.
3. **Fetch + inspect** — `git fetch` then `git status -sb` and `git log --oneline -5`.
4. **Diagnose one of three states:**
   - **(a) Clean + up-to-date with origin/main** → proceed to work.
   - **(b) Behind origin/main AND working tree shows "modifications" matching the ahead commits** (classic cross-PC lag — OneDrive synced the files, local git didn't see the commits): run `git stash push --include-untracked -m "<pc>-sync-s###"` → `git pull --ff-only` → `git stash drop` (the stash content is identical to HEAD, safe to discard). Verify clean via `git status -sb`.
   - **(c) Genuinely uncommitted local work** (modifications that are NOT on origin/main) → this is real WIP from a session that never pushed. Do NOT stash-drop. Ask the user before touching it.
5. **Announce the PC + HEAD** in the session briefing so it's obvious which side was lagging.

The lagging-side symptom looks identical to (c) — the differentiator is whether the ahead commits on origin/main already contain those changes. A quick check: `git diff HEAD origin/main --stat` lists the same files that appear as "modified" or "untracked" in `git status`. If yes → state (b). If `git status` has files NOT in that diff → mixed state; ask.

## Current state (S020 close, 22/APR/2026)
- **Live:** https://ffc-gilt.vercel.app — full auth flow. `/login` (email/password + Google OAuth buttons), `/signup` (3-stage: email → who-are-you → waiting), `/admin/players` (Pending/Active/Rejected tabs with approve/reject RPC wiring), `/poll` stub loads under 4-tab RoleLayout for players or 5-tab for admins. All deep-link URLs resolve (SPA catch-all rewrite).
- **Database:** 20 tables + 12 migrations (`0012_grants.sql` added in S019). 20 RPCs. RLS on all tables. 2 real players in `profiles`: super-admin `m.muwahid@gmail.com` (id `cce905a8…`) + Test Player `m.muwahid05@gmail.com` (id `ca3181b2…`). 1 rejected row: `m.muwahid+reject@gmail.com` (id `7f8bb630…`). 2 pending_signups rows both resolved.
- **Auth:** Email confirmations DISABLED (admin approval is the Phase 1 gate). Google OAuth provider enabled in Supabase; GCP project `FFC App` (ID `ffc-app-494112`) in Testing mode; OAuth 2.0 Web Client `FFC Web` (Client ID `991515683563-ncjuidcn08psinv7oq8jb9kevp4k6g32.apps.googleusercontent.com`). Test users allowlisted. Redirect URLs: `https://ffc-gilt.vercel.app/**` + `http://localhost:5174/**`.
- **Types:** `ffc/src/lib/database.types.ts` (1816 lines). Zero TS errors.
- **Repo:** `main` at `0e62ffd`. S020 shipped 3 commits: `8f8668e` (S019 bundle), `dca48cf` (D6 UX fix), `0e62ffd` (SPA rewrite).
- **`ffc/vercel.json`** — LOAD-BEARING. Ships the SPA catch-all rewrite. Deleting it will 404 every non-root URL.

### Live operational gotchas
- **Supabase MCP PAT is scoped to PadelHub org only.** `execute_sql` returns 403 on FFC project. Workaround: Supabase CLI (`npx supabase db push`, `db query --linked`) throughout. Update PAT in Claude settings → MCP connectors → Supabase if MCP access to FFC is wanted.
- **`supabase gen types typescript --linked 2>/dev/null`** — the `2>/dev/null` redirect is mandatory; "Initialising login role..." diagnostic goes to stdout and corrupts the types file without it.
- **Windows OneDrive `&`-in-path bug:** `.bin/*.cmd` batch wrappers truncate at `&` in "11 - AI & Digital". FFC's `package.json` uses `node ./node_modules/<pkg>/bin/<bin>` direct invocation, not `npm run`. Vercel Linux CI is unaffected.
- **`supabase link`** uses cached auth token — no DB password needed on a machine already authenticated.
- **Supabase "Confirm email" must stay OFF for Phase 1.** Flipping it ON will re-break `Signup.tsx` Stage 1 silent-stuck bug (signUp returns session:null → onAuthStateChange never fires). Before flipping ON, add the "check your inbox" handler to `Signup.tsx`.
- **Supabase email validator rejects `example.com` and throwaway domains.** Test emails MUST be real Gmail or `+tag` aliases. Convention: `m.muwahid+s###<role>@gmail.com`.
- **Google OAuth consent screen shows `hylarwwsedjxwavuwjrn.supabase.co`, not "FFC".** Platform limitation in Testing mode + default Supabase callback URL. Fix requires Supabase Pro custom domain ($25/mo) — not worth it for a private league.
- **Google Cloud Test Users modal "Ineligible accounts not added" is a red herring on duplicate-case adds.** Gmail is case-insensitive at the SMTP level; the add actually succeeded.
- **Vercel static-file precedence over rewrites is automatic.** Catch-all `/(.*) → /` does NOT break `/sw.js`, `/manifest.webmanifest`, `/ffc-logo.png`.
- **Terminal roles (`rejected`, future `banned`) must auto-signOut in `AppContext.tsx`** — not just render a display flag. Handled for `rejected` in `dca48cf`. Apply the same pattern to any future terminal role.

### Next session (S021)
Phase 1 Step 3 is DONE. S021 candidates: (1) Google OAuth Path B end-to-end retest on production (config is in place). (2) PWA logo variants (32/180/192/512 PNG + SVG master) wired into `manifest.webmanifest`. (3) `Signup.tsx` confirm-email handler (latent fix for future). (4) `admin_audit_log` column audit (S020 D5 query failed on `target_table` name). (5) Step 4 of V2.8 — check masterplan §17 for next milestone. (6) Palette re-alignment (backburner).

## Session history
Session-by-session narrative lives in `sessions/INDEX.md` and per-session logs at `sessions/S###/session-log.md`. Do not duplicate here.

Durable lessons across sessions: `tasks/lessons.md` (inherits PadelHub's lessons too).
