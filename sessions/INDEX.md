# FFC Session Index

| # | Date | PC | Topic | Outcome |
|---|------|----|----|---------|

> **S001–S059 archived** to `sessions/_archive/INDEX-pre-s060.md` (rotated 04/MAY/2026 to keep this file under the 30 KB / 1,000-line trim threshold). Per-session logs at `sessions/S###/session-log.md` are unchanged.

| S060 | 30/APR/2026 | Work | Phase 3 payment tracker close-out (verified shipped) + PR #39 triage + Issue #41 ref name & bandage injury tag + #38 CRITICAL topbar touch targets + slop-mockup lesson | **2 implementation commits + this docs commit. 1 migration applied (0064). Live DB: 63 → 64. `main` pushed clean to `fb0b542`.** **Phase 3 payment tracker** — inventoried; backend complete since S056 mig 0055 (10 RPCs + 2 triggers + realtime), frontend complete since S056+S058 (`Payments.tsx` + `PaymentLedgerSheet.tsx` + CSS restored verbatim). Live-verified `/payments` empty state at dev preview: header / season pill / summary strip / banner-hidden / `No matches played this season yet.`. RPC `get_season_payment_summary` POST → 200. Empty because Game 31 was approved before mig 0055; spec §11 explicitly accepts no historic backfill. V3.0:147 marked verified-shipped in todo.md; full pipeline live-verification moved to Thursday-matchday block. **PR #39 triage** — atomosh `feature/fix-login` was 69 commits behind main, mergeStateStatus DIRTY; net diff −19,315/+1,639 would have erased S053–S059 (16 migrations 0047/0049/0051–0063, Awards.tsx, Payments.tsx, PaymentLedgerSheet.tsx, shareMatchCard.ts, render-match-card EF, 7 plan/spec docs). Root cause: PR #9 squash-merged 29/APR 11:22Z but atomosh kept pushing to the same branch from old base `d8c8938`; six PRs landed on main during the next 36h, branch never pulled any of them. Posted comprehensive triage comment at `https://github.com/mmuwahid/FFC/pull/39#issuecomment-4352366245` with impact tables, commit-by-commit timeline, and 9 workflow rules to prevent recurrence (never reuse branch after squash-merge, sync daily, delete merged branches, trust mergeStateStatus, etc). Decision: don't merge; awaiting atomosh response. **Mockup-first slop-mockup correction cycle** — first #41 mockup invented a fresh match-card design (gold WINNER text, gold-bordered VS pill, uniform dark splitc halves, square fake "F" logos, separate dark footer strip) instead of using live `matches.css` ruleset. User rejected hard ("this is much worse now. why are you not following the existing format from live database?"). Located canonical CSS at `ffc/src/styles/matches.css` (143L, separate from `index.css` where `.mt-screen` lives — cross-file rule sets are a recurring trap). Rebuilt mockup with matches.css copied verbatim into `<style>` + only `.mt-card-ref-pill` and `.mt-stat-icon--injury` added. Approved. **Lesson captured in 3 places:** sharpened `tasks/lessons.md` Critical Rule #6 from "Match existing app styling" to "Mockup matches live state verbatim, additions only" with explicit `grep -rn "\.<class>" ffc/src/styles/` instruction; added S060 patterns section (verbatim-mockup rule, screenshots-are-ground-truth rule, inline-SVG-for-cross-platform-emoji rule); created auto-memory `feedback_mockup_no_redesign.md` + index pointer. **Phase #41 `b962291`** — Migration 0064 adds `matches.ref_name TEXT NULL` + `pending_match_entries.ref_name TEXT NULL` + `pending_match_entry_players.is_no_show BOOL NOT NULL DEFAULT false`; `submit_ref_entry` reads `p_payload->>'ref_name'` and per-player `is_no_show`; `approve_match_entry` copies `ref_name` from pending → matches and `is_no_show` from `pending_match_entry_players` → `match_players`. Frontend: `useMatchSession.ts` extended with `refName` + `injuredIds: Set<string>` + `toggleInjured`, `PersistedState` extended (forward-compat hydration). RefEntry pre-match — required ref-name input above KICK OFF, gated `≥2 chars`. RefEntry review — new "🩹 Injured Players" section per team with toggle button. RefEntry submit payload extended with `ref_name` + per-player `is_no_show`. Matches.tsx — `ref_name` added to query, gold REF pill renders in banner middle when set, 🤕 → 🩹 swap in `ParticipantBadge`. CSS: `.mt-card-ref-pill` + `.mt-stat-icon--injury` in matches.css; `.ref-name-*` + `.ref-review-injuries-*` + `.ref-review-injury-btn--active` in ref-entry.css. **Phase #38 CRITICAL `fb0b542`** — `.app-topbar-bell` + `.app-topbar-avatar` 36×36 → 44×44 per Apple HIG; font-sizes tuned (bell 18→20, initials 13→15); avatar radius 12→14. Verified live via `preview_inspect`. Single CSS file commit. **Verification:** `tsc -b` EXIT 0 across both commits; mig 0064 applied via `npx supabase db push --linked`; types regen had 13 ref_name/is_no_show occurrences; /matches DOM eval confirmed banner renders correctly with 0 ref pills (Game 31 approved pre-mig); /payments DOM eval confirmed empty state renders. Live: https://ffc-gilt.vercel.app. **Open GitHub: 1 PR (#39) awaiting atomosh response. 1 issue still open: #40 player-name standardization (opened mid-session by atomosh, deferred to S061).** |

| S061 | 30/APR/2026 | Home | Issue #40 player formal_name + #41 FIFA player table + atomosh PR #42 drag-and-drop roster | **Live DB: 64 → 65 (mig 0065). Final HEAD: `928e511`.** (a) **PR #42 (atomosh drag-and-drop roster)** merged. (b) **Issue #40 closed** — `formal_name TEXT NULL` on profiles (mig 0065), `v_season_standings` prefers formal_name via COALESCE, `set_player_formal_name` RPC, AdminPlayers "Formal name" field, Matches.tsx + MatchDetailSheet.tsx name-resolution updated. (c) **Issue #41 (FIFA player table)** — approved mockup at `mockups/match-card-fifa-options.html`; two-column player table with goal badge / name / icon cluster replacing old scorer footer; Black column mirrored via row-reverse; MOTM ⭐ at end of icons; `ParticipantBadge` component + all old scorer CSS removed. _(Session log reconstructed from git history — original session was interrupted before logging.)_ |
| S062 | 01/MAY/2026 | Home | #38 design-system polish — focus-visible / radius / skeletons / typography / spacing / state-flips | **10 commits between `b65da7a..120accc`. No migrations.** `#38` audit fully closed (CRITICAL + all HIGH + all POLISH boxes ticked). **HIGH:** focus-visible ring + radius tokens + Awards shimmer skeleton (`b65da7a`); shimmer skeletons for Settings / Payments / AdminPlayers / AdminMatches / AdminSeasons (`229d5e8`). **POLISH:** typography tokens `--text-xs..2xl` + global `button:active` press (`d969b5d`); `.admin-seg` tab transitions + card press scale(0.985) (`d591496`); `app-state-fadein` state-flip + team colour tokens + reduced-motion safeguard (`730969c`); 4pt spacing tokens `--space-1..10` + Settings padding-top unification (`abbffa0`). _(Session log reconstructed from git history.)_ |
| S063 | 01/MAY/2026 | Work | Live match-day hardening — RefEntry submit + MatchEntryReview + share PNG overhaul + 4 GH issues | **17 commits, 3 migrations (0066/0067/0068), 5 EF redeploys. Live DB: 65 → 68. Final HEAD: `36b2963`.** First-real-match-day diagnostic session: ref submitted entry crashed on enum cast (winner→result, `e6cd218`); MatchEntryReview Approve hidden behind bottom nav (`61c153d`); "[object Object]" toast hid the real PG error (`3ac36ef`); root cause was `INSERT INTO notifications SELECT DISTINCT ..., 'match_entry_approved', ...` failing the `text→notification_kind` cast — fixed via mig 0066 explicit `::notification_kind` casts in 4 RPCs (`7f39c2b`). UX polish: Reject filled red / Approve filled green (`e23740f`); MOTM gold+star (`cf93728`); Player Aggregates 2-col + zero-stat hide (`9205f9b`); RefEntry event-strip names (`d8b1b8a`); AdminMatches FINAL Black Team Wins (`68517f9`); Matches tab ⚽ back in goal badge (`c93b1ab`); MatchDetailSheet rosters side-by-side WHITE | BLACK (`ad152dd`); MatchDetailSheet footer "Game N" with games_seeded (`8461ad1`). **Share PNG overhaul:** EF Satori Fragment+display:flex bug (`c93b1ab`); Game-N + force=true on success-state share + inline gold MOTM + mig 0067 games_seeded fix (`8cfdaa8`); RENDER_VERSION cache invalidation (`fb5a241`); full layout rewrite — bigger crest, single-line title, side-aligned scorers, twemoji `graphemeImages` for ⭐⚽🟨🟥, mig 0068 cards in payload (`659e105`). **GH sweep:** #44 splash crest CSS, #45 W/D/L colours, #46 nav GPU compositing, #47 sticky header z-index (`36b2963`). **Open GitHub at end:** 0 PRs · 1 open issue (#43 dark/light mode, deferred — needs 15-scope inversion). |

| S064 | 01/MAY/2026 | Home | PR #49 merge — #43 light/dark mode all screens + #48 awards view fix (mig 0069) | **1 merge commit `29c4986`. 1 migration (0069 — already applied from work PC). Live DB: 68 → 69. Open GitHub: 0 PRs · 0 issues.** Short session: cross-PC lag fixed at start (`.git` pointer → `User/FFC-git`; stash-pull-drop fast-forwarded 18 commits). PR #49 (atomosh) reviewed — TS build clean, 1 commit ahead of main, no conflicts. **#43:** extracted shared `applyThemeClass()` to `lib/theme.ts`; `AppContext` now reads + applies `theme_preference` on profile load; `.theme-light` + `@media prefers-color-scheme:light .theme-auto` overrides added for all 15 per-screen brand blocks (13 in `index.css`, `.mer-screen` in `match-entry-review.css`, `.mt-card` hardcoded colours in `matches.css`). **#48:** `v_season_award_winners_live` view recreated with `JOIN matchdays` + `NOT is_friendly` + `NOT is_no_show` filters — aligns awards with leaderboard (`v_season_standings` has had these since mig 0013). Merged, pushed, Vercel auto-deployed to Production. |

| S065 | 01/MAY/2026 | Home | NumberInput component — fix backspace UX on all 13 numeric inputs | **1 commit `b5046fe`. No migrations. Live DB: 69 (unchanged). Open GitHub: 0 PRs · 0 issues.** Short focused session. Created `ffc/src/components/NumberInput.tsx` (65 lines): `type="text"` + `inputMode="numeric"` + `pattern="[0-9]*"` to avoid `type="number"` quirks; local `displayValue: string` state decoupled from parent `value: number` while focused; `onChange` fires props callback only when non-empty (prevents snap-back-to-zero bug on backspace); `onBlur` coerces empty/NaN to `min`, clamps out-of-range to `[min, max]`. Migrated 13 call-sites across 4 groups in `AdminMatches.tsx` (ResultEntrySheet score boxes, scorer rows, EditResultSheet score boxes, post-match edit scorer rows), 1 in `AdminPlayers.tsx` (ban-days picker), 2 in `MatchEntryReview.tsx` (final score boxes). `tsc -b` EXIT 0. |

| S066 | 04/MAY/2026 | Work | Token-efficiency audit (bloat trim) — closes #50 | **3 chore commits between `b5046fe..609ca3a`. No migrations. ~72K tokens saved per session start. Active context working set: 359 KB → 79 KB.** _(Session log reconstructed from git history at the start of S067.)_ `f6c674b` archived S001–S059 from INDEX (188→11 KB), S049–S063 history from todo (105→12 KB), S058+S059 narrative from lessons (65→56 KB), trimmed CLAUDE.md "Current state" (15.4→13.5 KB), added `.claudeignore` (81 lines). `d27b229` moved CLAUDE.md operational-gotchas (1.5K tokens) to `docs/platform/operational-gotchas.md` and per-screen brand tokens to `docs/ui-conventions.md`; tightened Cross-PC protocol (28→16 lines); CLAUDE.md 13.5→6.5 KB. `609ca3a` closed #50 — dropped 14 duplicate lessons.md rules now owned by operational-gotchas.md; archived planning V1.0–V2.8 (118 KB), `2026-04-*` plans (568 KB) and specs (962 KB) to `archive/`; simplified `.claudeignore` to `_wip/ + archive/`. |
| S067 | 04/MAY/2026 | Work | Injury / Unavailable list — spec + plan (no code) | **2 docs commits. No migrations. Live DB: 69 (unchanged). Open GitHub: 0 PRs · 0 issues.** Brainstorm + design + implementation plan for V3.0:147 (long-term unavailability flag on profiles, distinct from per-match `is_no_show`). User asked to add H2H comparison (V3.0:144) too — decomposed into a separate spec/plan for a follow-up session (independent subsystem). 6 clarifying questions yielded: typed states (Injured/Travelling/Suspended/Retired) · self-serve from Settings + admin override · optional return date with pg_cron auto-clear at 00:00 UAE · hide from Poll · stay in rank on Leaderboard with muted pill · notifications to admins on self-mark + auto-clear (admin override silent). Data-model approach A picked: 3 cols on `profiles` (no history table — YAGNI). **Spec at `docs/superpowers/specs/2026-05-04-injury-unavailable-design.md`** — 6 UI surfaces, 4 RPCs, edge cases (locked-roster no-auto-cascade, open-poll-vote cleared on OUT-flip, past-date rejected, retired+until rejected, transition fires fresh push, cron miss caught next day). **Plan at `docs/superpowers/plans/2026-05-04-injury-unavailable.md`** — 12 bite-sized tasks (T0 mockup gate · T1 mig 0070 · T2 apply+types · T3 `<UnavailabilityPill>` · T4 AppContext · T5 Settings · T6 AdminPlayers · T7 Poll · T8 AdminRosterSetup · T9 Leaderboard · T10 Profile · T11 push+session-log). S063 lessons baked in: explicit `::notification_kind` casts; `extractErrMessage` toast wrapper. Backfilled S066 session log mid-session. **Execution deferred to S068** — 12 tasks × subagent-driven was too heavy for remaining context budget. |

**Next session: S068** — (a) Mandatory session-start sync. Expected tip: `9e4cb8a` (or descendant). Live DB: 69 migrations. 0 PRs · 0 issues. (b) **Execute injury/unavailable list plan** at `docs/superpowers/plans/2026-05-04-injury-unavailable.md` via subagent-driven-development. Start at Task 0 (mockup) — Rule #1 user-approval gate before T1 migration. (c) **Then brainstorm + spec H2H comparison** (V3.0:144) as a separate spec/plan cycle. (d) **Live verification still owed Thursday matchday** (S063 RefEntry pipeline + MatchEntryReview + share PNG + MatchDetailSheet + GH polish). (e) **Backburner:** Awards backfill RPC; Resend custom sender domain; remaining Phase 3 stats backlog (multi-season comparison · player analytics · badges).

