# FFC Session Index

| # | Date | PC | Topic | Outcome |
|---|------|----|----|---------|
| S001 | 2026-04-17 | Work | Project kickoff — brainstorming Phase 1 | Approach A approved. Phase 1 design Section 1 presented. Sections 2–6 pending. Session paused for handoff to home PC. |
| S002 | 2026-04-18 / 19 | Home | Phase 1 sub-designs + masterplan V2.0 → V2.2 | Six Phase-1 sub-sections locked (3.1 captain helper · 3.2 last-5 strip · 3.3 self-signup · 3.4 ref entry · 3.5 +1 guest · 3.6 vote-order). Brand + palette captured. All S002 assumptions approved. Sections 2, 4, 5, 6 still pending. |
| S003 | 2026-04-19 | Home | Section 2 Data Model Parts 1–3 + masterplan V2.3 | Scope locked = full production DDL. Four architectural decisions locked (split commitments, MOTM single-source, separate pending queues, 3 RLS roles). Parts 1 (types + base), 2 (match data), 3 (poll + ref workflow) approved. Mid-session requirements batch: admin dashboards, super-admin tier, season archive, scheduled WhatsApp auto-post (Option A — native share sheet). Masterplan bumped to V2.3. Parts 4 & 5 pending. |
| S004 | 19/APR/2026 | Home | Section 2 closed + Section 1 approved | Part 4 (operational tables), Part 5A (views), Part 5B (SECURITY DEFINER RPCs — expanded to 11 incl. captain-pick helpers), Part 5C (RLS policies) all approved. Full Section 2 DDL written into the Phase 1 design spec (spec grew 324 → ~1150 lines). Four inline self-review fixes. User refinements: Section 1 formally approved; cron schedule revised (5 reminders Mon 17:00 / Tue 21:00 / Wed 20:00 / Wed 22:00 / Thu 12:00); late-cancel penalties revised (−1 after lock, −1 + 7d ban within 24h); captain formula simplified to three per-player criteria + early-season randomizer + White=weaker pair rule. Masterplan V2.4 bump queued for S005. |
| S005 | 19/APR/2026 | Home | Masterplan V2.4/V2.5 + Section 3.0 + §3.7 Poll screen + positions + theme | Masterplan V2.4 written (captures S004). Section 3.0 Navigation + IA approved (4-tab player / 5-tab admin / anon ref; 16 routes; auth-aware layouts; badge rules). §3.7 Poll screen speced (Depth B) and mockup v2 delivered — light + dark phones side-by-side + 5 alternate-state tiles. Mid-session additions: **5-position palette** (GK/DEF/CDM/W/ST — CM dropped) with colour-coded pills; primary required + secondary optional at signup; data model amended (new `player_position` + `theme_preference` enums + 3 new `profiles` columns + CHECK + index). **Light/dark theme toggle** added, stored on `profiles.theme_preference`. Masterplan V2.5 written. |
| S006 | 19/APR/2026 (home, partial unlogged) + 20/APR/2026 (work, close) | Home → Work | §3.13 Leaderboard · §3.14 Player profile · §3.15 Match-detail stub + data-model amendment | **§3.13 Leaderboard APPROVED** — four open decisions resolved (tiebreak chain, separate not-yet-played group, persistent sort, medal icons) + seven mid-review refinements (colour W-D-L triplet app-wide rule, column-header row, MP column added, fixed column widths, dashes removed, medal tint polish, penalty column dropped to post-match report). **§3.14 Player profile APPROVED** — P1–P5 resolved (live best-finish, fully tappable recent-matches, email sheet-only, no "You" chip, match-detail sheet as tap destination) + totals card scope dropdown (current season / past seasons / entire career). **§3.15 Match-detail sheet STUB** added as new Phase 1 scope item (read-only bottom sheet, full Depth-B deferred). Data model amended — new `leaderboard_sort` enum + `profiles.leaderboard_sort` column. **Guest-player stats scope locked (Q1–Q6)** but implementation deferred to S007. **§3.1-v2 captain helper layout locked** (one-screen mode toggle, pair-confirmation sheet) but full Depth-B deferred to S007. Masterplan not bumped — V2.5 remains authoritative; V2.6 queued for S007. |
| S007 | 20/APR/2026 | Work | Guest-stats DDL · §3.7 spec persisted · §3.14 refined (R1–R6) · §3.1-v2 reconciled · Masterplan V2.6 | **Guest-stats Q1–Q6 applied**: §2.1 +2 enums (`guest_rating`, `guest_trait`), §2.3 match_guests +6 columns +2 CHECKs, §3.5 invite flow amended with "Tell us about your +1" step, §3.7 mockup guest rows enriched (pills · rating chip · description). **§3.7 Poll screen Depth-B spec PERSISTED** — discovered S005 never wrote the spec into the design-spec file; reconstructed from scratch (~180 lines: purpose · 7 states · rendering contract · acceptance). **§3.14 Player profile REFINED (R1–R6)**: W-D-L alignment fix (CSS specificity collision root cause), last-5 circle centering fix, **MP added** to Season stats, Rank removed from KPI grid (still shown as card-header hint), **Totals card REPLACED with Achievements card** (⭐ MOTMs · 🔥 W-streak · 🎯 Goals · 🟨 Yellows · 🟥 Reds · 📉 L-streak), zero-match state updated. **§3.1-v2 Captain helper APPROVED** — Depth-B spec + v1 mockup (light formula-mode + dark randomizer-mode phones + 4 state tiles: balanced pair sheet · rank-gap warning sheet · zero-eligible fallback · guest subsection). Supersedes §3.1. Rank-gap>5 = amber warning, not a hard block. Guests visible in read-only subsection for pair-balance context but cannot captain. **Masterplan V2.6** captures all of the above + 2 new numbered sections (§11 Leaderboard Sort Preference · §12 Guest Player Stats). Design spec grew ~1,573 → ~1,990 lines. |
| S008 | 20/APR/2026 | Work | Cold-start + 4 decisions locked + safe-area research | Framing session that closed **all** queued decisions before handoff. **§3.7 Poll team-colour preview = Option A** (full state 8 — status card row + per-row `[W]/[B]` pills) — closes S005 open item. **§3.15 match-detail:** W/D/L chip from profile-owner perspective · guest row stats lighter (goals/cards only, no S007 chip/description) · wide-viewport 640w × 80vh provisional. **Settings = 6 rows** (added display-name shortcut; extras deferred) · state tile = push-permission-prompt (signed-out dropped, screen auth-gated). **NEW platform concern — iPhone notch / Dynamic Island.** User flagged; research completed (`_wip/iphone-safe-area-research.md`): `viewport-fit=cover` + `env(safe-area-inset-*)` pattern covers notch, Dynamic Island, and home-indicator on every iPhone. **Gap discovered:** all 5 approved S005–S007 mockups lack safe-area CSS; added as S009 retrofit item. Lesson logged: mockup review didn't enforce Rule #10 despite spec commitment. Zero design-spec edits this session — drafting happens S009. |
| S009 | 20/APR/2026 | Home | Safe-area v2 + 5 new Depth-B screens + 3 new features spec'd (PARTIAL close — forced pause) | Marathon. S008 items 0–6 **DONE**: §2.7 + §2.3 audit cols + 2 new RPCs landed · §3.0 Platform safe-area + CLAUDE.md Rule #10 paragraph · §3.7 State 8 "Teams revealed" added · §3.15 upgraded STUB → Depth-B · §3.16 Settings NEW · §3.17 Admin Players NEW · §3.18 Admin Matches NEW · 4 new mockups created. **Safe-area bug — 2 iterations.** v1 used `env(safe-area-inset-top, 59px)` — the fallback semantic is only for UNRECOGNISED vars, not zero-resolved values; desktop browsers return `0px` so fallback never fired and Dynamic Island overlapped titles. v2 hardcodes `--safe-top: 59px` etc. on `.phone` AND sets `.statusbar { height: var(--safe-top); display: flex; justify-content: space-between; }` so time/battery FLANK the island on left/right (matches real iOS), not pushed below (the second bug the user caught). All 7 retrofitted phone mockups now use v2 pattern. **FFC naming corrected** — "Friends FC" / "Friends Football Club" removed from CLAUDE.md + poll crest (historical session logs preserved). Memory saved. **Scope expansion mid-session:** State 7 button colour rule (green keep / red cancel, pending) · State 8 roster → 2-team layout (pending) · **NEW live captain draft visibility** (subagent B spec DONE at `_wip/item-b-draft-reroll-spec.md`, not integrated) · **NEW captain reroll on post-lock dropout** (subagent B spec DONE, not integrated) · **NEW formation planner §3.19** (subagent C in-flight at pause) · **Settings v2** applied by subagent A: dark default · remove `position_changed` · `poll_reminder` 2-min-before-close · add `dropout_after_lock`. Forced pause on tokens with subagent C still running + integration work not done. Items 7 (close-out · masterplan V2.7 · lessons v2 row) deferred to S010. |
| S010 | 21/APR/2026 | Home | 5 mockup reviews + user feedback pass + statusbar flex-shrink bug caught (PARTIAL close — spec integration deferred) | **Mockup review round for S009 deliverables.** User approved 4 out of 5: **Captain helper · Leaderboard · Match detail · Admin players** (no changes). User requested 5 amendments via subagent pass: (1) **Admin matches** full 14-player roster always-visible (tap-to-expand removed, 7 WHITE + 7 BLACK rendered inline); (2) **Settings** checkboxes → iOS-style pill toggles (12 switches, 10 ON red · 2 OFF muted); (3) **Player profile** KPI + Achievement tiles text cutoff — diagnosed as missing flex-column + min-height on tile parents, fix applied; (4) **Formation** major redesign — Dynamic Island clearance fix (`align-items: flex-end` + `padding-bottom: 8px`), team-colour header strip (WHITE/BLACK), 7-player roster with rotation chips, **NEW rotating-GK toggle** (Dedicated GK vs Rotate every 10 min), starting-GK picker, auto-assign rotation numbers 1–6; (5) **Poll mockup S009 backlog closed** — State 6 + State 7 green/red action buttons (durable green=safe / red=destructive rule), **State 8 restructured** into WHITE TEAM + BLACK TEAM 2-section layout (7+7 rows), **NEW State 6.5 "Draft in progress"** tile with live-chip + pulsing dot + two-column picks-so-far + pool + last-pick footer. 5 parallel subagents dispatched (A–E), all completed. **New durable rules captured in memory:** always-visible rosters (≤14 items) · pill switches > checkboxes for toggles · text-cutoff diagnostic order · **rotating-GK gameplay rule** (real FFC practice — captain picks who starts in goal, remaining 6 auto-assigned rotation numbers for 10-min swaps). **LATE-SESSION BUG DISCOVERY.** On re-review, user reported Profile + Formation still look broken. Live `preview_inspect` revealed `.statusbar` computed `height: 25px` not `59px` despite correct CSS. Root cause: `.phone-inner` is `display: flex; flex-direction: column` and `.statusbar` defaults to `flex-shrink: 1` — long content overflows the 844px phone and flex shrinks the statusbar, which pulls all content up behind the Dynamic Island. **Fix** = add `flex-shrink: 0` to `.statusbar` rule (statusbar v2.1 amendment). Not applied this session at user's request — logged as S011 item 0. Only Profile + Formation hit this because they're the only screens with content overflowing 844px. Close-out without masterplan V2.7 or spec integration — deferred to S011. |
| S011 | 21/APR/2026 | Home | Statusbar fix + spec integration (§3.7/§3.16/§3.18/§3.19) + masterplan V2.7 | **Item 0 (CRITICAL):** `flex-shrink: 0` applied to `.statusbar` in all 9 phone-frame mockups; verified 59px computed height on Profile + Formation via DOM inspection. **Items 1–5:** 3 WIP scratch files integrated into master spec — §3.7 updated (9 states, State 6.5 "Draft in progress", captain reroll sub-section, State 6/7 green/red buttons, State 8 two-section layout), §3.16 updated (dark default, 6 push keys, pill-toggle UI, Section-5 wiring stub), §3.18 updated (Phase 5.5, always-visible roster), §3.19 Formation planner inserted (full Depth-B spec, rotating-GK toggle, 12 acceptance criteria). **Item 6:** `planning/FFC-masterplan-V2.7.md` created — full S009+S010+S011 consolidation with complete data-model delta (3 new enums, 4 new tables, 2 new columns, 6 new RPCs, 2 new `app_settings` flags, 6 new notification types). Phase 1 design spec feature-complete. |
| S012 | 21/APR/2026 | Home | Mockup review round 2 + bug fixes + brand logo + 5v5/7v7 scope lock | **7 of 9 mockups APPROVED** (Poll · Admin Matches · Admin Players · Settings · Match Details · Leaderboard · Captain Helper). **2 mockups had bugs — fixed this session:** (1) **Player Profile** — Bug A: `.card` compressed to thin strips because `overflow: hidden` triggered flex auto→0 min-height; fix `.card { flex-shrink: 0 }`. Bug B: bottom tabbar appeared mid-scroll after Bug A unlocked content length; was `position: absolute; bottom: 0` which pins to scroll-content bottom not viewport; fix `position: sticky; bottom: 0; margin-top: auto`. Removed leftover `padding-bottom: 110px` on `.phone-inner`. (2) **Formation** — every direct child of `.phone-inner` compressed (pitch 112/505, roster 83/372, team-strip 34/50, pattern pills 19/37, gk-card 41/180); fix `.phone-inner > * { flex-shrink: 0; }`. GK picker converted from radio-card to native `<select>` per user feedback. Pitch SVG already had both goals + halfway + penalty boxes — just needed decompression. **Brand discovered** in `shared/`: `FF_LOGO_FINAL.pdf` (classic shield crest, gold "FF" monogram), `COLORS.pdf` (4-colour palette: black · white · khaki-gold #AEA583 · cream #EDE9E1). Palette gap identified (mockups use red+navy, not in brand) but user explicitly chose to keep current palette, swap crest only. Logo swapped on Poll (both phones) via `<img class="crest" src="ffc-logo.jpg">` — stopgap JPG with white-bg artifact on dark mode, flagged for S013 transparent-PNG fix. **NEW scope: 5v5/7v7 multi-format support** — default 7v7, toggleable per matchday, entire season can be 5v5 league. Scope decision: season default + per-matchday override. Data-model + poll-cap + formation-pattern spec updates deferred to S013. **Only Section 1 of the spec reviewed this session** — user jumped ahead to mockup feedback. Formal Phase 1 approval deferred to S013. |
| S013 | 21/APR/2026 | Home | Full spec walkthrough + S009–S013 delta consolidation + 5v5/7v7 multi-format spec | **All 10 spec sections reviewed and approved with edits** (§1, §2.1–§2.9, §3.0, §3.2–§3.6). **Item 1 — Section 1 non-goal fix** (captain draft / auto-pick scoping). **Item 2 — §2 Data Model:** S009–S011 deltas from V2.7 all landed — 3 new enums (`user_role+=rejected`, `draft_status`, `draft_reason`), 6 new `notification_kind` values, 2 new columns (`profiles.reject_reason`, `match_players.substituted_in_by`), **4 new tables** (`admin_audit_log`, `draft_sessions`, `draft_picks`, `formations`), **7 new RPCs** (`edit_match_result` + 6 captain-draft/formation) taking the count 13 → 20, `log_admin_action` helper + admin-audit convention, **4 new RLS policies** for the new tables, `formations` gated by `shared_at OR creator`, migration notes bumped. **Item 3 — §3.0 + §3.2–§3.6:** 10 drift fixes reconciling old sub-designs against authoritative §2 DDL (column-name drifts dating back to S002). Notable: `voted_at`→`committed_at`, `profiles.is_admin boolean`→`role user_role` enum, `match_guests.goals_scored`→`match_players via guest_id` (Section 2 prologue had flagged this since S003 — finally fixed), "your position changed" notification formally retired. **Item 4 — 5v5/7v7 multi-format spec** (S012 scope lock):  locked A(i) captain min-matches stays 5 · B(i) unified leaderboard · C(i) unified profile · D(i) per-season storage only. Landed `match_format` enum + `seasons.default_format` + `matchdays.format` nullable + 2 helpers (`effective_format`, `roster_cap` → 14/10) + expanded `formations_pattern_valid` CHECK to include 5v5 patterns (1-2-1, 2-1-1, 1-1-2). §3.5/§3.6/§3.7/§3.18/§3.19 all parameterised by roster_cap. §3.18 gets a Format chip at matchday creation + mid-poll format-change warning. §3.19 adds 5v5 coordinate preset table + rotation 1..roster_cap/2−1 + 13th AC. WhatsApp share template uses `{{roster_cap}}` placeholder. **Item 5 — S012 CSS contract persistence** into §3.14 + §3.19 AC blocks (flex-shrink:0 defensive rule · sticky-tabbar pattern · native select GK picker — all with diagnostic reference to lessons.md). **Item 8 — 3 WIP files archived** (`item-b-draft-reroll-spec.md` · `item-formation-planner-spec.md` · `item-settings-v2-amendments.md`); `_wip/` now empty. **New feedback memory saved:** `feedback_table_presentation.md` — DB/spec tables render as markdown tables, not prose walls. **Spec stats:** ~2940 → ~3100 lines · RPCs 13 → 20 · tables 16 → 20 · enums 15 → 18. **Deferred to S014:** masterplan V2.8 · logo rollout (user exports PNG/SVG) · formal Phase 1 approval · implementation kickoff · palette re-alignment (still on backburner). |
| S014 | 21/APR/2026 | Home | Masterplan V2.8 · formal Phase 1 approval · collaborator Word brief with 10 embedded mockups | **Masterplan V2.8 landed** (`planning/FFC-masterplan-V2.8.md`, 378 lines within the 350–500 target). Consolidates all S009–S013 deltas on top of V2.7. Structure: revision history (5 delta groups) · §§1–15 carryover pointing to V2.7 for unchanged sections · **§16 NEW — 5v5/7v7 Multi-Format Support** (decisions table + data model table + format-awareness convention table + UI parameterisation table) · Section 2 delta with SQL for new helpers (`effective_format` · `roster_cap` · `log_admin_action`) + CHECK expansion · 10-drift reconciliation table · **11-file migration order** (supersedes V2.7) · **implementation sequencing notes** (Steps 0–3 each with acceptance criterion). Heavy use of markdown tables per `feedback_table_presentation.md`. **Phase 1 design FORMALLY APPROVED** — `CLAUDE.md` status header flipped from "Brainstorming (pre-implementation). Design Phase 1 not yet fully approved. Code not started." to "Design Phase 1 APPROVED — implementation ready. Formally approved by user on 21/APR/2026 (S014) after masterplan V2.8 consolidation landed. Code not yet started. Next gate: repo + Supabase project + Vite scaffold." Memory file `project_ffc.md` updated with 4 edits (frontmatter description · opening paragraph · masterplan reference · Latest/Next session blocks). **Collaborator brief Word document built** at `docs/FFC-Collaborator-Brief.docx` (14.2 MB · 305 paragraphs · 33 archive files · **10 embedded PNG screenshots** of every approved mockup). Infrastructure: restarted preview server (`preview_screenshot` timed out, pivoted to headless Chrome `--screenshot` via `/c/Program Files/Google/Chrome/Application/chrome.exe`); caught a bash path bug (mixed forward/back slashes broke `${f}` expansion — fixed by using forward slashes end-to-end). 10 screenshots saved to `docs/brief-screenshots/` (~13 MB total). Built with `docx-js` via `docs/build-collaborator-brief.js` (Node script, reusable). Sections: Cover · Executive Summary · What We're Building · Core Features · Tech Stack · Current Progress · Data Model Snapshot · **10-page mockup gallery** (each with title · purpose · embedded image · highlight bullets · page break) · What's Next. Sanity-checked via zipfile + xml.etree (skill's `validate.py` crashed on Windows cp1252 encoding of its own Unicode output — not a docx problem). **Deferred to S015:** logo rollout (still blocked on user asset export) · implementation kickoff (Steps 0–2 of V2.8 sequencing + first feature slice) · palette re-alignment · masterplan §§4–6. |
| S015 | 21/APR/2026 | Home | Phase 1 implementation kickoff — Step 0 infrastructure live end-to-end | **Step 0 of V2.8 sequencing FULLY COMPLETE.** Workspace moved from OneDrive to `C:/Users/UNHOEC03/FFC/` (git-only sync going forward; OneDrive kept as read-only snapshot for safety). **GitHub repo `mmuwahid/FFC`** (private) created; 5 commits on `main` (initial 52-file design bundle + Vite scaffold + env-var boot logs + .vercel gitignore + length-check re-acceptance). **Supabase project `ffc`** (ref `hylarwwsedjxwavuwjrn`) created in new FFC org — region `ap-south-1` Mumbai (for UAE latency), Free tier, Data API enabled, automatic RLS on, automatic table exposure OFF. **Vercel project `ffc`** (`prj_2NszuyOepArCTUAJCOxH8NsAAeSv`) on existing team, Git-connected, Root Directory `ffc`, framework auto-detected as Vite, live at **https://ffc-gilt.vercel.app**. **6 env vars wired cleanly** (URL × 3, anon key × 3) via `vercel env add --value --yes` after discovering PowerShell `echo \| pipe` appends `\n` and corrupts values. **Anon key upgraded** from legacy JWT (`eyJhbGci…`) to new publishable (`sb_publishable_EbFLhm6kXbTJBqrge-A7vw_0LswX2EB`) to eliminate pending tech debt. **Step 0 acceptance verified twice** via DevTools console (first with JWT: URL + key prefix resolve; second with publishable + length canary: both values exact lengths 40 / 46 confirm zero newline drift). **Four infrastructure gotchas surfaced and captured in lessons**: (1) PowerShell `echo` always adds trailing `\n` — use `--value --yes` flags instead; (2) Go-compiled `gh` CLI fails TLS on networks with SSL-inspection (Windows OS cert store not consulted) — sidestep via Chrome for repo create + raw `git` (schannel backend = OS store); (3) Vercel CLI `--yes` does NOT auto-accept "all preview branches" — empty-string positional arg required: `vercel env add NAME preview "" --value V --yes`; (4) README-referenced paths must not point inside gitignored dirs (initial mockup path was under `.superpowers/` which is excluded — fixed by copying 10 mockups to tracked `mockups/`). **Five validated patterns** logged. **React 19.2.5** landed via `npm create vite@latest` (scaffold default); CLAUDE.md Stack line updated from "React 18" → "React 19" at close. **Logo rollout + Step 1 (scaffold elaboration) + Step 2 (11 migrations) DEFERRED to S016** — Step 0 alone was a genuine milestone worth closing cleanly. |
| S016 | 21/APR/2026 | Home | Step 1 of V2.8 — elaborate Vite scaffold + home-PC workspace alignment | **Step 1 of V2.8 FULLY COMPLETE and LIVE** at https://ffc-gilt.vercel.app. Elaborated the minimal Vite scaffold into a real PWA shell: Supabase client singleton with fail-fast env validation (`ffc/src/lib/supabase.ts`), plain-object React Context per Rule #8 (`ffc/src/lib/AppContext.tsx` with `session` / `role` / `loading` / `signOut`; subscribes to `onAuthStateChange`), class `ErrorBoundary`, safe-area-aware global CSS with dark-default theme + `:root.theme-light` opt-in + layout primitives (`.app-shell`/`.app-topbar`/`.app-bottom-nav`/`.app-main`), three layouts (`PublicLayout` · `RoleLayout` 4-tab + 5th Admin tab when role ∈ admin/super_admin · `RefLayout` stripped), 14 page stubs (1 Welcome with real CTA content + 13 spec-ref stubs), auth-aware react-router tree with index route conditional on session, inline `#ffc-splash` painted pre-JS + `requestAnimationFrame` hide on React commit, `vite-plugin-pwa` with `generateSW` + `cacheId=ffc-<ISO-timestamp>` (Rule #19), manifest.webmanifest (standalone/portrait/theme `#0e1826`), `workbox-window` SW registration in PROD only with `messageSkipWaiting` on `waiting` event. Deleted Vite template cruft (App.css, assets/*, icons.svg). **Home-PC workspace aligned** to the separate-git-dir architecture agreed in S015 follow-up commit `fa1c0a8`: OneDrive working tree + `C:/Users/User/FFC-git/` external `.git/` (matches work PC's `C:/Users/UNHOEC03/FFC-git/` pattern). Rewrote the OneDrive `.git` pointer from the work-PC path to `gitdir: C:/Users/User/FFC-git`; set `core.worktree` + `core.autocrlf=true`; the 24 pre-existing "modifications" confirmed as pure CRLF drift via `git diff --ignore-cr-at-eol --name-only` returning empty. **Two commits shipped:** `c7b2b74` Step 1 scaffold (36 files, +8,344/−422) — first Vercel auto-deploy ERRORED in 7s with ERESOLVE (vite-plugin-pwa@1.2.0 peers vite≤7, scaffold on vite@8.0.9); `dd0c00b` fix = `ffc/.npmrc` with `legacy-peer-deps=true` → redeploy Ready in 15s. **Live verification** via curl: `/` 200 (2088 B) with correct meta tags + inline splash + manifest link; `/manifest.webmanifest` 200; `/sw.js` 200. **Local preview acceptance** via preview_start on port 5174: Welcome renders under PublicLayout (no bottom nav); `/login` stays PublicLayout; `/poll` flips to RoleLayout with 4-tab bottom nav; zero console errors. **`gh` CLI lesson retraction** — user confirmed gh works fine on both home PC (verified via `gh auth status`) AND work PC; S015's Go-binary TLS lesson is outdated and needs editing at close. **Direct-to-main workflow re-confirmed** — every commit since repo creation pushes to main; Vercel auto-deploys. **Step 2 (11 migrations) deferred to S017** per user instruction. One edge-case captured: Windows local `npm run build` breaks on `&` in path ("11 - AI & Digital") because `.bin/*.cmd` wrappers mangle the truncated module spec — workaround is `node ./node_modules/<pkg>/bin/*` direct invocation; Vercel's Linux builds are unaffected. |
| S017 | 21/APR/2026 | Home | Step 2 of V2.8 — 11 migration files + TypeScript types + CRLF chore + build fix | **Step 2 of V2.8 FULLY COMPLETE.** `.gitattributes` chore resolved 47-file CRLF drift (`git add --renormalize .`, commit `3cd2677`). All 11 Supabase migration files written (`0001_enums` → `0011_seed_super_admin`) and pushed to live project `hylarwwsedjxwavuwjrn` via `supabase link` + `supabase db push` (CLI v2.90.0; no DB password — cached auth token). Verified: 20 tables, 11 migrations, 30 functions, 7 app_settings rows, 5 scheduled_reminders, Season 1 seeded, super_admin `m.muwahid@gmail.com` present. TypeScript types generated from live schema (`ffc/src/lib/database.types.ts`, 1816 lines, `2>/dev/null` to suppress login-role diagnostic from stdout). `ffc/src/lib/supabase.ts` updated to `createClient<Database>()` + `SupabaseClient<Database>`. Zero TypeScript errors verified (`tsc -b --noEmit`). `ffc/package.json` build script rewritten to `node ./node_modules/typescript/bin/tsc -b && node ./node_modules/vite/bin/vite.js build` — bypasses Windows `.cmd` wrapper truncation at `&` in OneDrive path; Vercel Linux unaffected. All committed `cab85b9` (16 files, 4260 insertions) and pushed to main. **Supabase MCP** scoped to PadelHub org only (403 on FFC project) — workaround: CLI throughout; no MCP fix needed. |
| S018 | 22/APR/2026 | — | CLAUDE.md optimization + docs/ui-conventions.md extracted | Housekeeping session outside V2.8 sequence. `CLAUDE.md` rewritten in place: **40,880 → 6,983 chars (−82.9%)**, 291 → 84 lines. Bloat source identified as S007–S017 session narratives (~85% of file) duplicating `sessions/S###/session-log.md`. Externalized via 2-line pointer to `sessions/INDEX.md`. Preserved 100% verbatim: Stack, Philosophy, Folder layout, Operating rules 1–12, Cross-PC protocol, all live URLs/IDs, all 4 live S017 operational gotchas (MCP PAT scoped to PadelHub, `supabase gen types 2>/dev/null` redirect, Windows `&`-in-path `node ./node_modules/` workaround, `supabase link` cached auth). Rule 10 trimmed to pointer (was inlining 5 CSS check-points). **New file:** `docs/ui-conventions.md` (2,900 chars, 39 lines) — canonical home for durable UI rules previously buried in session narratives: DD/MMM/YYYY dates · W-D-L green/grey/red · green=safe / red=destructive button rule · fixed-column-width preference · `.card { flex-shrink: 0 }` · `.phone-inner > * { flex-shrink: 0 }` defensive rule · statusbar safe-area hardcoded tokens · "no data without explanation" · CSS-specificity-first debugging · FFC-naming rule · Visual Companion discipline · 5-step mockup review checklist. Plan-mode workflow: audit presented first (sections classified KEEP/COMPRESS/EXTERNALIZE/REMOVE, projected size, verification plan), user-approved via ExitPlanMode, executed. Zero-loss verified via 9-token grep across new CLAUDE.md + ui-conventions.md + existing `docs/platform/iphone-safe-area.md`. Session logs S014–S016 confirmed untouched. **No commits** (pending user sign-off). |
| S019 | 22/APR/2026 | Home | Step 3 of V2.8 — Auth flow (mockups + code + partial acceptance) | **PARTIAL close — forced pause at D2.** Phase A (mockups): 3 approved (`mockups/login.html` · `mockups/signup.html` 3-stage · `mockups/admin-players-pending.html` NEW focused S019 mockup); Welcome mockup built then dropped mid-session per user scope simplification ("Login is the app entry"). Logo asset arrived mid-session: `shared/FFC Logo Final.png` (white-bg) → user re-exported `shared/FFC Logo Transparent Background.png` (1.44 MB transparent) → wired into all mockups + `ffc/public/ffc-logo.png`. Three user amendments during mockup review: (1) Welcome deleted, (2) Login crest 160×160 centered with no title/subtitle, (3) Signup Stage 1 matches Login's crest treatment. Phase B (code): 11 files modified / 2 created (`PasswordInput.tsx` with SVG eye-toggle · `PendingApproval.tsx`) / 1 deleted (`Welcome.tsx`). `AppContext.tsx` rewired to fetch profile via `profiles.select('id, role').eq('auth_user_id', userId).maybeSingle()` with `profileLoading` + `profileId` exposed. `HomeRoute` 3-state dispatch (no session → /login · session + role → /poll · session + no role → PendingApproval). `Login.tsx` real form w/ email+password+Google+error banners. `Signup.tsx` 3-stage state machine with ghost-profile claim list. `AdminPlayers.tsx` Pending tab interactive w/ approve_signup + reject_signup RPC wiring + 10-char min reject reason + bottom-sheet confirmations. `RoleLayout.tsx` bottom nav emoji icons added. `index.css` +~450 lines of auth/admin classes incl. `.password-wrap` / `.password-toggle`. Zero TS errors. Phase C: super_admin `m.muwahid@gmail.com` bootstrapped — Supabase Auth user created via dashboard (UUID `67d8219c-6086-4f23-a2fa-deeb3fcc28bf`), bound to existing super_admin profile `cce905a8-8f42-48c4-bf9e-65a3cb301757` via direct SQL UPDATE. **CRITICAL BUG DISCOVERED + FIXED mid-Phase D:** migrations 0001–0011 enabled RLS and wrote policies but emitted **no table-level GRANTs** — the `authenticated` role had zero SELECT/INSERT/UPDATE privilege on public tables because the project was provisioned with "automatic table exposure OFF" (S015). RLS filters rows on top of GRANTs; without the GRANT every query silently returns empty (no client error). Diagnosis: `information_schema.role_table_grants` showed only `REFERENCES, TRIGGER, TRUNCATE` for `authenticated`. Fix: **new migration `supabase/migrations/0012_grants.sql`** — `GRANT USAGE ON SCHEMA public TO anon, authenticated; GRANT SELECT/INSERT/UPDATE/DELETE ON ALL TABLES IN SCHEMA public TO authenticated; GRANT USAGE, SELECT ON ALL SEQUENCES TO authenticated; ALTER DEFAULT PRIVILEGES ... FOR NEW TABLES`. Applied via `supabase db push`. Phase D: **D1 PASS** — super_admin signs in → lands on `/poll` → 5-tab bottom nav visible (Home 🏠 · Table 📊 · Profile 👤 · Settings ⚙️ · Admin 🛠). **D2 BLOCKED** — Supabase signUp rejected `test.s019@example.com` as invalid (strict validator); D2–D6 parked for S020. Infra side-quests: `.claude/launch.json` mockup preview dir fixed (was `.superpowers/brainstorm/...` → now `mockups/`) and OneDrive Files-On-Demand stale-stat symptoms observed (welcome.html briefly served 3,820-byte stub instead of 8,801-byte real file; re-write forces materialisation). **NO COMMITS THIS SESSION** — user closing out; S020 opens with `git add` + commit + push covering all S019 work including 0012_grants.sql. |
| S020 | 22/APR/2026 | Home | Step 3 acceptance D1–D6 + D6 UX fix + SPA rewrite | **Step 3 of V2.8 COMPLETE.** Commits: `8f8668e` (S019 bundle committed+pushed — 24 files incl. `0012_grants.sql`), `dca48cf` (D6 UX fix — AppContext auto-signOut on role=rejected + reason passed to Login banner via sessionStorage), `0e62ffd` (`ffc/vercel.json` SPA catch-all — every route was HTTP 404 from Vercel edge until fixed). **D1–D6 all PASS** (verified via DB + user drove production UI + preview_inspect for banner). **Side-quests:** disabled Supabase "Confirm email" (fix for D2 silent-stuck bug — `supabase.auth.signUp()` returns session:null with confirmations on, breaks stage 1→2 transition); **Google OAuth configured end-to-end** — GCP project "FFC App" + OAuth consent (External/Testing) + Web Client "FFC Web" (Client ID `991515683563-ncjuidcn08psinv7oq8jb9kevp4k6g32…`) + 120×120 logo variant (Python Pillow one-shot → `_wip/ffc-logo-google-120.png` 16.7 KB) + test users allowlisted + Supabase provider enabled + Site URL + Redirect URLs (`/**`) set. Google OAuth probe via REST returns 302→accounts.google.com with Client ID embedded ✅. Path B end-to-end retest deferred to S021 (redirect URLs weren't saved at first attempt; config is now correct). **Lessons added (3 mistakes + 6 patterns).** Also caught a D3 SQL column-name mismatch on `admin_audit_log` — non-blocking, logged for S021 audit. |
| S021 | 22/APR/2026 | Home | Path B OAuth retest · PWA icons · Signup confirm-email · audit-log docs · cross-PC sync protocol | **4 polish items shipped, Step 4 decision deferred to S022.** Commits: `ff82978` (PWA icons) · `0ed3499` (CLAUDE.md sync protocol) · `a39f56f` (Signup confirm-email + audit-log docs + lessons). **Cold-start trap resolved** — home-PC `.git` pointer still referenced work-PC path (OneDrive synced files but local `.git/` never pulled the S019+S020 commits); rewrote pointer + stash-pull-drop got HEAD from `f10f138 → 5791a77`. Rule baked into CLAUDE.md as mandatory session-start sync protocol. **Google OAuth Path B end-to-end GREEN** — curl probe + playwright verified FFC→Google redirect with correct client_id + scopes + state; user click-through on real Chrome (browser-automation blocked by Google's anti-Chromium detection); DB delta confirmed `auth.identities` for `m.muwahid05@gmail.com` now has `[email, google]` (was `[email]`), Supabase identity-merge did NOT fork a duplicate profile (3 profiles / 3 unique auth_user_ids / 0 ghosts). **PWA icons LIVE** — generated 5 PNG variants (32/180/192/512 + maskable-512 centered in 80% safe zone on `#0e1826`) via Pillow one-shot from the transparent-bg 1024×1024 master. Found `ffc/public/favicon.svg` was a purple-bolt Vite/shadcn placeholder (NOT FFC branding) wired into manifest + both `<link>` tags — deleted, replaced with PNG references. Splash tile text→`<img>` swap. Scope revision: "SVG master via Pillow" not feasible (Pillow is raster-only) — SVG path deferred until vector crest sourced. Live smoke-test confirms all 5 icons serve 200 with correct byte counts; manifest.webmanifest + index.html <head> verified. **Signup.tsx latent bug fixed** — when Supabase "Confirm email" = ON, `signUp()` returns session:null and `onAuthStateChange` never fires so stage 1 stays silently stuck. New `'confirm_email'` Stage with "Check your inbox" screen + resend button + `auth.resend({type:'signup'})` + stage-preservation across re-renders + `auth-banner--success` (green) variant. Latent since Phase 1 gate uses admin approval (confirmations OFF) but primed for future flip. **admin_audit_log column audit** — S020 D5's query used wrong column `target_table`; actual is `target_entity`. New `docs/admin-audit-sql.md` cheat-sheet (schema + 4 common queries + known actions). Pattern codified: any non-obvious-schema table gets `docs/<table-name>-sql.md`. Lessons row added. **Deferred to S022:** Step 4 UI slice (Poll full Depth-B §3.7 vs Leaderboard §3.13 — planner agent recommended), SVG master if user sources vector crest, palette re-alignment (backburner). |
| S022 | 23/APR/2026 | Work | Step 4 of V2.8 — §3.13 Leaderboard Depth-B shipped end-to-end | **Step 4 of V2.8 FIRST UI SLICE SHIPPED.** Commits: `59889f1` (initial slice) · `9d5c3c2` (compact filter+sort icons) · `c33b781` (season picker anchored dropdown) · `5c97867` (last-5 strip) · `d3cdcf8` (cards col + MOTM chip + pin last-5 to grid row 2) · close commit. **Cold-start protocol exercised cleanly** (first real-world run of the S021 rule — state (b), stash-pull-drop worked on first try). **Planner agent** compared Poll vs Leaderboard on 7 axes and recommended Leaderboard (§17 order, zero new SQL, renders against 0-match DB via empty states). **Pre-flight SQL** verified GRANTs + `profiles.leaderboard_sort` column/enum + Season 1 seed. **Slice shipped in one pass** (`Leaderboard.tsx` stub → 490 lines, `index.css` +469 lines .lb-* classes): season picker, position filter, sort dropdown with `profiles.leaderboard_sort` persistence, sticky column header, ranked rows (rank/medal/avatar/name/WDL/MP/cards/Pts), last-5 strip on grid row 2, Not-yet-played muted group, "Season starts here" tile, tiebreak chain (points→wins→motms→goals→name), row-tap routes to §3.14. Medal icons top-3 current, trophy glyphs for archived. **Two rounds of UX polish driven by user feedback** — (1) sort pill + position chip row replaced with compact 38×38 icon buttons opening anchored dropdowns (multi-select filter with active-count badge, single-select sort with ✓). Sort order reordered to Points/Wins/Goals/MOTM/Last 5; labels simplified. (2) Season picker bottom sheet also replaced with anchored dropdown ("same location, no eye/finger divert"). All three popovers share one component + one click-outside effect. User feedback saved to memory as `feedback_anchored_dropdowns.md` — durable rule for future FFC screens. **Last-5 wiring + seed one approved match** (1 matchday, 1 match `win_white` 3-1, super_admin MOTM; 2 match_players rows). Schema discovery for seed hit 3 SQL gotchas: enum values wrong-guessed (`WHITE_WIN` vs actual `win_white`), UUID literal UNION ALL needs explicit `::uuid` casts every branch, CLAUDE.md truncated Test Player UUID to `ca3181b2…` — fill-in from memory was wrong (actual ends `-690962128530`). **Three miss-fixes after user review:** (a) last-5 was in DOM but grid auto-placement landed it in a collapsed slot → pinned `grid-row: 2` for determinism; (b) Cards column was deliberately dropped against spec — user wanted it — added 7th grid col with `🟨N 🟥M` cluster, hidden when 0; (c) MOTM was spec'd as tiebreak-only → user wanted it on-row → added compact gold `⭐N` chip inline with position pills. Seed updated with cards (Mohammed 1y · Test Player 1y+1r) so cards column has something to display. **User verified end-to-end**: "its rendering now working perfectly." **Deferred past this slice** per spec's Phase-1 Depth-B gate: realtime subscription on matches UPDATE, pull-to-refresh, skeleton rows ≥150ms. |
| S023 | 23/APR/2026 | Work | §3.14 Player Profile + §3.16 League Rules + Friendly Game data model | **Two parallel workstreams.** Main session: `Profile.tsx` fully replaced (977 lines) — hero band, season picker anchored dropdown, season stats card with rank hint (1st 🥇 / 2nd 🥈 etc.), last-5 strip, career achievements (6 tiles), recent matches list, edit sheet (positions/theme/sort). 6 parallel Supabase queries; `computeStreaks` + `computeRank` client-side. `pf-*` + `lr-*` + `st-*` CSS block (503 lines) added. Parallel session: migration 0013 (`matchdays.friendly_flagged_at`, `is_friendly`; `match_players.is_no_show`; `app_settings` no-show keys; `v_season_standings` recreated with `AND NOT is_friendly` + `no_show_penalties` CTE). Migration 0014 (search_path fix). TypeScript types regen. `Rules.tsx` (static League Rules screen, 4 cards). `Settings.tsx` + router wired for `/settings/rules`. AdminMatches friendly review panel (amber FRIENDLY? badge + Confirm/Dismiss modal). 11 commits total. Deployed; user acceptance post-close. |
| S024 | 23/APR/2026 | Work | S023 acceptance + Rules expansion + §3.20 Matches + §3.15 Match Detail sheet + §3.16 Settings full + AdminPlayers polish | **5 code slices + acceptance pass + close in one long session.** S023 acceptance A1–A9 all PASSED via user-driven prod testing (Profile variants, Rules, AdminMatches friendly-review SQL-seeded → amber chip → Dismiss RPC → clean). **Rules expansion** (`0c027df`+`840b572`) — Scoring restructured horizontally, Late-cancellation renamed "Dropping out" with 5-tier table, NEW Kick-off/Cards/Awards cards (Ballon d'Or = most MOTMs), centered headers + right-aligned penalty th. **§3.20 Matches + §3.15 Match Detail sheet** (`e1e9d19`) — new `/matches` tab + `Matches.tsx` (season picker, chronological list, friendly exclude client-side) + `MatchDetailSheet.tsx` portal overlay (rosters, MOTM banner, inline stats, W/D/L chip when `profileId` passed, Esc/scrim/grabber dismiss, safe-area). Profile Recent Matches refactored to share same sheet. New mockup `mockups/3-20-matches.html`; §3.15 mockup reused from S012. Schema drift handled (`venue` not `venue_label`; matchday_number computed client-side; `late_cancel_*` columns absent → strip deferred). **Migration 0015** `profiles.push_prefs jsonb NOT NULL DEFAULT` (7 keys) applied to live DB. **§3.16 Settings full** (`590fcc0`) — Depth-B replacement of stub. 6 rows: Theme (3 chips auto-save) · Push notifications (master pill + 6 child pills writing push_prefs jsonb, permission coupling, legacy `position_changed` normalised out) · Leaderboard sort (4 chips) · Positions (inline dropdowns, auto-clear secondary when == new primary) · Display name (regex + case-insensitive ILIKE uniqueness pre-check + shake animation) · Account (email ro, Sign out, Delete coming-soon toast). State tiles: permission-default prompt (dismissible) + permission-denied fallback (persistent, forces master OFF). `.st-*` CSS namespace expanded. **AdminPlayers Active+Rejected enrichment** (`c8cb463`) — position pills + `inactive` chip on Active rows; `reject_reason` inline on Rejected rows. Full edit/ban admin flow (needs migration 0016 with `update_player_profile` + `ban_player` + `unban_player` RPCs) explicitly deferred to S025. **Migrations on live DB now: 15 (0001 → 0015_push_prefs).** User requested dedicated testing session next — no acceptance pass on S024 code this session. |
| S025 | 23/APR/2026 | Work | §3.17 Admin Players full edit/ban/reinstate + §3.18 Admin Matches full CRUD + migration 0016 | **S024 acceptance + §3.17 + §3.18 shipped in one pass.** User opted for "code everything + 1 live testing pass" over step-by-step walkthroughs. Commits: `fc5d7ee` (S025 bundle — 5 files, +2251/−409) · `bde8c70` (polish — 4 files, +142/−51). **Migration 0016 — 8 admin RPCs** applied live: `update_player_profile` · `ban_player` · `unban_player` · `reinstate_rejected` · `create_matchday` · `update_matchday` · `lock_roster` · `admin_submit_match_result` (single-call atomic matches + match_players insert + approve). All `SECURITY DEFINER` + `is_admin()` gate + `log_admin_action()` audit. Explicit `GRANT EXECUTE ... TO authenticated` on all 8 (SECURITY DEFINER bypasses 0012 DEFAULT PRIVILEGES). **§3.17 AdminPlayers** rewritten 366 → 953 lines. 4 tabs (Active · Pending · Banned · Rejected) + client-side name+email search. Active row tap → Edit sheet (display_name / primary / secondary / is_active pill / role chip for super_admin); 🚫 button → Ban sheet (reason ≥10 + duration pills 7 Days / 14 Days + custom days input, "Returns {date}" live preview). Banned row Unban → `unban_player`. Rejected row Reinstate → `reinstate_rejected` (role flips rejected→player, clears reject_reason, reactivates). **§3.18 AdminMatches** rewritten 150 → 996 lines. 3 segments (This week / Upcoming / Past) bucketised by kickoff_at. Matchday card with day-of-week + date header, 4-tone phase chip (muted/warn/accent/success), state-aware actions (Edit matchday / Lock roster / Enter result / Edit result), ⚪/⚫ team-colour score preview. **Create matchday sheet** — datetime-local inputs with live preview `THU · 30/APR/2026 · 8:15pm`, defaults to next Thu 8:15pm, format chips 7v7/5v5 (7v7 default, Inherit dropped per feedback). **Result entry sheet** — score inputs (⚪ WHITE / BLACK ⚫), roster builder (+ WHITE/+ BLACK pickers → active-profiles list, per-row goals/🟨/🟥/(C)/NS inputs + ✕ remove), MOTM dropdown from rostered profiles, notes, Approve-now pill. Single `admin_submit_match_result` call. **Edit result sheet** — score/MOTM/notes editable via `edit_match_result` for already-approved matches. Per-team 2-column roster grid (⚪ WHITE left / BLACK ⚫ right) filtered to players with events only (scored / carded / no-show / captain / MOTM). MOTM ⭐ + captain (C) inline. Per-player post-approval stat edits explicitly Phase 2. **Friendly review card** kept from S024. **Three mistakes this session:** (1) called `log_admin_action(v_admin, entity, id, action, payload)` — 5 args — when real signature is 4 args (admin derived internally); fixed via Python in-place edit of migration file + hot-patch to live DB via `db query --linked --file`. (2) used `team_colour` (British) in `::public.team_colour` cast; Postgres lazy-parses plpgsql so CREATE passed but first call failed; enum is `team_color` (American); hot-patched. (3) used `seasons.started_on` in `.order()` when column is `starts_on`; TS doesn't catch PostgREST string columns. **Lessons added** for all three: query `pg_proc` / `pg_type` / `information_schema.columns` before writing PL/pgSQL that calls functions or casts enums, or PostgREST queries that reference column names. **Validated patterns:** hot-patch via `db query --linked --file`, Python in-place bulk replace for 8+ SQL call sites, datetime preview label under `<input datetime-local>`, admin direct-submit RPC shape (parent + children + audit in one tx), per-team column grid with events-only filter for result summaries, user-driven post-deploy feedback round (ship 90% → test live → polish iteration, faster than speculative mockup iteration). User completed full UI testing pass and approved all flows. **Migrations on live DB now: 16 (0001 → 0016_admin_rpcs).** |
| S026 | 23/APR/2026 | Work | Poll §3.7 Depth-B + Leaderboard gate + Phase 2 stat-edit seed + Phase 5.5 card — 5 scope items bundled | **Five points (§3.5 auto-flag · Poll · Leaderboard gate · edit_match_players · Phase 5.5) shipped in one commit.** User directive: "code everything then test at end" — same pattern as S025. One commit `45383bc` (+2047/-74 across 8 files), Vercel deploy `dpl_4pL9QyCsipwsF6ZVAeXdLWXYEr1a` READY. **Migrations 0017+0018+0019** applied live: 0017 `match_guests_friendly_flag_trg()` auto-stamps `matchdays.friendly_flagged_at` when active guest count hits `app_settings.guest_friendly_thresholds[effective_format]` (4 for 7v7, 3 for 5v5); 0018 `edit_match_players(match_id, players jsonb)` admin-only whitelist patch (`goals/yellow_cards/red_cards/is_no_show/is_captain/team`) with per-row + summary audit; 0019 `cast_poll_vote(matchday_id, choice)` idempotent upsert (re-vote-yes resets `committed_at` per §3.6 anti-hoard rule) + `invite_guest(matchday_id, display_name, primary, secondary, stamina, accuracy, rating, description)` confirmed-only slot validation. **§3.7 Poll.tsx** stub 9L → Depth-B 765L: all 9 states render from a state machine driven by `(now vs poll windows) × myVote × roster_locked_at × draft.status × commitments[].team` — pre-open, not-voted tri-button, confirmed-#N, waitlisted, guest-unlocked, roster-locked, State 6.5 Draft-in-progress (picking-team header pulses), State 8 teams-revealed two-section layout, penalty sheet (copy from `app_settings.match_settings`). Realtime on `poll_votes/match_guests/matchdays/draft_sessions/match_players`. Guest invite sheet with 5 required chip groups + optional secondary + 140-char description. **Leaderboard Depth-B gate** — `loadSeason(seasonId, mode: 'initial'|'refresh')` callback shared by initial mount + realtime + PTR. Realtime on `matches`+`match_players`. Touch-based pull-to-refresh (easing × 0.5, 70px arm threshold, ↓/↑/↻ states). Skeleton rows (6) with shimmer keyframe replacing plain "Loading…", 150ms minimum hold. **edit_match_players UI** — `ResultEditSheet` gains `[✎ Edit player stats]` toggle; off = events-only (S025 behavior), on = all rostered players + inline goals/🟨/🟥/(C)/NS inputs; dirty counter; Save calls `edit_match_result` then `edit_match_players` with batched payload. **Phase 5.5 card** on AdminMatches — renders when `draft_sessions.status='in_progress'`, shows pulsing amber dot + pick-count + picker team + captain name + elapsed; stuck threshold 6h exposes **Force complete**/**Abandon** buttons (disabled — Phase-2 RPCs). Two mistakes caught + fixed pre-push: `profiles.banned_until_matchday_id` doesn't exist (ban is separate `player_bans` table with `starts_at`/`ends_at`/`revoked_at`), `v_match_commitments` view doesn't expose `match_guests.id` (only `guest_display_name`) — Poll workaround matches by display_name. **Punted to follow-ups:** captain reroll modal (S010 subagent-B spec), `admin_draft_force_complete`+`admin_draft_abandon` RPCs, `v_match_commitments` view extension to expose guest-id. User opted to test on their own schedule post-close. |
| S027 | 23/APR/2026 | Work | §3.20 Matches flashcard redesign — spec + plan (no code) | Brainstorm→spec→plan cycle for replacing the one-line Matches row with a split-colour flashcard. Visual companion server ran 6 iterations to land Option C v6 (half-height, transparent FFC crest with invert-filter on black side, per-team scorer columns with one-row-per-scorer, hat-trick pink `HAT` pill, combined winner treatment = loser dim + green ribbon + `WINNER` label, neutral `DRAW` pill). Banner locked to `GAME N / TOTAL` sourced from a new `seasons.planned_games` nullable column admins set on season creation; banner degrades to `GAME N` when `NULL`. Clean-sheet flag dropped. Spec committed `20e8e49` (`docs/superpowers/specs/2026-04-23-matches-flashcard-design.md`, 144 lines). 7-task plan committed `db681ba` (`docs/superpowers/plans/2026-04-23-matches-flashcard-plan.md`, ~1025 lines) covering migration 0020 + `create_season`/`update_season_planned_games` RPCs + `AdminSeasons.tsx` page + router wiring + `Matches.tsx` data+markup rewrite + `matches.css` stylesheet + per-task commits + visual/SQL verification. **No code this session** per user direction — will execute next session. Scheduled-task reminder created. Branch tip `db681ba` (not yet pushed; S028 opens with `git push`). |
| S028 | 23/APR/2026 | Work | S027 backend carry-over + §3.19 Formation Planner Slices A+B+C | **Six commits, one deploy recovery.** User skipped S026/S027 live acceptance: "code everything, test later once app is ready." Three backend items + §3.19 new screen shipped. **Slice 1** `8a753cd` — migration 0020_v_match_commitments_guest_id (CREATE OR REPLACE VIEW appends `guest_id uuid` trailing column; null on player branch, match_guests.id on guest branch) + Poll.tsx refactor (slot→row mapping moved from display_name string match to pk lookup). **Slice 2** `c6bfb89` — migration 0021_admin_draft_override_rpcs (`admin_draft_abandon` flips status, `admin_draft_force_complete` iterates unpicked match_players, inserts draft_picks alternating teams from current_picker_team, updates match_players.team, flips status to completed; both SECURITY DEFINER + is_admin + audited; raises FFC_ALREADY_AT_CAP) + AdminMatches Phase 5.5 buttons wired live. **Slice 3 §3.19 A** `03bbcf5` — new `src/lib/formationPresets.ts` (170 LOC, 9 patterns: 6×7v7 + 3×5v5, coordinates on portrait viewBox 0..100 × 0..150, CUSTOM_PATTERN sentinel) · `FormationPlanner.tsx` captain-editable/team-readable page (moved out of /admin, deleted 11-line stub, new route `/match/:id/formation`) · team-colour strip + kickoff countdown + format chip + pattern chip picker (disabled for non-captains) + pitch SVG + auto-populated tokens (GK-preference slot 0) + roster list + save via `upsert_formation` · 177 LOC `.fp-*` CSS. **Slice 4 §3.19 B** `e78ee99` — liveSlots state replaces static preset rendering · pitchRef + ref-based dragStateRef · pointer-based drag with bounds clamping (x 2..98, y 4..146) · first drag auto-switches pattern to `custom` · touch-action: none on draggable tokens · Custom chip activatable · "Reset to {last named}" button when in custom mode · load hydrates liveSlots from saved layout_jsonb when pattern=='custom' · draggable cursor CSS. **Slice 5 §3.19 C** `f489c14` — gkMode (dedicated | rotate) + startingGkProfileId state hydrated from existing row (rotate iff formation_rotation_order non-empty) · profileMembers filtered to kind='member' (guests excluded per starting_gk_profile_id FK) · rotationRows derived (starter=1, others 2..N in roster order) · rotByProfileId Map · segmented toggle + native select GK picker per S012 redesign · token corner badges (GK gold + rotation number inverted on black team) · roster trailing chip (GK gold / number / em-dash for guests) · onSave passes rotationJsonb + gkProfileId (null in dedicated → server clears stale data) · 72 LOC CSS. **Fix** `7e9a890` — Vercel `dpl_CDVKY...` ERRORED with 4 TS errors that local `tsc --noEmit` missed but `tsc -b` caught: unused selectedPreset memo + unused FormationPreset import (removed); `null as unknown as null` casts failed strict-build on Supabase-generated RPC arg types (generated types use `Json \| undefined` / `string \| undefined` — not null); switched to conditional spread (`...(rotationJsonb ? { p_rotation_order: ... } : {})`) + explicit `as unknown as Json` widening for RotationRow[]→Json (Json's index signature can't auto-satisfy). AdminMatches `p_reason: null` fields removed; RPCs have DEFAULT NULL server-side so omission is equivalent. `dpl_A2LF7PLNjw2oVB9wbdcYDaixzRva` READY. **Migrations on live DB: 21 (0001 → 0021_admin_draft_override_rpcs).** **Migration number collision:** S027's unexecuted plan reserves 0020 for `seasons.planned_games` — my 0020 already landed as `v_match_commitments_guest_id`; if/when S027 plan runs, renumber to 0022. **Three mistakes + 4 validated patterns** (file: `sessions/S028/session-log.md`). **§3.19 Slices D (realtime + share_formation + notes) + E (entry links) deferred.** |
| S029 | 23/APR/2026 | Home | §3.20 Matches flashcard redesign executed (S027 plan) + AdminSeasons + migration renumber | **S027 plan executed on home PC after cross-PC sync.** 5 commits. Migration renumber: S027 plan's 0020 → 0022 (0020+0021 already taken by S028). Migration 0022 `seasons.planned_games` column + `create_season` + `update_season_planned_games` RPCs applied live. Migration 0023 patch adds `DEFAULT NULL` to both RPCs (Supabase type generator marks args optional, enabling conditional spread). `AdminSeasons.tsx` new page (list + create + inline planned_games edit). Route `/admin/seasons` + `AdminHome` link. `Matches.tsx` query extended (scorers embed + planned_games). Full flashcard markup + `ffc/src/styles/matches.css` (`splitc-*` classes): banner `GAME N / TOTAL`, split-colour scoreboard, winner ribbon + dim + WINNER label, DRAW pill, per-team scorer columns, HAT pill, MOTM strip. `tsc -b` strict build clean before commit. **Migrations on live DB: 23 (0001 → 0023).** Push blocked by settings rule — user must push manually. Live acceptance pass pending. |
| S030 | 23/APR/2026 | Home | §3.19 Formation Planner Slices D+E — realtime + share + notes + entry links | **Slices D+E shipped in one session.** 2 commits. Migration 0024: adds `formations.notes text` column + DROPs+recreates `upsert_formation` with new `p_notes text DEFAULT NULL` param (7 args total) + adds `formations` to `supabase_realtime` publication. FormationPlanner.tsx: captain's notes textarea (500 char, read-only for non-captains); realtime subscription on `matchday_id` with team filtered client-side → loadAll on any external update; Share to team button calling `share_formation(id)` (label flips to Re-share after first share); footer chip "Shared · last synced …" / "Draft · not yet shared". Entry links: Poll State 8 adds `🧩 Plan formation` CTA for captains + `🧩 View team formation` for teammates (new `matchId` state); AdminMatches MatchdayCard gains `🧩 Formation` action when md.match exists; MatchDetailSheet footer gets `🧩 View formation` button (navigate + onClose). `tsc -b` strict build clean at every checkpoint. **Migrations on live DB: 24.** Push blocked by Claude settings — 2 commits pending manual push. |
| S031 | 24/APR/2026 | Home (worktree `gracious-colden-c36fec`) | Live acceptance hand-off + §3.1-v2 Captain Helper Slices A+B built in parallel | **Two commits shipped** while user tested prior scope. Slice A (`0f2b820`) — new `/matchday/:id/captains` admin screen (Formula + Randomizer modes, suggested pair card, candidate list with ✓✓✓ triplet, White=weaker confirm sheet, `set_matchday_captains` RPC) + `👔 Pick captains` button on AdminMatches + entire `.ch-*` CSS namespace. Slice B (`a689ba3`) — guests-on-roster subsection (S007 stats, cancelled_at-filtered) + rank-gap >5 advisory sub-modal (not a hard block per spec). Backend was already in place (migrations 0006 + 0008). Strict `tsc -b --force` clean at both checkpoints. Automated pre-flight on production green (Vercel `dpl_5FgKYfJBE7uGWbQKAi6JmveXBq7v` READY, URL 200, no runtime errors). Acceptance checklist at [sessions/S031/acceptance-checklist.md](S031/acceptance-checklist.md) — 21 items across 4 scope buckets (S030 + S029 + S028 + S026). User still testing; compound-regression triage moves to S032. 3 commits pushed to origin/main at close (Slice A, Slice B, docs). **Migrations on live DB: 24 (unchanged — no migration this session).** |
| S032 | 24/APR/2026 | Work | §3.1-v2 Captain Helper Slice C — triplet click-to-expand + concurrent-admin toast | **UI-only polish pass, 1 commit.** Cross-PC sync on cold start (state (b) lag: `.git` pointer lagged at Home-PC path, 12 commits behind; stash-pull-drop advanced `f10f138`-era → `1357c70`). **Item 1 (triplet click-to-expand):** `Triplet` component extended with `expanded` + `onToggle` props; interactive variant stays a `<span>` (avoids nested-button a11y violation inside the row's main button) with `onClick={(e) => { e.stopPropagation(); onToggle() }}`. CandidateRow holds local expansion state and renders `.ch-triplet-detail` below the button showing `✓ min-matches N MP · ✓ attendance N% · ✓ cap Nmd ago` with per-check pass/fail colour coding. Non-interactive callsites (SuggestedPairCard + ConfirmSheet) keep `title`-tooltip because their surrounding context already prints raw stats. **Item 2 (concurrent-admin toast):** added `initialCaptainIds` state captured in `loadAll` from `match_players.is_captain=true` rows. Extended `commitPair(w, b, force=false)` with a pre-commit re-read (`match_players` captain rows with `team, profiles:profile_id(display_name)` embed) + most-recent `admin_audit_log` lookup (`target_entity='matches'`, `target_id=match.id`, `action='set_matchday_captains'`, `profiles:admin_profile_id(display_name)` embed) when the set differs from initial. New `ConcurrentAdminModal` shows `⚡ Captains were picked {Ns/Nm/Nh ago}` with current pair + admin name + intended pair + Cancel-and-refresh / Overwrite-anyway actions. `force=true` bypasses the check. `formatTimeAgo(iso)` helper. New `.ch-triplet--interactive`, `.ch-triplet--on`, `.ch-triplet-detail`, `.ch-sheet--concurrent`, `.ch-sheet-concurrent-body`, `.ch-concurrent-hint` (~45 lines CSS). **One mistake caught:** first pass embedded `team` on the `profiles:` join (team is on `match_players` directly); fixed by reading `database.types.ts`. **Verification:** `tsc -b --force` EXIT=0, `vite build` EXIT=0 (CSS 96.20 kB, JS 682.84 kB, PWA 10 entries). Live acceptance deferred to S033 per user ("will test later"). **Migrations on live DB: 24 (unchanged).** |
| S033 | 24/APR/2026 | Work | Season 11 data fix + §3.1-v2 palette re-theme (CaptainHelper-only brand tokens) | **1 commit.** Two tasks bundled. (1) **DB fix:** user clarified real-world league is Season 11, match 33/40 — seeded "Season 1" row renamed to "Season 11" and `planned_games=35→40` via `UPDATE seasons`. No code change; `Matches.tsx` flashcard banner already reads the column (S029). (2) **Palette re-theme:** replaced the entire `.ch-*` CSS block with a brand-tokenised version scoped to `.ch-root` (18 custom properties declared once; every descendant rule pulls from tokens). Mockup palette from `mockups/3-1-v2-captain-helper.html`: paper `#0e1826` · cream ink `#f2ead6` · accent red `#e63349` · gold `#e5ba5b` · success `#4fbf93` · warn `#d7a04a`. Role shifts: Use-this-pair & Roll buttons → red CTA · Mode-toggle active → red tint · Primary pair card highlight → gold · Roster-locked chip → gold-weak · Concurrent-admin modal (S032) → gold instead of cyan (informational, not "info"-blue) · Gap-warning sub-modal tokenised. Shared `auth-btn--approve` overridden scoped inside `.ch-root .ch-sheet-actions` so login/signup screens unaffected. Text dominant colour shifted cool-white → cream via `--ch-ink*` hierarchy. Scope kept narrow — CaptainHelper only; rest of app (Leaderboard, Poll, Profile, Admin, Matches) still blue/slate. Build clean (`tsc -b` EXIT=0, `vite build` PWA 10 entries / 2525 KB precache). Files: `ffc/src/index.css` `.ch-*` block replaced (~412 LOC); no `.tsx` change. **Migrations on live DB: 24 (unchanged).** |
| S034 | 24/APR/2026 | Work | Admin platform restructure + §3 AdminSeasons redesign + migration 0025 | **1 commit.** Two scope items bundled. (1) **Admin IA restructure** — removed the conditional 5th `🛠 Admin` bottom-nav tab; `RoleLayout` now renders 5 tabs for everyone. `Settings.tsx` gains a new bottom row `🛠 Admin platform` (red-tinted pill, `.st-admin-link`) visible only when `role ∈ {'admin','super_admin'}`, routing to `/admin`. `AdminHome.tsx` rewritten from plain stub → 3-card hub (Season / Player / Matches management) with icon chip + title + help + chevron. (2) **§3 AdminSeasons full redesign** — replaced always-visible inline create form with red `+ New season` pill CTA opening a bottom sheet; same sheet also backs row Edit. Rows now show name + format chip + status pill · labelled meta pairs `Start / End / Games / Matchdays` with **DD/MMM/YYYY** date format (inline `fmtDate(iso)` string-split to avoid TZ day-boundary shift). Edit + Delete icon buttons per row; delete hidden when `matchday_count > 0` (renders disabled placeholder with tooltip). Sheet fields: Name · First match date · End date (edit-only) · **Planned games (now required)** · Format (7v7/5v5 chip) · Roster policy (wide chip-column with "Carry forward" / "Fresh" + help text). **Migration 0025_seasons_admin_rpcs.sql** — DROP+CREATE `create_season` with required `p_planned_games`; new `update_season(id, name?, starts_on?, ends_on?, planned_games?, default_format?, roster_policy?, clear_ends_on?)` with `COALESCE`-on-NULL optional semantics + `p_clear_ends_on=true` explicit unset signal + auto-stamp `ended_at`/`ended_by` when `ends_on` moves into the past; new `delete_season(id)` guarded by `FFC_SEASON_HAS_MATCHDAYS` check + audit BEFORE DELETE so log survives. All SECURITY DEFINER + `is_admin()` gate + GRANT EXECUTE. Applied live; types regenerated (1916 → 1960 lines). CSS: ~180 LOC under `.as-*` + `.ah-*` + `.st-admin-link`. Dev-server smoke clean (0 console errors; unauth access-gate renders). **Migrations on live DB: 25 (0001 → 0025_seasons_admin_rpcs).** |
| S035 | 24/APR/2026 | Work | Poll brand palette propagation — S033 tokenisation playbook scaled to `.po-screen` | **1 commit, CSS-only.** User approved S033+S034 and asked to propagate the brand palette to Poll (most-trafficked player screen). 10-line scope-override added at the top of `.po-screen`: `--bg: #0e1826` / `--surface: rgba(20,34,52,.68)` / `--surface-2: rgba(10,22,40,.6)` / `--text: #f2ead6` (cream) / `--text-muted: rgba(242,234,214,.62)` / `--accent: #e5ba5b` (gold — restores semantic intent of me-tag/avatar-self/guest-rating-strong/novote-border which were rendering red due to root `--accent=#e63349` winning over gold fallbacks) / `--success: #4fbf93` / `--warn: #d7a04a` (aliased: root defines `--warning` but Poll uses `--warn`) / `--danger: #e63349` (brand red — unchanged, continues to drive CTAs) / `--skel-a/--skel-b` (cream shimmer). Zero changes to the ~95 downstream `.po-*` rules — every `var(--*)` lookup resolves to brand values. Also added `background: var(--bg); color: var(--text);` to `.po-screen` root. Rest of app (Leaderboard, Profile, Matches, AdminMatches, etc.) untouched. Build clean (`tsc -b` + `vite build` 2539 KB PWA). Preview verified: `.po-screen` computed `rgb(14,24,38)` bg + `rgb(242,234,214)` text. **Tokenisation playbook now proven to scale** — same approach will apply to Leaderboard/Profile/Matches in S036+. **Migrations on live DB: 25 (unchanged).** |
| S036 | 24/APR/2026 | Work | Boil the ocean — brand palette propagated across all 8 remaining screens | **1 commit, CSS-only, ~150 LOC.** User directive: "BOIL THE OCEAN!" Applied the S035 tokenisation playbook to every remaining non-auth screen in one sweep. 12-token brand block (`--bg`/`--surface`/`--surface-2`/`--border`/`--text`/`--text-muted`/`--accent`/`--success`/`--warn`/`--warning`/`--danger`/`--skel-a`/`--skel-b`) declared at each root + `background: var(--bg); color: var(--text);` anchor. Screens covered: Leaderboard `.lb-screen`, Profile `.pf-screen`, Matches `.mt-screen`, Rules `.lr-screen`, Settings `.st-screen`, AdminPlayers `.admin-players`, AdminMatches `.admin-matches`, AdminSeasons `.as-root` + AdminHome `.ah-root` (the latter two built before S033 with hardcoded rgba — now tokenised for consistency). Intentionally NOT touched: auth screens `.auth-screen` (Login/Signup/PendingApproval — distinguishes pre-auth from in-app), global `:root` defaults (safety net), shared portal overlays (inherit from parent). Design decisions applied uniformly: paper `#0e1826` navy, ink cream `#f2ead6`, accent **gold** `#e5ba5b` (per-screen override wins over root red — restores design intent signalled by local `var(--accent, #c49a4b)` gold fallbacks), danger `#e63349` red (unchanged, drives destructive CTAs), success fresh green `#4fbf93`, warn warm amber `#d7a04a`. `--warn` AND `--warning` both defined as aliases to catch different naming conventions across screens without app-wide renaming. CaptainHelper (S033) + Poll (S035) already tokenised — now all 10 in-app screens share the brand. Pattern rationale: per-root declaration (vs shared mixin/helper) keeps screen-level themeability explicit, adopts new screens by convention, no abstraction tax — ~100 LOC duplication is the right trade for zero accidental defaults. Preview verified 6/6 non-auth-gated routes render `bg: rgb(14,24,38)` + `color: rgb(242,234,214)`; zero console errors. Build clean (`tsc -b` + `vite build` PWA 2541 KB). **Migrations on live DB: 25 (unchanged).** |
| S037 | 24/APR/2026 | Work | Season 11 roster import + §3.15 captain reroll modal + final logo rollout | **Three scope items bundled, 2 commits, "boil-the-ocean" directive from user.** (1) **Roster import `53afc0e`.** Migration 0026 new `season_seed_stats` table (per-season aggregate seeds) + `v_season_standings` rewritten via `CREATE OR REPLACE VIEW` to UNION live `match_players` aggregates with seeds; pattern preserves view signature so zero TS changes downstream. Migration 0027 archives Test Player (role=rejected), TRUNCATEs 15 transactional tables via CASCADE (matchdays, matches, match_players, poll_votes, draft_sessions/picks, formations, match_guests, player_bans, notifications, admin_audit_log, pending_match_entries/players, ref_tokens, scheduled_reminders), inserts 39 ghost profiles with `auth_user_id NULL` (35 player + 4 admin: Ahmed Saleh, Rawad, Abood, Barhoom), inserts 40 Season 11 seed rows derived from user's external sheet (`draws_seed=max(0, P-3W)` · `losses_seed=GP-W-D` · `late_cancel_seed=LEAST(0, P-3W-D)` captures Murad/Simo -1 penalties). Types regen 1916→1993 lines. Verified live — Karim 44 pts · Firas 40/46G/4 MOTM/1 YC · Mostafa 39 all match sheet exactly. (2) **§3.15 Captain reroll modal `0198450`.** Poll.tsx new `dropoutNotif` + `rerollConfirmOpen` + `rerollCutoffHours` state · `loadAll` queries latest unactioned `dropout_after_lock` notification filtered client-side by `payload.matchday_id` (PostgREST `.contains()` unreliable); resolves substitute name via profiles lookup · renders gold-tinted card (S033 "attention" register) in State 8 when `iAmCaptain && locked && hoursToKick > 0` · within-cutoff: [Accept substitute]→`accept_substitute` RPC (marks notif read + stamps payload.outcome='accepted') + [Request reroll]→confirm sub-sheet→`request_reroll` RPC (new draft_session with reason='reroll_after_dropout', clears non-captain team assignments, broadcasts `draft_reroll_started`) · past cutoff: single [Acknowledge] action. ~70 LOC scoped CSS under `.po-dropout-card` / `.po-dropout-sheet`. (3) **Logo rollout (same commit).** Unblocked by existing `shared/FFC Logo Transparent Background.png` 1024×1024 RGBA source. Regenerated canonical PWA variants via `PIL.Image` LANCZOS: `ffc-logo-32/192/512.png` transparent direct downscale · `ffc-logo-180.png` opaque brand-navy with 140px inset (iOS squircle mask) · `ffc-logo-maskable-512.png` opaque brand-navy with 308px inset (60% safe zone for Android adaptive icon) · `ffc-logo.png` 512 transparent reference. NEW `og-image.png` 1200×630 brand-navy with centered 400×400 logo for WhatsApp/iMessage/Slack previews. `index.html` gains OG + Twitter card meta tags. PWA size dropped 2541→1340 KB (new PNGs more efficient than stopgap). `tsc -b --force` EXIT=0 + `vite build` EXIT=0 (11 precache entries). **Migrations on live DB: 27 (0001 → 0027_season11_roster_import).** **Patterns:** seed-aggregate table + UNION view (new reusable); `CREATE OR REPLACE VIEW` when dependents exist (not DROP); notifications payload client-side filter; TRUNCATE...CASCADE multi-table. |
| S038 | 25/APR/2026 | Work | P1 cream-bg PWA + share-preview polish + P2 ghost-claim routing fix (Barhoom unblock) | **Two commits, two scope items, no migration.** (1) **P1 `a44f1fb`** — regenerated PWA + OG share assets with cream `#f2ead6` background (was dark navy with cropped shield in S037). Generator script `_wip/gen_cream_assets_s038.py` (PIL 12.2): trims source via `getbbox()`, scales preserving aspect, centres on cream canvas — `og-image.png` 1200×630 / 80% inset · `ffc-logo-180.png` 78% inset · `ffc-logo-maskable-512.png` 60% safe-zone inset. `index.html` meta updates: `og:description` + `twitter:description` → "The official Home of the FFC." + `?v=2` cache-buster on og:image (forces WhatsApp scrape refresh) + added missing `<meta name="twitter:description">`. `PublicLayout.tsx` topbar stripped — auth screens already render their own centred crest, the `3573761` topbar header was redundant on Login/Signup/PendingApproval. Transparent `any`-purpose icons (`ffc-logo-32/192/512.png` + `ffc-logo.png`) untouched. (2) **P2 `4de4b05`** — bug report mid-session: admin Barhoom signed in via Google OAuth → blank screen → couldn't claim profile. Diagnosed during code-read: full ghost-claim infra ALREADY EXISTS (`Signup.tsx` Stage 2 ghost-picker + `pending_signups.claim_profile_hint` + migration 0008 `approve_signup(p_pending_id, p_claim_profile_id)`). Bug was purely a routing dead-end — `Login.tsx` correctly navigates to `/`, but HomeRoute rendered `<PendingApproval />` for `session && !role`, which has no path forward AND for OAuth users no `pending_signups` row gets created so admins never see them in the queue. **3-line fix, no migration:** `router.tsx` HomeRoute → `<Navigate to="/signup" replace />` (Signup self-derives Stage 2 ghost-picker if no pending row, or Stage 3 waiting if one does); `RoleLayout.tsx` defensive `!session || !role` redirect to `/` so direct URL hits don't render blank; `AdminPlayers.tsx` ApproveSheet adds green ✓ "Email matches" / amber ⚠ "Expected X, got Y" sanity banner when `ghost.email` is set. **Pre-claim email seed via `DO $$ BEGIN ... END $$` block** (multi-statement queries fail with `String must contain at least 1 character(s)` on the CLI): `ahmed.abdallahh@hotmail.com` → Barhoom · `amakkawi89@gmail.com` → Abood · `ahmed_msaleh@hotmail.com` → Ahmed Saleh · `rawadbn@gmail.com` → Rawad. **Patterns:** "read existing code before designing the fix" (initial P2 plan was full feature build with new migration + new table + 3 RPCs + new page + AdminPlayers tab — reading the codebase end-to-end revealed it was already 80% built; final fix was 3 routing edits + 4-row UPDATE); multi-statement SQL via CLI requires DO block; PIL bbox+LANCZOS asset regen pattern; `?v=N` cache-buster for OG image WhatsApp scrape refresh; admin sanity-banner pattern for destructive bind UIs. **Migrations on live DB: 27 (unchanged).** Vercel deploys both READY: P1 `dpl_13J5zwtKekvJaQBmUQsr6fd4NxkW` ~17s · P2 `dpl_Ge8bwT6Thc7kKbHgv2XJ67jDWeKX` ~22s. PendingApproval.tsx kept on disk but now dead code (import removed). |
| S039 | 26/APR/2026 | Home | Signup/login redirect-race fix + forgot-password + Phase 2 design (V3.0) | **Two commits, two unrelated scope items, no migration.** (1) **`677b1ed`** — Barhoom's first end-to-end run after S038 surfaced a redirect race: HomeRoute → /signup, but Signup's stage-derivation `useEffect` ran before AppContext's profile fetch completed; pending row read `approved` while `role` was still null, dropping the user back to Stage 2 ghost-picker. Fix: stage-derivation `useEffect` now waits for `profileLoading === false`; on `role` detection, `navigate('/poll')` instead of dead `if (role) return`; Stage 3 polls every 5s and fires `window.location.replace('/')` so AppContext re-initialises with correct role. Forgot-password button was a stub — wired to `supabase.auth.resetPasswordForEmail` (requires email field, success banner, `BannerKind` extended with `'success'`). New `ResetPassword.tsx` handles the Supabase recovery-link callback at `/reset-password` (validates match + min-length, calls `updateUser({ password })`, navigates to `/` after 2s). `router.tsx` route added under `PublicLayout`. (2) **`1d3f78e`** — Phase 2 brainstorm closed. `planning/FFC-masterplan-V3.0.md` + `docs/superpowers/specs/2026-04-26-ffc-phase2-design.md` + `mockups/3-21-ref-console.html`. Two tracks locked. **Track 2A — Poll→Lock→Captain automation:** Web Push end-to-end (push_subscriptions + `notify-dispatch` Edge Function + SW handler + VAPID lifecycle) · pg_cron vote reminders T-24h/T-3h/T-15m · auto-lock at deadline · captain auto-pick on lock via existing `suggest_captain_pairs`. **Track 2B — Live Match Console:** admin generates 6h ref-link URL token · ref enters live events (goal/own-goal/yellow/red/halftime/fulltime/pause/resume) with minute + actor · admin approves to promote into permanent log · 5 timing cols flow through the same approve path. **Build-order locked: 2B FIRST** (concrete single-screen flow, soak-tests on real Thursdays sooner; 2A is cross-cutting cron+push+edge+SW infra that benefits from waiting). Patterns: stage-derivation `useEffect` must wait for ALL upstream loading flags; `window.location.replace('/')` is the cleanest "re-init the whole app" transition; build-order is a decision separate from masterplan write — leave for explicit user sign-off. **Migrations on live DB: 27 (unchanged).** Slice 2B-A immediately followed in S040. |
| S040 | 26/APR/2026 | Home | Phase 2 Slice 2B-A — Live Match Console backend foundation (migration 0028) | **4 commits, 1 migration, no UI.** Backend foundation for Phase 2 Track 2B shipped to live. **Plan (`0feb349`)** — slice plan documenting migration 0028 scope. **Migration 0028 (`a8c23b5`)** — `match_event_type` enum (8 values: goal · own_goal · yellow_card · red_card · halftime · fulltime · pause · resume); new `pending_match_events` + `match_events` tables (participant XOR + minute non-negative + ordinal index); 5 timing cols added to both `pending_match_entries` + `matches` (kickoff_at, halftime_at, second_half_kickoff_at, fulltime_at, total_stoppage_seconds — nullable on `matches`, default 0 stoppage on pending); RLS (pending events admin-select; permanent events authenticated-select USING true); `regenerate_ref_token(matchday_id)` admin RPC mints fresh 6h base64url tokens via `gen_random_bytes(24)` + 3-replace base64url normalisation; `submit_ref_entry` rewritten DROP+CREATE to read `events` + `timing` payload keys (backwards-compatible); `approve_match_entry` rewritten with sentinel-guarded timing copy + event-log promotion. **Code-review fixes (`08ddfbf`)** caught 2 real bugs before going live: (1) `approve_match_entry` timing-overwrite guard `CASE WHEN v_pme.kickoff_at IS NOT NULL` prevents nulling existing `matches.*` timing on the admin direct-submit path (which carries no timing); (2) `regenerate_ref_token` advisory lock `pg_advisory_xact_lock(hashtext('regenerate_ref_token:' || p_matchday_id::text))` prevents dual-token mint under concurrent admin calls (snapshot isolation alone insufficient for two-statement burn-then-mint sequences). **Apply + types regen + close (`7af341d`)** — migration applied via `npx supabase db push`; types regenerated 1916 → 2183 lines; build clean (`tsc -b` EXIT 0 + `vite build` EXIT 0); no UI changes (RefEntry.tsx still stub — slice 2B-B opens that work). **Patterns (additive):** (a) base64url tokens via `gen_random_bytes` + 3 `replace()` (URL-safe vs Postgres `encode(..., 'base64')` which produces `+`/`/`/`=`); (b) two-statement burn-then-mint always needs an advisory lock under snapshot isolation; (c) timing-copy paths need a sentinel column to distinguish "no data" from "0/null"; (d) when a spec hardcodes a migration number, confirm build-order before applying — Phase 2 spec hardcoded "0029" for Track 2B but build-order locked 2B FIRST so it claimed 0028. **Migrations on live DB: 28 (0001 → 0028).** Live: https://ffc-gilt.vercel.app (no UI delta). |
| S041 | 26/APR/2026 | Home | Phase 2 Slice 2B-B — admin "Generate ref link" UI on matchday cards | **5 commits, no migration.** Frontend wiring on top of S040's slice 2B-A backend. **Plan (`600ab99`)** — implementation plan locks scope: extend AdminMatches data layer with optional `activeToken` per matchday + `RefLinkSection` rendered inside MatchdayCard + `RefLinkSheet` portal + single `handleMintRefLink` handler driving `regenerate_ref_token(p_matchday_id)`. **Task 1 data layer (`c6add71`)** — `MatchdayWithMatch` extended with optional `activeToken: { expires_at: string }`; `loadAll` adds a fourth `Promise.all` branch fetching `ref_tokens` filtered to `consumed_at IS NULL AND expires_at > now()`, building a `tokensByMd` Map keyed on `matchday_id` and folding into the per-row merge alongside `match` + `draft`; new `formatExpiresIn(iso)` helper drives the chip ("expires in 4h 22m" / "expires in 12m" / "expired"). No UI yet. **Task 2 main (`b2788c9`)** — `RefLinkSection` rendered conditional on `roster_locked_at`: dashed-border `+ Generate ref link` CTA when no active token, gold-tinted `Ref link · expires in Xh Ym` button + 🔄 icon when one exists. `RefLinkSheet` portal via `createPortal` captures the raw token in `useState`, builds `${origin}/ref/${token}`, renders title + explanation copy ("Shown once — regenerate to share again") + read-only URL `<input>` (long-press selectable) + grid of two action buttons (📋 Copy via `navigator.clipboard.writeText` + 💬 Share to WhatsApp via `https://wa.me/?text=<encoded>` deep link) + Done. Single `handleMintRefLink(md)` shared by Generate + Regenerate calls `regenerate_ref_token`, captures the returned plaintext token, opens the sheet, then re-runs `loadAll()` so the card chip flips to fresh expiry the moment the sheet closes. CSS appended under `.admin-ref-link-*` (dashed-border generate CTA, gold-accent active variant `rgba(229,186,91,0.08/0.35)`, monospace URL input, safe-area-bottom padding). **Code-review fix #1 (`2fa75b5`)** — first pass introduced parallel `.admin-sheet*` naming family for the new portal sheet; self-review caught it before push (the same file already had `<ConfirmSheet>` using shared `.sheet*` foundation; two parallel families would drift in style over time). Refactored to reuse `.sheet`/`.sheet-scrim`/`.sheet-grabber`/`.sheet-title`/`.sheet-actions` — only the truly-novel ref-link bits (`.admin-ref-link-*` for the section + URL input + sheet-specific copy) keep custom classes. **Code-review fix #2 (`1d22d8e`)** — added `mintBusy: string | null` per-row in-flight guard. The Generate / Regenerate button issued an RPC followed by `loadAll()`; fast double-tap before the refresh resolved could mint a second token (the advisory lock from slice 2B-A serialises burn-then-mint, but the second call burns the just-minted token from the first, so the admin's first sheet would show a now-already-burned URL). Click-handler short-circuits if `mintBusy === md.id`; button disabled during in-flight; cleared in `finally` so a thrown RPC doesn't permanently lock the row. **Patterns (additive):** (a) **one-shot raw token UX** — RPC returns plaintext only at mint (server stores `sha256(token)` only); sheet captures the URL the moment RPC resolves; matchday-row reload only fetches `expires_at` (the chip-driving fact), token never persisted. (b) **`loadAll` fan-out for derived per-row state** — auxiliary table data fetched as a Promise.all branch and merged via Map lookup; single source of truth, no N+1 per-card useEffect queries. (c) **reuse base sheet classes — don't invent parallel naming** — when a portal-sheet already exists with shared `.sheet*` foundation, reuse it; only truly-novel bits earn custom classes. (d) **`mintBusy: string | null` per-row in-flight guard** — `null = idle | <id> = busy on this row` lets disabled-state + click-handler short-circuit synchronously without ref-vs-state mismatch. (e) **WhatsApp deep link via `wa.me`** — `https://wa.me/?text=<encoded>` opens the app on mobile + Web WhatsApp on desktop; phone omitted so user picks recipient; `target="_blank"` + `rel="noreferrer noopener"` safe default. **Build clean:** `tsc -b` EXIT 0 + `vite build` EXIT 0 (11 precache entries / ~1485 KB). **Migrations on live DB: 28 (unchanged).** Live: https://ffc-gilt.vercel.app. |
| S042 | 26/APR/2026 | Home | Phase 2 Slice 2B-C — RefEntry pre-match + pgcrypto search_path hotfix | **5 commits, 2 migrations.** Slice 2B-C builds the anonymous ref-entry surface on top of slice 2B-A's backend. **Plan (`a156f3e`)** — locks scope: anon-callable `get_ref_matchday(token)` RPC + `useMatchSession` hook + RefEntry stub→full rewrite. Live mode (clock/score/events/MOTM) deferred to slice 2B-D. **Migration 0029 (`a4a15c4`, broken-but-shipped)** — `supabase/migrations/0029_get_ref_matchday.sql`. Anonymous-callable SECURITY DEFINER `get_ref_matchday(p_token text) returns jsonb`: validates raw token via `digest(p_token, 'sha256')` lookup against `ref_tokens`, returns curated JSONB envelope (matchday header + white roster + black roster + token expiry). `GRANT EXECUTE TO anon, authenticated`. Function shipped with a latent search_path bug — `SET search_path = public` (matching 2B-A's pattern) but pgcrypto's `digest()` lives in `extensions` schema on Supabase. Passed `db query --linked` smoke (postgres role has wider default search_path) but failed at runtime under PostgREST anon/authenticated calls with `function digest(text, unknown) does not exist`. Caught immediately during anon-key curl verification. **Migration 0030 (`b07db00`)** — `supabase/migrations/0030_pgcrypto_search_path_fix.sql`. DROP+CREATE on three SECURITY DEFINER RPCs to schema-qualify all pgcrypto calls: `get_ref_matchday` (just shipped in 0029, `extensions.digest`), `submit_ref_entry` (slice 2B-A migration 0028, `extensions.digest`), `regenerate_ref_token` (slice 2B-A migration 0028, `extensions.gen_random_bytes` + `extensions.digest`). GRANTs re-issued after each CREATE (DROP loses them). Function bodies otherwise byte-identical to originals. Types regen 2183 → 2184 lines. **UI (`b359eed`)** — new `ffc/src/lib/useMatchSession.ts` (~160 LOC, state machine `loading | invalid | pre | live | post`, localStorage key = `ffc_ref_<sha256(token)[0:32]>` so plaintext tokens never persist, Web Crypto async sha256, `startMatch()` requests `navigator.wakeLock.request('screen')`); `RefEntry.tsx` rewrite (~158 LOC, was 11) with 5-mode render, pre-match shows matchday header + white & black roster cards + bottom-anchored gold KICK OFF button; new `ref-entry.css` (~173 LOC) with standalone `--rf-*` brand tokens declared on `.ref-entry` root (RefEntry loads outside the authenticated app shell so existing screen-root tokens don't apply). **Anon-key curl verification** — all 3 RPCs now return business-logic errors from inside their function bodies, NOT "function digest does not exist" errors. `regenerate_ref_token` reached the "Matchday not found" path past the `IF NOT is_admin() THEN RAISE` admin gate when called with an anon key — either `is_admin()` returns true for anon (production security bug) or the test mistakenly used an authenticated key. Spawned as a follow-up investigation chip; not blocking S043. **Patterns (additive):** (a) **`pgcrypto` lives in `extensions`, not `public`** — any SECURITY DEFINER function calling pgcrypto MUST schema-qualify or include `extensions` in `SET search_path`; (b) **`db query --linked` is insufficient for verifying SECURITY DEFINER** — runs as postgres role with wider search_path; real verification requires PostgREST anon-key curl OR authenticated browser `supabase.rpc(...)`; (c) **DROP+CREATE hotfix safe with unchanged signature** — re-issue GRANTs after CREATE; (d) **anonymous SECURITY DEFINER RPC for token-gated public reads** — function does auth + curated JSONB return, avoids granting direct SELECT on RLS-gated tables; (e) **localStorage keyed by sha256(token) prefix** safer than raw token; (f) **wake-lock on user-gesture only**, never from `useEffect` on mount; (g) **standalone CSS scope-root for unshelled routes** with unique prefix to avoid collisions. Build clean (`tsc -b` EXIT 0 + `vite build` EXIT 0, PWA 1498.39 KiB). **Migrations on live DB: 30 (0001 → 0030).** Live: https://ffc-gilt.vercel.app. |
| S043 | 27/APR/2026 | Home | Critical security hotfix — is_admin()/is_super_admin() NULL-3VL bug (migration 0031) | **Same-day production hotfix. 1 commit, 1 migration, no UI.** Spawned investigation from S042's anon-key curl observation closed as a security hotfix. **The bug:** `is_admin()` body was `SELECT current_user_role() IN ('admin','super_admin');` — for anon callers (no profile row) `current_user_role()` returns NULL, `NULL IN (...)` returns NULL, and PL/pgSQL's `IF NOT NULL THEN RAISE` is treated as `IF FALSE THEN RAISE` (3VL). Result: every SECURITY DEFINER admin RPC since migration 0007 (S016/17, Phase 1 bootstrap) silently skipped its `IF NOT is_admin() THEN RAISE 'Admin role required'` guard for anon callers and fell through to the next-line existence check. **Confirmed via anon-key curl POST:** `regenerate_ref_token` → `22023 Matchday not found` (past gate); `reject_signup` → `22023 Pending signup not found` (past gate); `approve_signup` → `22023 Pending signup not found` (past gate). **Fix — Migration 0031 `0031_is_admin_null_safety.sql`** (`CREATE OR REPLACE FUNCTION` — signature unchanged so GRANTs survive): wraps both helpers' bodies in `COALESCE(..., false)` — `is_admin()` body becomes `SELECT COALESCE(current_user_role() IN ('admin','super_admin'), false);` and `is_super_admin()` becomes `SELECT COALESCE(current_user_role() = 'super_admin', false);`. Applied via `npx supabase db push --linked`. **Verified after fix via anon-key curl POST:** `regenerate_ref_token` / `reject_signup` / `approve_signup` all now return `42501 Admin role required` ✓; control case `get_ref_matchday` (anon-callable by design) still returns `22023 Invalid or expired ref token` ✓ (legitimate anon surface unbroken). **No code or types changes** — function-body change only, types regen byte-identical to S042's 2184-line output, `tsc -b` EXIT 0. **RLS unaffected** — RLS treats NULL and FALSE identically (row excluded in USING, violation in WITH CHECK); only the PL/pgSQL `IF NOT helper()` callsites were buggy. **Patterns (additive):** (a) **PL/pgSQL `IF NOT NULL THEN ...` silently skips** — 3VL means `NOT NULL` evaluates to NULL and `IF NULL` is treated as FALSE; boolean SECURITY DEFINER helpers MUST `COALESCE(..., false)` so the RAISE actually fires. (b) **`db query --linked` hides authorization bugs** — runs as `postgres` login role with a profile row in test env, so `current_user_role()` returns non-NULL and `is_admin()` returns TRUE; the buggy fall-through is unreachable from that test surface. (c) **Anon-key curl is the only honest verification for admin RPCs** — `db query` has too much privilege; the actual API surface needs to be tested with the role the attacker has access to. (d) **The bug was invisible for 26 sessions** — Phase 1 admin testing went through the authenticated UI where the bug doesn't manifest (authenticated callers have a profile row, helper returns true/false correctly); the first anon-key curl in slice 2B-C was the moment of discovery, and only because that slice's testing protocol was tightened in response to the pgcrypto search_path bug found minutes earlier (migration 0030). (e) **Cascading discoveries are a good signal** — S042's pgcrypto lesson ("don't trust db query verification") directly led to the testing approach that uncovered S043's is_admin bug; tighten verification protocols when one bug is found, others often hide nearby. (f) **Defense-in-depth follow-up: REVOKE EXECUTE FROM PUBLIC on admin RPCs** — Postgres grants EXECUTE on user-defined functions to PUBLIC by default; combined with the is_admin bug this meant anon callers could reach the function body at all. After COALESCE, anon reaches the body but is correctly rejected at the admin guard. A future slice should REVOKE FROM PUBLIC + explicit GRANT TO authenticated for tighter blast-radius even if a future helper bug recurs. Logged as S044 backburner. **Migrations on live DB: 31 (0001 → 0031).** Live: https://ffc-gilt.vercel.app (no UI delta). |
| S044 | 27/APR/2026 | Home | Phase 2 slice 2B-D — RefEntry live match console (clock + pickers + undo + halftime) | **8 commits, no migration (client-only). 4 production code files + 1 new file + 1 plan + this close-out.** Slice 2B-D replaces the `mode === 'live'` placeholder in `RefEntry.tsx` with a fully working client-authoritative live match console. **Files:** `ffc/src/lib/refConsoleConstants.ts` NEW (5 named tunables — `REGULATION_HALF_MINUTES` 7v7=35/5v5=25, `HALFTIME_BREAK_SECONDS=300`, `MAX_STOPPAGE_SOFT_LIMIT_SECONDS=180`, `UNDO_WINDOW_MS=15000`, `HALFTIME_ADD_MIN_SECONDS=60`); `ffc/src/lib/useMatchClock.ts` NEW (~545 LOC client-authoritative match clock + event log + score + MOTM, persisted to sibling localStorage key `<sessionStorageKey>:clock` with hydration-effect-then-1Hz-tick render cycle, 8 mutators: `addGoal/addCard/pause/resume/endHalf/startSecondHalf/addBreakMin/setMotm` + void-returning `undoLast` + derived `canUndo`); `ffc/src/pages/RefEntry.tsx` rewritten (159 → 555 LOC) hosting `LiveConsole` shell + `LiveHeader` (LIVE/BREAK chip with red-pulse / accent-static dot) + `HalfStrip` (1ST/2ND HALF + progress bar + stoppage chip with red-alarm pulse over 180s soft limit) + big M:SS clock display (`--rf-pause` amber when paused) + `ScoreReadOnly` + `HalftimeView` (break countdown + Skip Break + Add Min) + `EventStrip` (most-recent-first, max 8, M' / M+S' notation, per-event-type icons + descriptions) + 3-row action grid (Pause+Card · Undo+MOTM · End-Half+End-Match); `ffc/src/pages/RefEntryPickers.tsx` NEW (~293 LOC after extraction in 9d93527) hosts the 5 bottom-sheet pickers — `ScorerPicker` (Goal/Own-Goal toggle flips displayed roster but credit goes to scoring team), `PauseReasonPicker` (Foul/Injury/Ref decision/Other/Pause without reason), two-stage `CardPlayerPicker→CardKindPicker` (Yellow accent-tinted / Red danger-tinted), `MotmPicker` (combined-roster with isCurrent highlight + Clear MOTM); `ffc/src/lib/useMatchSession.ts` extended one line to expose `sessionStorageKey: storageKey` + retrofitted `eslint-disable react-hooks/set-state-in-effect` on async-hydration setStates; `ffc/src/styles/ref-entry.css` extended (174 → 466 LOC) with 4 new brand tokens `--rf-success/--rf-warn/--rf-pause/--rf-danger` + ~292 LOC of live-mode rules. **Commits:** plan `9a30297` · hook + tunables `50b026c` · undoLast half-routing fix `07c8595` · code-quality pass `cd91483` · LiveConsole shell `f4c85ca` · MatchEvent half field `df189c2` · scorer picker `fef1a0a` · pause/card/MOTM/undo `a840476` · extract pickers `9d93527`. **Patterns (additive):** (a) **`Date.now()` at render scope defeats `useMemo`** — move INSIDE memo body, depend on `tick` counter, suppress `react-hooks/exhaustive-deps` with intent comment; ESLint `react-hooks/purity` catches the bug. (b) **`setState` in `useEffect` for async-arrival hydration** is a real lint error but unavoidable when the dep is async (Web Crypto digest); fix with intent-comment eslint-disable. (c) **`undoLast(): boolean` was a setState-async race trap** — flag set inside `setState((prev)=>...)` is unreliable when read after the function returns; change signature to `() => void`, use derived `canUndo` for button-disabled state. (d) **Sibling-key persistence pattern** — when one hook owns a localStorage key, a downstream hook can write to a sibling key (`<parent>:clock`) instead of forcing the parent to know about clock-state shape. (e) **`MatchEvent` needs a `half: 1 | 2` discriminator** — without it the formatter can't tell 1st-half-stoppage from 2nd-half-regulation; populate at every event-construction site. (f) **Pause + score-cell disable invariant** — disabling score buttons while paused prevents goals at stale match-minutes; CARD also disables; PAUSE/RESUME stays enabled. (g) **Two-stage card flow > one-shot grid** — picker → kind sheet sequentially, mutually exclusive via `cardPickerOpen && !cardStage` for player-picker, `cardStage` truthy for kind-picker. (h) **Own-goal flips the displayed roster** — picker title flips, roster array swaps, but `clock.addGoal(team, ..., isOwnGoal=true)` receives the SCORING team, NOT the rendered roster's team; critical correctness invariant. (i) **Soft-limit stoppage alarm** — chip turns red and pulses over 180s; doesn't auto-end, just nudges. (j) **800-line file rule is a guardrail, not a target** — first finished implementation hit 828 LOC; extracted picker components into sibling `RefEntryPickers.tsx` before close-out (RefEntry 555 / Pickers 293, both well within typical). (k) **YAGNI > over-engineering for tunables** — TS constants ship now; deferred app_settings plumbing until a second league configures differently. **Migrations on live DB: 31 (unchanged).** Live: https://ffc-gilt.vercel.app. |
| S045 | 27/APR/2026 | Home | Phase 2 slice 2B-E — RefEntry post-match review + submit_ref_entry wiring | **4 commits, no migration (client-only). 4 production code files + 1 plan + this close-out.** Slice 2B-E replaces the `mode === 'post'` placeholder + the disabled `END MATCH` stub with a real post-match flow. **Architecture:** new `'review'` mode added to `useMatchSession`'s `MatchMode` union (between `'live'` and `'post'`); new `endMatch()` / `confirmSubmit()` / `reopenLive()` actions on the session hook. `useMatchClock` gains `fulltime_at: string \| null` on `ClockState` plus three new mutators: `endMatch()` (captures `fulltime_at`, finalises any open pause into the current half's stoppage, emits a `'fulltime'` event with stamp computed pre-pause-finalisation), `reopen()` (drops the trailing `'fulltime'` event and clears `fulltime_at` for BACK TO LIVE), `deleteEvent(ordinal)` (refuses pause/resume/halftime/fulltime to avoid corrupting clock-machine state; for goal/own_goal also reverses score impact — own_goal decrements the OPPOSITE of `event.team` because addGoal credits the scoring team, not the participant's roster team). `useMatchClock` call lifted from inside `<LiveConsole>` to `RefEntry()` top-level so Live + Review share the same hook instance. **Files:** `ffc/src/lib/useMatchSession.ts` extended with the new mode + actions; `ffc/src/lib/useMatchClock.ts` extended with `fulltime_at` + 3 new mutators + defensive `??`-normalisation in `readClockState` so legacy persisted state without `fulltime_at` hydrates as `null` (not `undefined`); `ffc/src/pages/RefEntry.tsx` rewritten (~555 → ~750 LOC) with hook lifted to top + new `<ReviewConsole>` (final-score block with winner-highlight or DRAW badge, MOTM card with change/clear, optional notes textarea max 500 chars, full chronological event log with `<EventDeletePicker>` confirm sheet for goal/own_goal/yellow/red rows only, BACK TO LIVE + SUBMIT TO ADMIN action row, error banner) + new `<PostSubmittedView>` (big green checkmark + final-score readout + MOTM line + "close this tab" copy); `ffc/src/pages/RefEntryPickers.tsx` extended with `<EventDeletePicker>` (~50 LOC danger-tinted confirm sheet); `ffc/src/styles/ref-entry.css` extended (~470 → ~720 LOC) with `.ref-review-*` and `.ref-post-*` rules + `.ref-action-btn--submit` (gold-on-bg primary). **`buildSubmitPayload(state, payload, notes, winner)` helper** assembles the `submit_ref_entry` jsonb: aggregates per-player goals (only `'goal'` events with matching `profile_id`/`guest_id` AND matching roster team) + yellow_cards + red_cards, derives `is_motm` from `state.motm` (matches profile_id/guest_id AND `state.motm.team` matches roster team), strips client-only `committed_at`/`half` fields from events, includes timing block (`kickoff_at`/`halftime_at`/`fulltime_at`/`stoppage_h1_seconds`/`stoppage_h2_seconds`). On submit success: token server-burned, mode flips to `'post'` via `session.confirmSubmit()`, `:clock` localStorage cleared (best-effort try/catch). On error: surface message in banner, allow retry. **Commits:** plan `5dd1be8` · hooks `6c0b4aa` · review console + post view + submit wiring `0348ce4` · close-out (this commit). **Patterns (additive):** (a) **Lifting a stateful hook to share between sibling routes** — `useMatchClock` already tolerated null sessionStorageKey + null kickoffIso, so calling it at RefEntry top-level (before mode is `'live'`) is a safe no-op; cleaner than threading through context for two siblings. (b) **Defensive `??`-normalisation on hydrate when `ClockState` evolves** — adding `fulltime_at` to the interface meant any S044-persisted state would deserialise with `fulltime_at: undefined`, and `undefined !== null` would spuriously trip the "already ended" guard in `endMatch()`. Fixed by normalising every field in `readClockState` rather than bumping a version number. Cheap and forward-compatible. (c) **`result` is derivable from scores** — `submit_ref_entry` accepts both `result` and `score_white`/`score_black`; client computes result via `useMemo` so they can't drift. (d) **Own-goal score-reversal in `deleteEvent`** — `addGoal(team, ..., isOwnGoal=true)` records `event.team` = SCORING team (not the participant's roster team), so reversing on delete decrements the SAME team as scored (i.e. `event.team` for `'goal'`, OPPOSITE of `event.team` for `'own_goal'`)... wait, re-reading the addGoal source clarifies: `event.team` IS the SCORING team for both goal AND own_goal (NOT the participant's roster team for own_goal). Same semantic in `deleteEvent`'s reversal — decrement `event.team` for goal, decrement OPPOSITE of `event.team` for own_goal because own_goal credits the opposite team via `scoringTeam = team === 'white' ? 'black' : 'white'`. Inverted-mirror invariant; comment at every callsite. (e) **Pause/resume/halftime/fulltime are non-deletable in review** — removing them would corrupt accumulated stoppage / break / fulltime state. `deleteEvent` returns `prev` unchanged for these types as a belt-and-braces defence; UI doesn't render the delete affordance. Ref's escape hatch for a wrong PAUSE: undo within 15s, or BACK TO LIVE → resume → re-end. (f) **`as unknown as Json` for jsonb RPC args** — Supabase's generated `Json` type has an index signature `[k: string]: Json \| undefined`; hand-written interfaces don't, so direct assign fails TS2322. Precedent in `AdminMatches.tsx`. Alternative (importing Args type for the specific RPC) is verbose with no real benefit. (g) **Top-level `useMatchClock` needs a fallback `format`** — when `session.payload` is null, the hook still receives a `format` param. `'7v7'` is the safe default; in no-op state (no kickoffAt) the value is ignored anyway. **Verification:** `tsc -b` EXIT 0 + `vite build` EXIT 0 (PWA precache 11 entries, ~1540 KiB). ESLint clean on changed files. Dev-server preview verified at `/ref/dummy-token` — renders `'invalid'` mode with no console errors, confirming the hook-lift didn't break the loading/invalid path. End-to-end live → review → submit flow needs a real token + Thursday matchday to test (carry-over). **Migrations on live DB: 31 (unchanged).** Live: https://ffc-gilt.vercel.app. |
| S046 | 27/APR/2026 | Work | Phase 2 slice 2B-F — Admin match-entry review screen + migration 0032 | **8 commits, 1 migration. Migrations on live DB: 32 (0001 → 0032).** Slice 2B-F closes the ref-flow loop: admin can now load a submitted pending entry, review timing + events + per-player aggregates, edit match-level fields (score / MOTM / notes ready in builder), Approve → `approve_match_entry(p_pending_id, p_edits)` or Reject → `reject_match_entry(p_pending_id, p_reason)`. Drop-event escape hatch wired via new RPC. Sub-agent driven plan-execute pattern with two-stage review (spec compliance + code quality) ran across 5 Tasks; review-fix commits are peer to feature commits in the slice ledger. **Files:** `supabase/migrations/0032_admin_drop_pending_match_event.sql` NEW (43 LOC SECURITY DEFINER `admin_drop_pending_match_event(p_pending_id uuid, p_event_ordinal smallint)` with `is_admin()` guard + `log_admin_action` audit + idempotent on missing row); `ffc/src/pages/admin/MatchEntryReview.tsx` NEW (640 LOC final — full read-only render + inline-editable score inputs + 3 action sheets); `ffc/src/styles/match-entry-review.css` NEW (281 LOC, brand-themed `.mer-screen` scope-root tokens); `ffc/src/pages/admin/AdminMatches.tsx` extended (sixth `Promise.all` branch fetching `pending_match_entries` with `status='pending'` ordered `submitted_at desc`, new `pendingByMd` Map, new `pendingEntryId` field on `MatchdayWithMatch`, conditional gold-tinted CTA inside MatchdayCard); `ffc/src/index.css` (3 new `.admin-md-pending-*` rules); `ffc/src/router.tsx` (1 import + 1 route line at `/admin/match-entries/:id`). **Commits:** plan + Task 1 migration `f161c19` · Task 2 feature `02f40a3` · Task 2 review-fix (disable score inputs in this Task pending Task 4 wire-up + drop dead `regulationHalf` plumbing + remove dead `.mer-score-value` CSS) `4ee238e` · Task 3 feature `c98d98f` · Task 3 review-fix (order_by + invariant comment) `f2b64c3` · Task 4 feature (3 sheets + handlers, score inputs re-enabled) `1f294ae` · Task 4 review-fix (`openSheet` helper for actionError clearing + `Number.isFinite` guard before `Math.max`) `762447d` · close-out (this commit). **Schema-drift caught in Task 2:** `match_result` enum is `'win_white'\|'win_black'\|'draw'` (not `'white'\|'black'\|'draw'` as the plan assumed, matching the `team_color` enum convention). `deriveResult()` and the result-display IIFE corrected. **Patterns (additive):** (a) **Schema-drift check applies to enum values, not just column names** — even when the enum value space looks "obvious" from a sister enum (`team_color`), query the live types before writing code. The S024 lesson generalises. (b) **`openSheet(s)` wrapper that clears error state on every sheet transition** — small but useful idiom for screens with multiple action sheets sharing one screen-scope error banner; replaces all `setSheet(...)` callsites with one helper, removes a class of stale-error bugs. (c) **`Number.isFinite` guard before `Math.max`** when parsing user input via `parseInt` — NaN propagates silently through Math.max + setState + JSX rendering, surfacing only as a confusing Postgres type error at RPC time. (d) **`event: _event: PendingEventRow` rename + `void _event` belt-and-braces** — TS escape hatch for prop types kept for API stability while the body doesn't read them. Sister pattern to `_setEditNotes`/`_setEditMotm` for setters that aren't yet wired. (e) **Discriminated-union "drop a kind that has no UI yet"** — pre-approved when the variant adds noise without value; pragmatic over canonical-completeness. (f) **Pre-existing-vs-new ESLint triage at slice close** — when the lint sweep on changed files reports errors, run `git blame -L line,line file` on each error site before fixing. Pre-existing errors from prior sessions are out of scope; only new errors introduced by the current slice need addressing. Reduces both noise and risk of touching unrelated code at close-out. **Verification:** `tsc -b` EXIT 0 + `vite build` EXIT 0 (PWA precache 11 entries, ~1560 KiB total / 749.61 KB main JS / 130.47 KB CSS). ESLint clean on changed files after one targeted fix on a new `react-hooks/set-state-in-effect` error in MatchEntryReview.tsx mount/id-change useEffect (intent-comment eslint-disable, S044 pattern). Two pre-existing AdminMatches.tsx errors (lines 259 + 773) confirmed via git blame — out of scope. **Out of scope (deferred):** MOTM picker UI · Notes textarea UI (read paths live, setters waiting) · per-player aggregate edit-before-approve · realtime subscription on `pending_match_entries` (needs `ALTER PUBLICATION`) · admin nav-badge (Phase 2A push covers operational need) · S043 REVOKE/GRANT pass. **Carry-over:** real Thursday matchday end-to-end test for the entire 2B-B/C/D/E/F chain still pending live device acceptance; captain reroll modal (S037) also untested live. Live: https://ffc-gilt.vercel.app. |
| S047 | 27/APR/2026 | Work | Slice 2B-F carry-over (MOTM picker + Notes editor) + S043 defense-in-depth (migration 0033) + admin nav badge | **4 commits, 1 migration. Migrations on live DB: 33 (0001 → 0033).** Three S046-deferred items shipped in one short session — Tasks 2 (live device acceptance) and 3 (captain reroll on MD31) remain blocked on a real Thursday matchday. **Task 1 — MOTM picker + Notes editor in MatchEntryReview** (`8fba2dd`): promoted `_setEditMotm` / `_setEditNotes` placeholders to real setters; extended `Sheet` discriminated union with `motm` + `notes` variants; computed `effectiveMotm` / `effectiveMotmName` / `effectiveNotes` post-guard so cards reflect either the override or pending data; MOTM card gets Change/Set affordance + combined-roster bottom sheet (gold-tinted active state + Clear MOTM); Notes card always rendered with Add/Edit affordance + truncated single-line preview + bottom sheet (textarea max 500 + char counter, Save/Clear label by trim outcome). Both sheets local-state-only — diff sent in `p_edits` on Approve via existing `handleApprove` builder. `editMotm` 4-state semantics documented inline (`null` = no edit · `{x, null}` = profile · `{null, x}` = guest · `{null, null}` = explicit clear). Did NOT extract a shared MotmPicker — `RefMatchdayPayload` (ref-side) and `PendingPlayerRow[]` + lookup maps (admin-side) shapes differ enough that normalising would cost more than the duplication. Both files now ~660 LOC, well under the 800-LOC guardrail. CSS: `.mer-notes-preview` (single-line truncate) + `.mer-motm-list` / `.mer-motm-team` / `.mer-motm-pick` / `.mer-motm-pick--active`. **Task 4 — Migration 0033 REVOKE EXECUTE FROM PUBLIC** (`992d1bc`): defense-in-depth follow-up to S043 NULL-safety hotfix. S043 patched the helpers; 0033 adds the OUTER gate. Postgres grants EXECUTE to PUBLIC by default on every CREATE FUNCTION; the explicit GRANT TO authenticated in earlier migrations coexisted with that PUBLIC grant rather than replacing it. After 0033, anon-key callers get `42501 permission denied for function ...` at the PostgREST gate before any PL/pgSQL runs. Audited 35 SECURITY DEFINER RPCs whose body calls `is_admin()` or `is_super_admin()` against live `pg_proc`. `accept_substitute` (captain-OR-admin) included — captains are authenticated users, so REVOKE PUBLIC + GRANT authenticated preserves their access. `request_reroll` was a false positive in the LIKE search and excluded. Out of scope (deliberately untouched): `submit_ref_entry` / `get_ref_matchday` (anon-callable; auth via ref token) + `cast_poll_vote` / `invite_guest` / non-admin RPCs (no `is_admin` reference). **Signature drift caught:** first draft hand-typed signatures from migration history; rewrite generated DDL directly from `pg_proc.pg_get_function_identity_arguments(oid)` to avoid drift (`ban_player` 2-arg → 3-arg, `admin_submit_match_result` arg-order change). **Verified:** ACL transitions `{=X/postgres,postgres=X/postgres,authenticated=X/postgres}` → `{postgres=X/postgres,authenticated=X/postgres}` (PUBLIC grant gone). Anon-key curl: `regenerate_ref_token` / `reject_signup` / `admin_drop_pending_match_event` all return `42501 permission denied for function ...` at PostgREST gate; `get_ref_matchday` (anon-allowed) untouched, still returns `22023 Invalid or expired ref token`. **Task 5 — Admin nav badge** (`d56eea8`): user picked placement after a 3-option chip prompt — badge sits on the existing red 🛠 Admin platform row in Settings (the one introduced in S034). New `pendingEntriesCount` state; admin-only `useEffect` gated by `isAdmin` runs cheap `count: 'exact', head: true` query against `pending_match_entries WHERE status='pending'` on mount; cleanup flag prevents stale setState. Badge renders as a `.st-admin-badge` red pill (`#e63349`) with cream digit, 22px high, `min-width: 22px` (single-digit doesn't look like stray pixel), 2px ring (`box-shadow`) to lift off red row, `font-variant-numeric: tabular-nums`. `aria-label` reads `"N pending match ${count === 1 ? 'entry' : 'entries'} awaiting review"` with proper pluralisation. No realtime — count only matters when admin is actively triaging; Settings is rarely open. Realtime subscription deferred until `pending_match_entries` goes through `ALTER PUBLICATION` (Phase 2A). **Files:** `ffc/src/pages/admin/MatchEntryReview.tsx` (~640 → ~660 LOC) · `ffc/src/styles/match-entry-review.css` (extended) · `supabase/migrations/0033_admin_rpc_revoke_public.sql` NEW (116 LOC) · `ffc/src/pages/Settings.tsx` (extended) · `ffc/src/index.css` (.st-admin-link-label + .st-admin-badge — 21 new lines after .st-admin-link). **Patterns (additive):** (a) **Two-layer defense for SECURITY DEFINER RPCs** — Layer 1 helper-function NULL safety (S043) + Layer 2 REVOKE EXECUTE FROM PUBLIC at PostgREST gate (S047). Either alone shuts out anon, but both together survive future helper bugs and accidental signature regressions. ACL transition `{=X/...} → {postgres=X/...}` is the durable check in `pg_proc.proacl::text`. (b) **Generate migration DDL from `pg_proc`, not from migration history** — hand-typing function signatures from S008/S016/S028/etc. drifted; querying `pg_proc.pg_get_function_identity_arguments(oid)` and emitting SQL programmatically saved a "function does not exist" fail at apply time. The S024 schema-drift lesson generalises beyond column names to function signatures. (c) **`accept_substitute` is captain-OR-admin, not admin-only** — easy false positive in the "calls `is_admin()`" search; body shape is `IF NOT EXISTS (captain check) AND NOT is_admin() THEN RAISE`. Both captains and admins are `authenticated`, so REVOKE FROM PUBLIC + GRANT TO authenticated preserves the captain path correctly. (d) **`request_reroll` was a false positive in `prosrc LIKE '%is_admin%'`** — body doesn't actually call `is_admin()`. Don't trust LIKE-on-source as audit's final answer; confirm by reading the body. (e) **Local-state-only sheets > round-tripping setters through RPC at confirm time** — instant UX, no network on every cancel-able edit, single transaction at Approve. (f) **`editMotm` 4-state semantics via discriminated `null`** — null = no edit · `{x, null}` = profile · `{null, x}` = guest · `{null, null}` = explicit clear. Fourth state distinguishes "user actively cleared" from "user didn't touch it". (g) **`min-width: 22px` on a count badge** — without it single-digit counts collapse to narrow oval; with it the badge is shape-stable regardless of count length. (h) **Screen-reader copy for count badges:** `"N pending match ${count === 1 ? 'entry' : 'entries'} awaiting review"` — pluralisation matters; "1 pending match entries" reads broken. (i) **Two-track session pattern (carry-over + targeted-from-todo)** works well for short sessions where blocked items accumulate but other agenda items are independent. Carry-over items get a "Task N" label that links them to the originating slice; new items get standalone task numbers. **Verification:** `tsc -b` EXIT 0 + `vite build` EXIT 0 after each commit (PWA precache 11 entries, ~1565 KiB total / 752.78 KB main JS / 131.32 KB CSS at close). ESLint clean on changed files; two pre-existing errors in `Settings.tsx` (lines 120 + 214) confirmed via git blame — out of scope. Dev server (`preview_start ffc-dev`) mounted cleanly after each push. **Out of scope (still deferred):** live device acceptance for 2B-B/C/D/E/F chain (blocked on real Thursday) · captain reroll live test on MD31 · realtime subscription on `pending_match_entries` · per-player aggregate edit-before-approve · transactional email on signup approve/reject. Live: https://ffc-gilt.vercel.app. |
| S048 | 27/APR/2026 | Work | Phase 2 Slice 2A — Push backend foundation (Tasks 1–3 complete; 4–6 carry to S049) | **5 commits** (`30d8935` plan · `ffc4079` Task 1 migration 0034 · `a308600` Task 2 v1 Edge Function · `9ee8b7c` Task 2 auth-fix migrations 0035+0036 · `eba2aa0` Task 3 PWA strategy switch + sw.ts) + close-out. **3 migrations** added: 0034 push_subscriptions table + delivered_at + publication-add + pg_net trigger; 0035 dispatch_shared_secret + trigger sends two headers; 0036 service_role DML grants on push_subscriptions + notifications. **End-to-end push backend operational** — INSERT INTO notifications fires pg_net trigger → Edge Function `notify-dispatch` → returns `{"dispatched":0,"failed":0,"deleted_invalid":0}` (0 because no subscriptions yet). Two production blockers found and fixed mid-session: (a) Vault secret had `PA` paste artifact prefix; (b) Supabase auth-key dual-model trap — `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` returns new-style `sb_secret_*` but Functions gateway only accepts legacy JWT bearers AND `supabase-js` only bypasses RLS with legacy JWT. Fix: two-bearer model (`Authorization: Bearer <legacy-jwt>` for gateway + `X-Dispatch-Secret: <shared-secret>` for function-level caller-auth) + dedicated `LEGACY_SERVICE_ROLE_JWT` env var for `createClient`. Schema-drift discovered on `push_subscriptions` (out-of-band table existed with diverged shape) — user picked drop-and-recreate per plan. Task 3 switched vite-plugin-pwa from `generateSW` to `injectManifest` with new `ffc/src/sw.ts` (~80 LOC: workbox precache + push handler with 7-kind deeplink map + notificationclick) and isolated `tsconfig.sw.json`. Migrations on live DB: **36 (0001 → 0036)**. Pre-flight (VAPID keys + Vercel env + Supabase secrets + Vault `service_role_key` + `dispatch_shared_secret` + Edge Function env `DISPATCH_SHARED_SECRET` + `LEGACY_SERVICE_ROLE_JWT`) fully complete. Tasks 4–6 (client subscribe wiring + iOS install gate + multi-device E2E acceptance) carry over. |
| S049 | 28/APR/2026 | Work | UI restructure — bottom-nav 5→3 + top-bar/drawer + notifications panel + leaderboard expansion + delete account live | **1 consolidated commit `b711fbf`, 3 migrations.** User pivoted off the S048 plan (Phase 2A Tasks 4–6) at session open and asked for a 5-task UI restructure: rename Home→Poll, swap Leaderboard 📊→🏆, move Profile + Settings into a new avatar drawer, add notification bell to top-bar of every authenticated screen except Profile/Settings/Rules, fix the avatar-letter-escaping-the-square bug introduced by S048's camera badge, activate the "Delete account — coming soon" pill, and expand the leaderboard with last-5 W/L pills + goals + win % in a horizontally scrollable layout that fills the screen on landscape. Plan written inline (no plan file), user said "execute, don't ask permissions". **Task 3 — avatar fix** added `overflow: hidden` to `.pf-avatar` so initials clip to the 22px border-radius; camera badge stays unclipped on `.pf-avatar-wrap` parent. Wired `window.dispatchEvent('ffc:profile-changed')` after avatar upload + removal + display-name save so the new top-bar avatar refreshes. **Task 1 — shell primitives** rewrote `RoleLayout.tsx` end-to-end: 5-tab bottom nav reduced to 3 (🗳 Poll · 🏆 Leaderboard · 📅 Matches) with Profile + Settings moved into a right-side drawer; new `AppTopBar.tsx` (~75 LOC, crest left + bell + 32×32 avatar pill right) and `AppDrawer.tsx` (~120 LOC, portal slide-in with ESC + backdrop close + body scroll-lock); top-bar suppressed via `useLocation` pathname check on /profile and /settings/*; AppContext untouched (custom event drives refresh). **Task 2 — notifications panel** added migration `0038_notifications_realtime.sql` (idempotent ALTER PUBLICATION add), new `NotificationsPanel.tsx` (~180 LOC slide-down portal listing 50 most recent rows, optimistic mark-read on tap + deeplink, "Mark all read" header link, realtime via `postgres_changes` filter on `recipient_id`), new `lib/notificationDeeplinks.ts` typed mirror of sw.ts deeplink map covering all 19 `notification_kind` enum values + payload-key overrides for dropout/match_entry/formation_shared. Bell badge red pill with 99+ cap and pluralised aria-label. **Task 5 — leaderboard expansion** replaced flex-row layout with CSS-grid table inside `.lb-table-wrap` (overflow-x: auto). Columns now Rank · Player · P · W · D · L · GF · Win% · Last 5 · Pts. Win % computed client-side as `mp > 0 ? Math.round(wins/mp*100) : 0` (denominator includes draws per user). Last 5 promoted from inline overlay strip to a proper column with W/L/D pills (green/red/grey). Sticky rank + player columns via `position: sticky; left: 0|44px` with opaque var(--bg). Landscape on /leaderboard drops the global #root max-width via a `body.is-leaderboard-landscape` class toggled by a matchMedia listener that mounts/unmounts with the screen — no migration needed (existing v_season_standings + v_player_last5 expose everything). **Task 4 — delete account live**: migration `0039_profiles_soft_delete.sql` adds `profiles.deleted_at` + partial active index + `CREATE OR REPLACE VIEW v_season_standings` with identical column signature plus `AND pr.deleted_at IS NULL` predicate (v_captain_eligibility stays compatible); migration `0040_delete_my_account_rpc.sql` ships `delete_my_account()` SECURITY DEFINER + search_path locked + REVOKE PUBLIC + GRANT authenticated, audits BEFORE the destructive update (S034 pattern), sets display_name='Deleted player' + avatar_url=NULL + auth_user_id=NULL + email=NULL + is_active=false + deleted_at=now(); Settings.tsx pill no longer "coming soon", new portal confirm sheet with consequence copy + type-DELETE input, on success → sign out → /login. auth.users row left in place — re-signup works because auth_user_id is cleared (OAuth lands user in ghost-claim picker). **Verification:** tsc -b EXIT 0 (one fix iteration: `iconForKind` referenced non-existent `match_result_posted` enum value — corrected to actual notification_kind values). vite build EXIT 0; PWA precache 11 entries / ~1590 KiB; types regenerated 2185→2189 lines. Dev server boot clean; index.css fully parsed; all 5 new CSS sections present in document.styleSheets; no console errors at /login. Live verification (auth-gated screens) deferred to S050 since preview sandbox can't run real auth. **Patterns (additive):** (a) **Custom DOM events for cross-screen state refresh** — `window.dispatchEvent('ffc:profile-changed')` from Profile + Settings, RoleLayout listens + refetches, avoids touching AppContext (load-bearing for routing logic + Rule #8 plain-object Context). (b) **Sticky-cols + grid + min-width + overflow-x: auto** — for tables that exceed mobile viewport without being a real `<table>`. Sticky offsets must equal cumulative widths of preceding sticky cells (rank 44px → player `left: 44px`). CSS grid per-row preserves the existing button-per-row click structure. (c) **`body.is-X` orientation class scoped to a single screen** — useEffect with matchMedia listener, sets/clears the class on mount/unmount/orientation-change. Avoids `:has()` complexity, broad browser support, lets one screen break out of global #root max-width without affecting others. (d) **`CREATE OR REPLACE VIEW` with identical column signature when dependents exist** — adding a `WHERE` predicate to v_season_standings while keeping column list/order/types unchanged means v_captain_eligibility doesn't need rebuilding. REPLACE only requires column-signature compatibility, not body equivalence. (e) **Audit BEFORE destructive update for self-delete RPCs** — same as S034's delete_season; audit log entry needs to survive even if the soft-delete rolls back. (f) **Single consolidated commit for entangled multi-task slice** — when 3+ tasks edit overlapping files (here index.css + Settings.tsx), a single feat(s049) commit with task breakdown in the body reads better than artificially split commits. git add captures full file state; trying to split via add -p adds friction without benefit when the tasks ship together. (g) **`iconForKind` mapping must use the actual `notification_kind` enum, not the `push_prefs` keys from Settings.tsx** — caught by tsc -b at end-of-task. The two namespaces look similar but only partially overlap. **Out of scope (deferred to S050+):** live device acceptance for entire S049 stack · Phase 2 Slice 2A Tasks 4–6 (push client wiring + iOS install gate + multi-device E2E) · auth.users hard-purge Edge Function for delete_my_account follow-up. Live: https://ffc-gilt.vercel.app. |

| S050 | 28/APR/2026 | Work | Context-file trim — archive history + distill lessons | **1 docs-only commit `88f73f9`, no migration, no app code.** User flagged that CLAUDE.md / todo.md / lessons.md / INDEX.md felt bloated. Audit measured 4 context files at 498 KB total (CLAUDE.md alone was 69 KB with a single 58k-char `Status:` paragraph on L3 that grew narrative blobs from S028 → S049). Aggressive trim picked over conservative: replaced CLAUDE.md L3 mega-paragraph with a fresh 6-line `## Current state` block + distilled `## Live operational gotchas (durable)` section (17 cross-session rules); archived `tasks/todo.md` history blocks for S001 → S048 to `tasks/_archive/todo-history-pre-s049.md`, kept NEXT SESSION + S049 close-out (1,675 → 90 lines); rewrote `tasks/lessons.md` from prose paragraphs into 9 grouped one-liner rule sections (mockup safe-area / Windows+OneDrive / schema verification / TS+RPC typing / auth / Vercel deploy / React+UI / realtime+Edge / process), prose archived at `tasks/_archive/lessons-pre-distill.md`; rewrote `sessions/INDEX.md` keeping all 49 session rows sorted numerically, dropped 22 `### Prior next-session pointer (kept for archaeology)` stubs, kept the live S050 pointer. **Result:** 498 KB → 168 KB total (66% reduction); CLAUDE.md alone 69 KB → 12 KB (83%). History preserved in `tasks/_archive/`. **Patterns:** (a) when context-file bloat shows up, profile by file size first AND by load frequency — CLAUDE.md is sent every prompt so byte savings there are worth ~5× the same byte savings elsewhere; (b) archive-don't-delete preserves prose for future grep without bloating live context; (c) per-domain grouping for lessons > chronological listing — scan-friendly when looking up "what's the rule for X"; (d) for files with row patterns, `awk + sort + heredoc` reconstruction beats Edit chains. **Migrations on live DB: 40 (unchanged).** Live: https://ffc-gilt.vercel.app. |

| S051 | 28/APR/2026 | Work | Phase 2A close — push client + auth purge + Resend + SVG + auto-lock + auto-pick + vote reminders | **3 commits, 5 migrations.** User asked to knock the easy backlog off list, then do Phase 2A-D + 2A-E together, then 2A-F. Closed every code-level Track 2A piece. **Migrations on live DB: 45 (0001 → 0045)** (up from 40). **Commit 1 `7d82b6a`** — Tasks 4+5 push client (`pushSubscribe.ts` ~165 LOC: `urlBase64ToUint8Array` allocates fresh `ArrayBuffer` to satisfy `Uint8Array<ArrayBuffer>` typing; `isIosNonStandalone` UA + display-mode + legacy `navigator.standalone` triple-check; `subscribeAndPersist` idempotent upsert on `(profile_id, endpoint)`; `unsubscribeAndDelete` best-effort) + `IosInstallPrompt.tsx` (~50 LOC 3-step modal portal + safe-area-bottom) + `Settings.tsx` `handleMasterToggle` (iOS gate → permission → subscribe/unsubscribe → patchProfile, with masterBusy/Error/iosInstallOpen state). **auth.users hard-purge** — migration 0041 patches `delete_my_account` to stash `auth_user_id` in audit payload BEFORE nulling, adds `purge_auth_user_trigger()` (mirrors notify-dispatch two-bearer pattern from 0035) firing on `deleted_at` NULL→NOT NULL transition; new EF `purge-deleted-auth-user` validates `X-Dispatch-Secret`, calls `auth.admin.deleteUser` (404=idempotent), best-effort prunes `push_subscriptions` for the deleted profile. **Resend signup-outcome scaffold** — migration 0042 trigger on `pending_signups.resolution` pending → approved/rejected; new EF `notify-signup-outcome` posts to `https://api.resend.com/emails` with branded HTML for both outcomes; `RESEND_API_KEY` set as Supabase project secret. **Commit 2 `227f0f7`** — vector FFC crest SVG (`pip install pymupdf` + `fitz.open(pdf).get_svg_image()` extracted 26 vector paths to `shared/ffc-logo.svg` + `ffc/public/ffc-logo.svg` ~11.5 KB; brand tokens `#ebe7e0` cream + `#b0a48a` gold preserved; swapped 5 `<img>` PNG → SVG on Login/Signup/ResetPassword(×2)/Matches splitc(×2); PWA manifest icons + sw.ts push notifications stay PNG). **Phase 2A-D + 2A-E** — migration 0043 adds 3 enum values (`captain_auto_picked`, `captain_assigned`, `you_are_in`), `is_captain_of(matchday)` helper, `promote_from_waitlist(uuid)` 1-arg overload alongside existing S012 `(uuid, uuid)` admin overload (different arity = no PG conflict; captain-or-admin gated; finds next waitlisted yes-voter by `committed_at`; fires `you_are_in`), `auto_lock_matchday(matchday)` (idempotent, locks roster + fires `roster_locked` partitioned by ROW_NUMBER into confirmed/waitlist + conditionally calls auto-pick), `auto_pick_captains_on_lock(matchday)` (top `suggest_captain_pairs` + INLINE `match_players` UPDATE bypassing `set_matchday_captains` is_admin guard which fails in cron context; audit `payload.auto_picked = true` flag for UI detection; notifies both captains + admins with override deeplink), `notify_dropout_after_lock_trigger()` AFTER UPDATE OF `cancelled_at` ON `poll_votes`, app_settings `auto_pick_captains` default true, pg_cron `auto-lock-matchdays` `* * * * *` against `poll_closes_at <= now() AND roster_locked_at IS NULL`. **Migration 0044 fix** inlines the captain UPDATE in auto_pick (caught at design review: `set_matchday_captains` requires `is_admin()` which is false in cron context where `current_profile_id()` is NULL). **`CaptainDropoutBanner.tsx`** (~210 LOC) realtime sub on `notifications` filtered to current user, client-filters to `dropout_after_lock` + matchday match, two flavours via `payload.was_captain` (regular = Promote from waitlist via promote_from_waitlist(uuid); captain = Roll for new captain via existing S037 request_reroll); marks notifications read on action. **CaptainHelper integration** — banner above mode toggle + `autoPickedAt` audit-log fetch (queries most recent `set_matchday_captains` audit on this match.id, sets state if `payload_jsonb->>'auto_picked' === true`; re-runs on match change OR `saving` toggle so manual override clears the gold pill) + gold "Auto-picked at lock — roll or pick a new pair to override" announcer banner. **Commit 3 `7a47dfd`** — Phase 2A-F vote reminders (T-24h / T-3h / T-15m). Migration 0045 adds `vote_reminder` enum, unique partial index `vote_reminder_unique_idx` on `(recipient_id, kind, payload->>'matchday_id', payload->>'reminder_kind') WHERE kind = 'vote_reminder'`, 3 app_settings rows (each window flippable), `enqueue_vote_reminders()` SECURITY DEFINER that scans matchdays where poll still open + within 25h, computes active windows with 10-min lookback for boundary safety, joins active non-rejected non-soft-deleted profiles whose push_prefs.master + .vote_reminder both true via COALESCE default, LEFT JOIN poll_votes filtered to non-cancelled, picks `pv.id IS NULL` rows, INSERT … ON CONFLICT DO NOTHING; pg_cron `vote-reminders` `*/5 * * * *`. Spec said "WHERE vote IS NULL" but `poll_votes.choice` is NOT NULL — re-interpreted as non-yes-voters only (people who said no/maybe are engaged enough). Settings.tsx PushPrefs gains `vote_reminder` toggle. **Patterns (additive):** (a) **Two-bearer pattern reused 3× this session** (0041 + 0042 + 0043 cron infrastructure) — S048's notify-dispatch design is now a 30-line template for any trigger-driven Edge Function. (b) **PyMuPDF for PDF→SVG when source is genuine vector** — `page.get_drawings()` count + `page.get_images()` zero-count is the probe. (c) **Idempotent unique partial index for cron retry idempotency** — `CREATE UNIQUE INDEX … (cols) WHERE kind = 'X'` + `INSERT … ON CONFLICT DO NOTHING` lets a 5-min cron with 10-min lookback overlap itself without duplicates. (d) **`current_profile_id()` is NULL inside pg_cron** — RPCs called from cron should never go through admin-guarded functions; either inline or service-role variant. (e) **Audit `payload.auto_picked` flag as UI detector** — simpler than dedicated columns; latest-entry-wins semantics auto-clear the pill on manual override. (f) **Probe-before-trace** — `which pdftocairo pdf2svg inkscape potrace gs magick` before assuming a tool exists; pivoted to `pip install pymupdf` in 30 sec. (g) **Allocate `ArrayBuffer` explicitly when typing-strict TS demands `Uint8Array<ArrayBuffer>`** — default `new Uint8Array(n)` returns the looser `ArrayBufferLike`. **Out of scope (still deferred to live):** Thursday acceptance for entire S049 + S051 stack · Resend custom sender domain (defaults to onboarding@resend.dev). Live: https://ffc-gilt.vercel.app. |

| S052 | 28/APR/2026 | Work | GitHub-issue fix-pack — issues #2 / #3 / #4 / #5 / #6 / #7 closed (#1 deferred per user) | **1 commit, 1 migration. Live DB: 45 → 46.** User pointed at 7 open GitHub issues on `mmuwahid/FFC` and asked for plan-then-execute. After clarifying questions, user kept position + MOTM pills (deferring #1) but still wanted #3's portrait readability fixed without removing pills, plus 2 unrelated polish items (rename "P" → "MP", restore Last 5 colours). **Issue #3 (Leaderboard portrait fits)** — `lb-table-grid` rank `44px → 36px`, player `minmax(180px, 1.5fr) → 160px` fixed (kills the 1.5fr expansion that was eating all visible space), MP `36px → 40px`, `min-width: 760px → 604px`. Sticky-2 `left: 44px → 36px` to match. Pills + MOTM stay inside the 160px player cell. **Polish** — bumped Last 5 selector specificity from `.lb-last5-pill--X` → `.lb-cell--last5 .lb-last5-pill--{W,L,D}` so green / red / cream colours survive any future cell-level resets. Header `P` → `MP`. **Issue #2 (Poll No/Maybe visual feedback)** — root cause was the status-card branch checking `myVote.choice === 'yes'` only; voting No or Maybe fell through to the same prompt → looked like a no-op. Added two new state cards (`po-status--no` red border-left with "Change my mind" cancel button; `po-status--maybe` amber border-left with "Confirm Yes" / "Switch to No"). Existing `cast_poll_vote` RPC already accepts `cancel`. **Issue #5 (Matches stale-on-tab-return)** — extracted matches+matchday loader into `useCallback`; added (a) realtime sub on `matches` + `match_players` mirroring Leaderboard's pattern, (b) `visibilitychange` + `focus` listeners that re-fetch on tab return. **Issue #6 (Admin scroll-to-top)** — `useEffect(() => { window.scrollTo(0,0); document.getElementById('root')?.scrollTo?.(0,0) }, [])` on AdminHome mount (both window AND `#root` because `#root` is the actual scroll container). **Issue #4 (Settings restructure)** — extended `IosInstallPrompt.tsx` into tabbed iOS / Android `InstallPrompt` (auto-detects platform via UA, default `initialTab='auto'`); kept `IosInstallPrompt` as back-compat alias `<InstallPrompt {...p} initialTab="ios"/>` for the existing Settings push-gate caller. AppDrawer got two new rows: 🛠 Admin platform (admin-only) + 📲 Install app (everyone). RoleLayout passes `isAdmin` + `onInstallClick` + renders the InstallPrompt portal at layout level. Settings.tsx: new `‹ Back` chip top-left, Account section moved to bottom (after League Rules) and merged email + Sign out + Delete account into one `.st-account-row`, removed the `🛠 Admin platform` row entirely. Cleaned up unused `isAdmin` + `pendingEntriesCount`. **Issue #7 (admin_delete_player RPC + EditSheet delete button)** — migration `0046_admin_delete_player_rpc.sql`: SECURITY DEFINER + `is_admin()` body guard + REVOKE PUBLIC + GRANT EXECUTE TO authenticated. Refuses self-target (use `delete_my_account`), already-deleted targets, and `super_admin` targets. Audits BEFORE the destructive UPDATE (mirrors S034 / S049 pattern). Same anonymisation as `delete_my_account`. AdminPlayers.tsx EditSheet now has a "Delete player" button under the Save row (super_admin profiles hidden); new `DeletePlayerSheet` mirrors the type-DELETE confirm pattern. **Verification.** `tsc -b` EXIT 0 after type regen (2189 → 2213 lines); `vite build` EXIT 0; PWA precache 12 entries / 1619.63 KiB. Migration applied via `npx supabase db push --linked`. Live screen verification deferred to S053 since auth-gated routes can't run in preview. **Cross-PC sync gotcha** caught at commit time — session-start system snapshot showed stale "M CLAUDE.md / sessions/INDEX.md / tasks/lessons.md / tasks/todo.md" entries from the home-PC S051 close even though local HEAD was already at `608d1dc`; `git update-index --refresh` cleared them (NOT a stash situation — stat cache lag). **Patterns (additive):** (a) `git update-index --refresh` when stale-mtime files appear modified but `git diff` is empty. (b) Bumped-specificity `.parent .child--variant` as cheap colour-regression fix. (c) Width-based "fits on portrait" fix > pill-removal — usually an unconstrained `fr`/`auto` term mis-allocates space. (d) Status-card "no visible change after click" — every legal choice deserves its own confirmation state. (e) `visibilitychange + focus` is a 5-line stale-data antidote for SPA tabs. (f) Back-compat alias when refactoring a component into a more general shape. **Out of scope (deferred to S053):** all live-device acceptance from S049 + S051 + S052 itself. Live: https://ffc-gilt.vercel.app. |

| S053 | 29/APR/2026 | Work | Phase 3 begins — awards page (Ballon d'Or · Golden Boot · Most MOTM · Wall of Fame) shipped end-to-end | **9 commits, 1 migration. Live DB: 46 → 47. Pushed clean fast-forward `d8c8938..27576f7`.** User asked to "start phase 3" with full slice (spec → mockup → plan → code). **Pivot mid-session.** Initial pick was player analytics + H2H (V3.0:145–146); brainstorming locked scope and shipped spec + 2 mockups (Profile single + compare; Leaderboard select-mode states). User rejected both mockups without itemising what failed; clean revert via `git reset --hard HEAD~2` (commits were local-only, not pushed). Pivoted to V3.0:139 awards page on user's pick. **Awards-page brainstorming.** Style A/B preview file (`mockups/3-23-phase3-awards-style-compare.html`) — celebration vs stats. User picked "mix elements from both" with per-element specifics: A's serif gold "Season N Awards" header + A's big trophy + serif gold winner name + photo avatar hero cards + B's tabular Wall of Fame grid. Entry → trophy icon-btn on Leaderboard's controls row. Active vs ended seasons share template; "PROVISIONAL · Active" badge for active, "FINAL · Ended" for ended. Tie-break = leaderboard cascade (`<metric> DESC, wins DESC, total_cards ASC, display_name ASC`). Hybrid data: live view for active, snapshot table for ended. Awards points formula `wins*3 + draws` (NO late-cancel term — celebrates match performance, diverges from leaderboard `points` by design, documented inline). **First awards mockup attempt + revision.** Built `mockups/3-24-phase3-awards.html` final layout. User approved the awards page itself but asked the Leaderboard frame to match the live screen format. Re-read `Leaderboard.tsx` + live CSS, replaced frame B with proper `lb-head` sticky header, season chip with status dot, Filter + Sort + new gold trophy as third icon-btn, `lb-table-grid` with `lb-row--gold/silver/bronze` medal tints. User approved. Mockups committed `82ae899`. **Spec + plan.** `docs/superpowers/specs/2026-04-29-phase3-awards-design.md` (10 sections, 400 lines, commit `134a840`) + `docs/superpowers/plans/2026-04-29-phase3-awards.md` (7 tasks, 1298 lines, commit `31792e2`). Schema verified up-front (`match_players.profile_id` not `user_id`, `matches.motm_user_id`, `match_result` enum). **Subagent-driven execution Tasks 1–3.** **Task 1 (`9116216`)** migration `0047_phase3_awards.sql` — `season_awards` table (id PK, season_id FK CASCADE, award_kind text+CHECK on `('ballon_dor', 'golden_boot', 'most_motm')`, winner FK RESTRICT, runner-up FK RESTRICT, metric_value numeric, runner_up_metric numeric, meta jsonb, frozen_at, UNIQUE (season_id, award_kind)) + RLS + GRANT SELECT to authenticated; `v_season_award_winners_live` view via 4 CTEs (base agg → scored → 3 ROW_NUMBER ranks per award) + 3-way UNION ALL with LEFT JOIN runner-up self-join (CTE rewrite faster than spec's correlated-subquery draft); `snapshot_season_awards_trigger()` SECURITY DEFINER + search_path=public + guard `IF OLD.ended_at IS NOT NULL OR NEW.ended_at IS NULL THEN RETURN NEW;` + INSERT…SELECT FROM view ON CONFLICT DO NOTHING; AFTER UPDATE OF ended_at trigger. Spot-check 1.4 returned 0 rows (live DB has zero approved matches yet — vacuous); transactional rollback test confirmed trigger fires + idempotent. Spec ✅, code review ✅ (CTE rewrite flagged as beneficial deviation). **Task 2 (`446aa40`)** Supabase types regen — `database.types.ts` 2213 → 2283 lines. **Task 3 (`f256f09`)** Awards page skeleton — implementer discovered routes live in `ffc/src/router.tsx` (createBrowserRouter object literals), NOT App.tsx (which is just `<RouterProvider />`). Adapted correctly. `noUnusedLocals: true` AND `noUnusedParameters: true` in tsconfig forced dropping many imports/types/setters; documented what Tasks 4/5 must re-add in a comment block. Code review surfaced 5 cleanup recs for Task 4 (drop the cast, drop the comment block, gate badge behind `!loading`). **Tasks 4–7 in-session.** Subagent rate-limit hit at Task 4; continued in-session with same rigor (build gates after every step). **Task 4 (`bb687d2`)** hero cards + season picker + ~150 lines of CSS scoped to `.aw-screen`. Re-added `supabase` + `AwardKind` + `WinnerRow` + `ProfileLite` + state setters. Wrote data-fetch `useEffect` with cancellation pattern, `HERO_META` lookup table + `renderHero` helper. Folded in all 3 Task 3 cleanup recs. **Task 5 (`57d5395`)** Wall of Fame — added `WallOfFameRow` interface + `wallOfFame` state. Single `season_awards` query joined to `profiles` via `profile:profiles!winner_profile_id(...)` syntax + client-side group-by-season Map + sort by `seasonsById.get(id)?.ended_at` desc (cleaner than plan's draft which re-queried ended_at via the same wallRaw rows). Tabular grid renders ENDED seasons only. Empty state: "First season — Wall of Fame begins after this season ends". **Task 6 (`27576f7`)** Leaderboard trophy entry — `TrophyIcon` SVG (24×24, mirrors `FilterIcon`/`SortIcon` pattern) + 3rd `lb-icon-wrap` after Sort. New `.lb-icon-btn--awards` CSS variant: gold-tinted border + faint gold background + subtle 12px glow. `onClick` navigates to `/awards?season_id=${selectedSeasonId ?? ''}`. **Task 7** final verify + push — `tsc -b` + `vite build` clean across all 6 implementation commits; PWA precache 12 entries / 1632.21 KiB final (was 1620.37 at Task 3 = +12 KiB for the entire awards feature). **Verification deferred** to post-deploy: live DB has 0 approved matches → hero cards will show "No matches played yet this season" placeholder; Wall of Fame empty until S11 ends. **Patterns (additive):** (a) **Subagent rate-limit doesn't stop the session** — `tsc -b` + `vite build` were the actual quality gates. (b) **`router.tsx` ≠ `App.tsx`** — FFC routes are createBrowserRouter object literals; pin to CLAUDE.md. (c) **`tsconfig.app.json` strictness in skeleton commits** — drop unused symbols + comment-block-document what later tasks re-add. (d) **Discarded mockup without specifics** → don't probe; offer a clean pivot menu. (e) **A/B style preview as low-cost de-risk** when style-feel matters and the spec is silent. (f) **CTE-based view > correlated subqueries** for "rank-and-pick-N" patterns. (g) **Awards points ≠ Leaderboard points by design** — name divergent metrics differently in code AND spec risk table. (h) **Snapshot trigger on NULL → NOT NULL transition** + ON CONFLICT DO NOTHING for fire-once-on-state-change idempotency. (i) **Live view + snapshot table split** for "frozen on close" features — frontend chooses based on `ended_at == null`. (j) **Defensive cast as a sign you're dropping something you should keep** — in skeletons, prefer temporary deletion over keeping unused interfaces alive via `as` casts. (k) **Clean revert via `git reset --hard HEAD~N`** when commits are local-only; verify with `git status -sb` first. **Out of scope:** Wall of Fame backfill RPC for past seasons (V3.0:148 future), awards push notification on season end (easy follow-up), WhatsApp share PNG (V3.0:140), Best Defender / Worst Discipline awards (table CHECK extends without schema change). Live: https://ffc-gilt.vercel.app. |

| S054 | 29/APR/2026 | Work | Phase 3 share-PNG (V3.0:140) shipped end-to-end + 3 PR merges (Atomo #9 / #13 / #14) + 2 issue fixes (#8 / #12) + 4 issues closed | **22 commits, 5 migrations applied (0048, 0049, 0051, 0052, 0053 — gap at 0050 from PR #13 renumber). Live DB: 47 → 53.** Long working block. **PR #9 merge** (Atomo's morning PR — own-goal/clock/refresh/unlock-roster) — caught 0047 collision with our just-shipped `0047_phase3_awards`; renumbered his 0047 → 0048; verified his `db push` had silently no-op'd (schema_migrations already had a 0047 row); pushed 0048 to live; merged at `14e3047`. **Phase 3 share-PNG full slice** — brainstorming → spec (`b6f96e8`) → plan (`126b61c`) → A/B mockup `3-25` (`d082023`) → final mockup `3-26` (`cc2297d` + `1eaf5cd` for element picks: A's centred crest + serif gold title, B's TEAM WHITE/BLACK split scoreboard with vertical gold-gradient divider, B's tight sans scorer grid, B's gold-pill MOTM, no footer; user added match-number prominence + dropped venue) → spec/plan corrections (`9970535`) for OG attribution bug (own-goal scorer goes under THEIR OWN team with `(OG)` marker — standard football reporting; not the team that benefitted) + match_number/total_matches addition. Implementation in 11 tasks: migration `0049_match_card_payload_rpc` (RPC + private `match-cards` storage bucket; SECURITY DEFINER + is_admin guard), EF scaffold (`24f4a30`), EF source (`382e96c`; `index.ts` 140 LOC + `MatchCard.tsx` 135 LOC Satori component), EF deploy fix #1 (`4db1161` — Supabase EF CLI silently drops binary assets in subdirs; switched to runtime fetch from jsDelivr + base64-inlined the crest), EF deploy fix #2 (`09e4281` — Satori v0.10 doesn't support WOFF2; swapped to WOFF format from `@fontsource`), frontend helper `lib/shareMatchCard.ts` (`c353175`; `navigator.share({files})` + browser-download fallback; discriminated `ShareResult` union), wire-in to MatchEntryReview success state replacing `navigate()` (`987cd04`), wire-in to MatchDetailSheet admin footer (`91b76b9`; uses `useApp` not `useAppContext` — corrected during implementer self-review), close-out (`ccc940d`) — CLAUDE.md S054 status, lessons.md gained the EF + Satori lessons, AdminMatches stale `@ts-expect-error` cleaned. **PR #13 merge** (Admin Roster Setup screen + 2 RPCs — Atomo) — same migration-collision pattern; renumbered 0049/0050 → 0051/0052 (gap at 0050 left as paper trail); CSS conflict in `index.css` resolved (both sides additive at end of file); applied 0051 + 0052 to live DB; `admin_add_guest` + `admin_update_match_draft` RPCs verified; merged at `cbdecac`; types regen at `98e54e8`. **Issue #8** Leaderboard restructure — drop avatar disc from Player cell (component preserved for other surfaces), Pts moves to col 3 right after Player, Last 5 stays last, W/D/L cells coloured success-green / muted-cream / danger-red (same colour family as Last 5 W/L/D pills). Grid min-width unchanged (604px) so S052's portrait fit isn't regressed (`930cec7`). **Issue #12** login + Poll flicker — diagnosed two independent races: (1) `AppContext` `setLoading(false)` cleared on `getSession()` resolve but profile-fetch `useEffect` runs one cycle later → routing guards in HomeRoute / RoleLayout bounced to `/login` for one render; fix defers `setLoading(false)` until profile settled + branded splash (FFC crest + pulse) renders during loading (`1ba7e71`). (2) React 18 doesn't batch state updates inside Promise continuations — `Poll.tsx`'s `loadAll` had sequential `setMd`, `setMyVote`, `setCommitments` after `await`s causing intermediate renders showing the vote prompt before existing vote was known; fix wraps the whole load with a single `dataLoading` boolean. Audited Leaderboard / Matches / Profile / CaptainHelper — all use proper single loading gate, clean. Issues #10 + #11 closed as duplicates of PR #9 / PR #13. **PR #14 merge** (admin_add_commitment — Atomo) — first clean PR of the day; no conflicts, correctly numbered 0053; applied to live; `admin_add_commitment` RPC verified; merged at `71d2a2c`, types regen at `d289aee`. **Patterns (additive):** (a) **Migration-collision is the operational cost of branch-divergence** — happened 3× today (PR #9, PR #13's 2 migrations); before adding a migration, run `supabase migration list --linked` and number = highest applied + 1. (b) **Supabase EF CLI silently drops binary assets in subdirs** — only `.ts`/`.tsx`/`.json` upload; fetch from CDN at module init or inline as base64 for small files. (c) **Satori v0.10 supports TTF/OTF/WOFF only, NOT WOFF2** — verify font-format claims against README before shipping. (d) **React 18 auto-batching does NOT extend across `await` boundaries** — multi-fetch loaders need a single `loading` flag wrapping the whole sequence. (e) **Renumber-on-branch + force-push + squash-merge** for migration collision conflicts — preserves git history via `git mv`, keeps `main` clean. (f) **Verify subagent fixes when DONE_WITH_CONCERNS** — caught the WOFF2 issue proactively before manual test. (g) A/B style-compare → final mockup pattern shipped on first impl pass (S053 awards + S054 share-PNG). (h) Skip subagent reviewers for mockup tasks — natural reviewer is the user opening the file. **Out of scope (S055):** live verification on real Thursday matchday (share PNG + auth splash + Poll skeleton + Leaderboard + Roster Setup + Add Player); Phase 2 close 8-box acceptance still owed (Atomo handling); V3.0:141 / 143 / 144 / 145 / 146. Live: https://ffc-gilt.vercel.app, `main` at `d289aee`. |

| S055 | 29/APR/2026 | Work | Phase 3 backlog refresh + issue #15 roster setup refinements | **2 commits, 1 migration applied (0054). Live DB: 53 → 54.** ~2-hour block. **Commit 1 `5a8e113` — Phase 3 backlog refresh.** Audit caught payment tracker silently dropped during V2.5 consolidation (originally Phase 4 — Extras in V1.0–V2.4); same provenance for player badges + injury list. User confirmed scope: **drop** photo-OCR fallback / match highlights/video clips / win-streak deep form (last-5 strip in Phase 1 covers); **add** payment tracker / player badges / injury list to V3.0 Phase 3 backlog. Marked already-shipped items with [SHIPPED S###] tags (awards S053, share PNG S054, signup email S051). Updated `tasks/todo.md` Backburner block to match. Doc-only commit. **Commit 2 `ffbdc46` — Issue #15 roster setup refinements.** GitHub issue #15 from Atomo (3 asks on the just-merged PR #13 screen). Resolved 3 ambiguities up-front via `AskUserQuestion`: (1) "delete a player" = delete from unassigned **pool** list, not from a slot; (2) auto-fill = **hybrid** (target if set, else alternating fill); (3) waitlist UI = below pool, **tappable to promote**. Plan written + approved without revision. **Migration 0054** (`0054_admin_cancel_commitment_and_guest_rpcs.sql`): two SECURITY DEFINER RPCs — `admin_cancel_commitment(p_matchday_id, p_profile_id)` soft-cancels active poll_votes row; `admin_cancel_guest(p_guest_id)` soft-cancels match_guests row. Both: idempotent, refuse if matchday's match has recorded result (mirrors `admin_update_match_draft`), audit BEFORE destructive UPDATE (S034/S049 pattern), two-layer admin guard (S047). Reuses `log_admin_action(text, uuid, text, jsonb)` from mig 0008. Live DB push successful, both `pg_get_function_identity_arguments` signatures verified. **Frontend** `AdminRosterSetup.tsx`: header doc rewritten with new 7-step interaction model; dropped `.limit(cap)` on poll_votes loader; partition `allProfiles.slice(0, cap)` → pool-eligible / `allProfiles.slice(cap)` → waitlist (guests always pool); both `loadRoster` branches updated; new `cancellingId` busy lock; new `handleCancelPoolChip` handler (RPC dispatch + auto-promote first waitlister into freed pool slot); new `promoteWaitlister` UI-only handler; `tapChip` rewritten as Path A (explicit target = S054 behavior preserved) / Path B (no target → `targetTeam = whiteCount <= blackCount ? 'white' : 'black'`, fall through to other team if first choice full, toast "Both teams full" if both full); JSX: pool chips wrapped in `.rs-chip-wrap` with × button (stops propagation, disabled while RPC in flight); new `.rs-waitlist` section between pool and teams (renders only if `waitlist.length > 0`). **CSS** `index.css` +47 lines: `.rs-chip-wrap` flex container, `.rs-chip-remove` red 20px circle button with hover + disabled states, `.rs-waitlist` bordered section, `.rs-waitlist-chip` muted dashed-border variant. **Types regen** 2283 → 2308 lines. **Verification**: `tsc -b` EXIT 0; `vite build` clean (PWA precache 12 entries / 1661.62 KiB; +25 KiB from feature additions, count unchanged); functional verification deferred to live admin session (auth-gated, unreachable from preview). **GitHub** issue #15 auto-closed by `Closes #15` commit trailer; shipping note posted as comment. Vercel auto-deploy in flight. **Patterns (additive):** (a) **Plan mode + AskUserQuestion BEFORE writing code on multi-ambiguity tasks** — 3 questions in one round-trip resolved 3 ambiguities; plan approved without revision; first-pass implementation. (b) **Mid-flight scope-shift sequencing** — when user dropped backlog updates while I was finalising the issue plan, finished plan → ExitPlanMode → fast doc-only commit first → larger feature work second. Two clean commits, no entanglement. (c) **State updater functions must be pure** — first version of `handleCancelPoolChip` mutated a closure variable inside `setWaitlist(prev => ...)`. Caught self-review before commit; fix reads state directly at handler top, passes value into both updaters. (d) **Edit `old_string` reproduces existing content verbatim — re-read surrounding lines IMMEDIATELY before composing the call.** Memory-reconstruction across more than 1 line is a coin flip; first V3.0 masterplan edit attempt failed for this reason. (e) **Issue close via `Closes #N` commit trailer** auto-closes the issue on push; if you want a comment with implementation specifics, post it via `gh issue comment` after-the-fact (not via `gh issue close --comment` since the issue is already closed). **Out of scope (S056):** live verification of issue #15 + all S054 deliverables; Phase 2 close 8-box; payment tracker / badges / injury list spec + mockup work. Live: https://ffc-gilt.vercel.app, `main` at `ffbdc46`. |

**Next session: S056** — (a) Mandatory session-start sync per CLAUDE.md Cross-PC protocol. Expected tip: S055 close `ffbdc46` on `main`. Migrations on live DB: **54** (deliberate gap at 0050). (b) **Live device acceptance pass on a real Thursday matchday** — covers Phase 2 close 8-box (V3.0:122) AND every S054 + S055 surface needing production validation: share-PNG end-to-end, auth-loading splash, Poll skeleton on existing vote, Leaderboard new column order, Roster Setup tap-to-assign + add-guest + edit-mode (PR #13) + Add Player (PR #14) + **issue #15 pool × + waitlist promote + hybrid auto-fill (S055)**. (c) **Awards page live verification** — still owed from S053. (d) **Phase 3 backlog work** — payment tracker / player badges / injury list (newly restored S055), or player analytics + H2H re-attempt with different style direction. Per CLAUDE.md operating rule #1, mockups go in `mockups/` first.
