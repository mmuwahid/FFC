# FFC Lessons Learned

FFC inherits **all lessons** from `Padel Battle APP/tasks/lessons.md` — read that before writing any FFC code. Every Critical Rule there applies here too.

The original prose-heavy lessons (S008–S049) are archived at [`_archive/lessons-pre-distill.md`](_archive/lessons-pre-distill.md). This file is the distilled rule set.

## Critical Rules (Always Enforce — inherited from PadelHub)
1. Mockup-first workflow. HTML mockup → user review → finalize → implement.
2. No design changes without explicit approval.
3. Placeholder data only in mockups. No invented names / leagues / stats.
4. Version control planning docs. V1, V2, V3. Never overwrite.
5. Never regenerate user documents. Edit originals.
6. **Mockup matches live state verbatim, additions only.** If you're mocking a screen that already exists in the codebase OR was previously approved: locate the actual CSS file (`grep -rn "\.<class>" ffc/src/styles/`) and copy the rule set verbatim into the mockup `<style>`. Then add ONLY the new classes/diffs needed for this round. Never invent fresh design tokens for a live screen. Never substitute "close enough" approximations of colours, gradients, borders, or layout grids — the user can spot every drift. ONLY when the screen genuinely doesn't exist yet are you free to design from scratch.
7. Safe-area insets for all fixed-position elements on mobile.
8. Verify before pushing fixes: run dev server, check console, visually verify.
9. When adding inline state-driven UI, declare `useState` first. Grep to confirm.
10. **CRITICAL: verify DB columns exist before any Supabase `.select()` / `.update()`.** Extends to `.order()` / `.filter()` / `.in()` column references too — TypeScript's PostgREST client accepts any string, drift is silent until runtime.
11. Never use `useMemo` for React Context with derived state. Plain object.
12. Never place hooks after conditional returns.
13. Global `<style>` must live in main render, not early-return subtree.
14. SECURITY DEFINER RPCs for all Edge Function DB access (never service-role + direct queries).
15. RPC parameters receiving JSON should be typed `TEXT`, cast to `JSONB` internally.
16. After `CREATE TABLE` via SQL editor: `GRANT SELECT, INSERT, UPDATE, DELETE ON public.X TO authenticated, anon;` — RLS ≠ GRANT, missing grants silently return empty rows.
17. Always include `window.location.search` in `redirectTo` URLs (OAuth + reset).
18. Always scope `.delete()` / `.update()` with `.eq("league_id", ...)` — defense-in-depth beyond RLS.
19. Bump service-worker `CACHE_NAME` on every major deploy. Format: `ffc-vN`.
20. Full Windows path in Node scripts: `C:/Users/User/AppData/Local/Temp/FFC/...` — never `/tmp/`.
21. CRLF-aware string replacements on Windows git clones.
22. After triggering React state changes in `preview_eval`, wrap DOM reads in a 200–300ms timeout.
23. Git identity for FFC commits: `git -c user.name="Mohammed Muwahid" -c user.email="m.muwahid@gmail.com"`.

## Mockup safe-area (S008–S012)
- **Every mockup review verifies safe-area** — `viewport-fit=cover` meta + simulated Dynamic-Island cutout in phone-frame CSS + `padding-top: var(--safe-top)` on `.topbar` + `padding-bottom: var(--safe-bottom)` on bottom nav + sheets pad with `calc(var(--safe-bottom) + 16px)`. Reference: `docs/platform/iphone-safe-area.md`.
- **Hardcode safe-area values in mockups** — `--safe-top: 59px; --safe-bottom: 34px`. `env(safe-area-inset-*, 59px)` fallback fires only when env var is unrecognised; modern browsers resolve to `0px` (defined value) so fallback never triggers.
- **Statusbar pattern:** `.statusbar { height: var(--safe-top); display: flex; align-items: flex-end; justify-content: space-between; padding: 0 calc(28px + var(--safe-right)) 8px calc(28px + var(--safe-left)); flex-shrink: 0; }` — flex-end positions time/battery 8px below the island; `flex-shrink: 0` prevents content overflow from compressing it.
- **`.phone-inner > * { flex-shrink: 0 }`** — phone-frame is a scroll container, not stretch container; ANY child with `overflow: hidden` (cards, pitch-wraps) gets shrunk to 0 by flex spec without this. Applies to all phone-frame mockups.
- **Sticky bottom-nav, never absolute** — `.tabbar { position: sticky; bottom: 0; margin-top: auto; flex-shrink: 0; z-index: 10 }`. Absolute inside scroll container anchors to scroll-content-bottom, not viewport-bottom — appears mid-screen when content overflows.

## Windows + OneDrive workflow (S015–S016, S052)
- **OneDrive working tree + external `.git/` per PC.** `git init --separate-git-dir=C:/Users/<user>/FFC-git`. The `.git` pointer file syncs via OneDrive across PCs but git's object DB stays local. Authoritative source = `origin/main`.
- **Bypass `.bin/*.cmd` wrappers** when project path contains `&` (e.g. "11 - AI & Digital") — cmd.exe truncates at `&`. Use `node ./node_modules/<pkg>/bin/<bin>` directly in `package.json` scripts.
- **Vercel env vars on Windows:** `vercel env add <NAME> <env> --value "<v>" --yes` (positional env, `--value` flag, `--yes`). Preview takes empty-string positional branch arg. Never pipe stdin via `echo |` — captures trailing `\n`. Add `console.log(..., '(len:', v.length, ')')` runtime canary.
- **`.npmrc` in package root for peer-dep workarounds** — `legacy-peer-deps=true`. `--flag` doesn't persist across CI's clean `npm install`.
- **`SSL_CERT_FILE` for Go-binary CA-pool failures** — Go CLIs (`gh`, `docker`, `kubectl`) bundle Mozilla CAs and ignore Windows cert store. Export OS roots to PEM, point env var, restart shell. Raw `git` works without this (uses schannel).
- **`git update-index --refresh` is the right move when stale-mtime files appear modified but `git diff` is empty (S052).** Touching working-tree files via OneDrive sync (or any external tool) updates mtimes without changing content; git's stat cache then reports them as "modified" until refreshed. Symptom: `git status` shows files as M but `git diff <file>` is empty for all of them. NOT a cross-PC stash situation.

## Schema + RPC verification (S024–S028, S046–S047, S051, S053)
- **Before referencing any column, function, enum, or view, query it from `pg_*` / `information_schema`.** Spec prose is not authoritative — DDL is. plpgsql lazy-parses function bodies so CREATE passes but first call fails on bad column/enum/cast.
  - Columns: `information_schema.columns WHERE table_name='X'`
  - Function signatures: `pg_get_function_identity_arguments(oid)` from `pg_proc`
  - Enums: `pg_enum` joined to `pg_type`
  - View projection: `pg_views.definition` or `information_schema.columns WHERE table_name='v_X'`
- **Schema-drift check applies to enum VALUES** too — even when a sister enum makes the value space look "obvious." `match_result` is `'win_white'|'win_black'|'draw'` (S046).
- **Generate migration DDL for REVOKE/GRANT programmatically from `pg_proc`**, not by hand-typing from prior migrations. Function signatures evolve (`ban_player` 2→3 arg, `admin_submit_match_result` arg-order change).
- **`CREATE OR REPLACE FUNCTION` cannot change arg defaults or add parameters** — DROP + CREATE required (re-GRANT EXECUTE after).
- **`CREATE OR REPLACE VIEW` requires identical column signature** — body can change freely (e.g. add `WHERE pr.deleted_at IS NULL` predicate) without rebuilding dependents.
- **`log_admin_action` is 4-arg** `(target_entity, target_id, action, payload)` — admin derived internally via `current_profile_id()`. Don't pass admin id.
- **Audit BEFORE destructive update / DELETE** — audit row needs to survive even if destructive path rolls back (S034 `delete_season`, S049 `delete_my_account`).
- **`admin_audit_log` columns:** `target_entity`, `target_id`, `payload_jsonb` (NOT `target_table`). See `docs/admin-audit-sql.md`.
- **`edit_match_result` is narrow** — score/result/MOTM/notes only, requires `approved_at IS NOT NULL`, no nested `players`. Use `edit_match_players` for per-player post-approval edits.
- **`bans` live on `public.player_bans`** (`profile_id`, `starts_at`, `ends_at`, `revoked_at`) — NOT a column on profiles. `rejected` lives on `profiles.role`. `inactive` on `profiles.is_active=false`.
- **Migration number collision check:** `ls supabase/migrations/` AND `grep -r "supabase/migrations/NNNN" docs/superpowers/plans/` for unexecuted plans before picking next number.
- **Spec prose drifts from reality 3× per session at scale (S051):** spec referenced `lock_at` column (doesn't exist; reality is `poll_closes_at`), `white_captain_id`/`black_captain_id` columns on matchdays (don't exist; reality is `match_players.is_captain`), `poll_votes.vote IS NULL` (column is `choice`, NOT NULL). Verify EVERY column / function / enum / view name against `database.types.ts` BEFORE writing the SQL — even when spec was written by someone who built the schema.
- **CTE-based view > correlated subqueries for "rank-and-pick-N" patterns (S053).** When a view computes `winner = rank=1 row` AND `runner_up = rank=2 row` for multiple awards/categories, compute each rank table once via `ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)` then `LEFT JOIN ranks ON rn = 2` for runner-up. Cleaner AND faster than 4× correlated subselects per row.
- **Awards points ≠ Leaderboard points by design (S053).** Awards use `wins*3 + draws` only; leaderboard `points` includes `late_cancel_points` (negative for late drop-outs). Document the divergence inline at the view definition AND in the spec's risk table — future readers WILL assume formulas match without an explicit note.
- **Snapshot trigger on NULL → NOT NULL transition (S053).** Pattern for "freeze derived data when parent state changes": `IF OLD.col IS NOT NULL OR NEW.col IS NULL THEN RETURN NEW;` ensures fire-once semantics. `INSERT … SELECT FROM live_view ON CONFLICT DO NOTHING` makes re-fire idempotent. AFTER UPDATE OF specific-col (not generic AFTER UPDATE) limits trigger scope.
- **Live view + snapshot table split for "frozen on close" features (S053).** Active state queries the view (recomputes every load); ended state queries the table (immutable history). Frontend chooses based on `parent.ended_at == null`. Clean read-write separation without an RPC layer. View grants `SELECT TO authenticated`; table writes only via SECURITY DEFINER trigger.
- **`text + CHECK` over Postgres ENUM for value sets you might extend (S053).** `award_kind text NOT NULL CHECK (award_kind IN ('ballon_dor', 'golden_boot', 'most_motm'))` extends to new values via a single ALTER TABLE … CHECK rewrite, vs ENUM which needs ALTER TYPE … ADD VALUE in its own transaction. Keeps migration footprint small.
- **`ON DELETE RESTRICT` on snapshot FKs preserves historical immutability (S053).** `winner_profile_id uuid REFERENCES profiles(id) ON DELETE RESTRICT` blocks profile DELETE while a snapshot row references it. Acceptable for FFC because profile DELETE is gated through soft-delete (sets `deleted_at`) — hard DELETE would only happen in cleanup scripts and should be blocked by historical references anyway.

## TypeScript + Supabase RPC typing (S028, S045, S051)
- **Vercel builds with `tsc -b` (project refs)** — stricter than local `tsc --noEmit`. Run `node ./node_modules/typescript/bin/tsc -b && node ./node_modules/vite/bin/vite.js build` before pushing. Catches TS6133 unused vars + stricter generated RPC arg types.
- **Optional RPC args: conditional spread, never `null`.** `...(x ? { p_field: x } : {})` matches generated `T | undefined`. Never `null as unknown as null` (silences local but fails strict build). RPC args that should be nullable need `DEFAULT NULL` in PL/pgSQL or generator marks them required.
- **`as unknown as Json` for jsonb RPC args** — Supabase generated `Json` carries an index signature `[k: string]: Json | undefined` that hand-written interfaces lack.
- **Defensive `??`-normalisation when persisted-state shape evolves** — adding a field deserialises legacy state with `undefined`; normalise every field in the read path rather than version-bumping.
- **Allocate `ArrayBuffer` explicitly when typing-strict TS demands `Uint8Array<ArrayBuffer>`** — default `new Uint8Array(n)` returns the looser `Uint8Array<ArrayBufferLike>` (which can be `SharedArrayBuffer`); lib.dom strict typings on PushManager.subscribe.applicationServerKey and other `BufferSource`-strict APIs reject it. Pattern: `const buf = new ArrayBuffer(n); const view = new Uint8Array(buf);`.

## Auth + signup flow (S019–S020, S038)
- **Supabase "Confirm email" must stay OFF for FFC Phase 1** (admin approval is the gate). Flipping ON re-breaks `Signup.tsx` Stage 1 silent-stuck — `signUp()` returns `session: null`, `onAuthStateChange` never fires. Fix the inbox-handler before flipping.
- **Terminal roles auto-`signOut`** in `AppContext.tsx` — not just render flag. Pattern: stash sessionStorage message → signOut → `window.location.replace('/login?err=<code>')`. Hard redirect beats `navigate()` — bypasses onAuthStateChange races. Apply to `rejected`, `banned`, `suspended`, `session_expired`.
- **Test emails: `m.muwahid+s###<role>@gmail.com`** — Supabase email validator rejects `example.com` and throwaway domains; Gmail `+tag` aliases pass and forward to main inbox.
- **OAuth ghost-claim flow already exists** — `Signup.tsx` Stage 2 ghost-picker + `pending_signups.claim_profile_hint` + `approve_signup(p_pending_id, p_claim_profile_id)`. For OAuth users with no pending row, route HomeRoute → `<Navigate to="/signup" />` so Signup self-derives the right stage.
- **Read existing code before designing the fix** — when reporting a bug in code untouched in days, read end-to-end first. S038 P2 plan was a full feature build; reading revealed 80% already shipped, final fix was 3 routing edits.
- **Multi-statement SQL via Supabase CLI requires `DO $$ BEGIN … END $$`** — `db query --linked "UPDATE ...; UPDATE ...;"` errors out.
- **Supabase Google OAuth consent screen shows backend domain** (`hylarwwsedjxwavuwjrn.supabase.co`) — Pro custom domain ($25/mo) is the only fix. Add logo on Google Branding page; URL string stays.

## Vercel deploy + verification (S015–S020)
- **`vercel.json` SPA catch-all rewrite** — every Vite SPA needs `{"rewrites":[{"source":"/(.*)","destination":"/"}]}` from day one. Static assets (`/sw.js`, `/manifest.webmanifest`) take precedence automatically.
- **Acceptance test for SPA deploys** must curl-probe at least 3 non-root paths (`/login`, `/poll`, `/does-not-exist`). `/` alone proves nothing about routing.
- **Concrete observable check per milestone**, not "deploy green." Build crash-free is necessary but insufficient — env vars might not resolve, routes might not wire, SW might not install.
- **SW cache bump via build-timestamp** — `VitePWA({ workbox: { cacheId: 'ffc-<ISO-timestamp>' } })` + `cleanupOutdatedCaches: true` eliminates Rule #19 manual discipline. Now superseded by `injectManifest` workflow with `cleanupOutdatedCaches()` call in `src/sw.ts` (S048).
- **Vercel MCP `list_deployments`** — first-line diagnostic for "did my push trigger a deploy" — returns SHA + state + timestamp + commit message in one call.

## React + UI patterns (S016, S025–S028, S045–S049, S052–S053)
- **Plain-object Context, per-render rebuild.** `AppProvider` rebuilds `{ session, role, loading, signOut }` every render. No `useMemo` (#1 source of stale-Context bugs). If perf becomes real (FFC isn't close), the fix is `useSyncExternalStore`, not memo.
- **Inline HTML splash + `requestAnimationFrame` hide on first commit** — paints before any JS parses; CSS-only splash is faster than any JS-rendered splash.
- **Custom DOM events for cross-screen state refresh** — `window.dispatchEvent('ffc:profile-changed')` from Profile/Settings; RoleLayout listens + refetches. Lighter than threading through Context. Caveat: events don't carry diff data, listeners must refetch.
- **Sticky-cols + grid + min-width + overflow-x: auto** for tables that aren't `<table>`. Sticky offsets must equal cumulative widths of preceding sticky cells.
- **`body.is-X` orientation class scoped to one screen** — `useEffect` with matchMedia listener, sets/clears on mount/unmount; CSS `body.is-X #root { max-width: none }` overrides global cap. Cleanup-on-unmount critical.
- **Per-screen brand tokens** — declare ~12 `--*` properties at scope-root (`.po-screen` etc.) and let downstream rules pick up via `var()`. When existing CSS already var()-based with fallbacks, scope-override is 20× cheaper than rule-by-rule editing.
- **`overflow: hidden` on the clipped element, not the wrap.** If sibling decorations (badges) must stay outside the clip, position them on the parent wrap.
- **`loadData(mode: 'initial'|'refresh')` callback** for realtime-capable screens — initial mount + realtime sub + pull-to-refresh share one query; mode flag gates skeleton-clear vs spinner.
- **State-machine UI from raw data only** — Poll's 9 states derive from `(now vs windows) × myVote × roster_locked_at × draft.status × commitments[].team`. No phase enum. Self-correcting under realtime.
- **Lifting a stateful hook to share between siblings** — when both children need the same authoritative state and the hook tolerates null inputs, lift to common parent over context-threading.
- **Ref-based drag state, not `useState`** — pointermove fires ~60Hz; ref mutation avoids per-frame re-render. Gate updates via `if (state.pointerId !== e.pointerId) return`.
- **Slice-size discipline** — multi-session screens split A/B/C/D/E; each slice's file-header JSDoc lists what lands + what's deferred. Aim < 800 LOC per file; extract sibling modules at slice-close before they grow.
- **Local-state-only sheets > round-tripping setters** — instant UX, single transaction at confirm, no network on cancel-able edits.
- **`min-width: 22px` + tabular-nums on count badges** — single-digit counts collapse to oval otherwise.
- **Single consolidated commit for entangled multi-task slice** — when 3+ tasks edit overlapping files, one feat(s###) commit with task breakdown beats artificially split commits.
- **Pre-existing-vs-new ESLint triage at slice close** — `git blame -L line,line file` on each error. Pre-existing errors out of scope.
- **Bumped-specificity `.parent .child--variant` is a cheap colour-regression fix (S052).** Before `!important`, qualify the selector with one extra ancestor — buys a specificity tier without ownership churn. Useful when colours stop showing up despite seemingly correct rules.
- **Width-based "fits on portrait" fix > pill/content-removal (S052).** When a flex/grid layout mis-allocates space, the cause is usually an unconstrained `fr` / `auto` / `1fr min-content` term, not the content cells themselves. Switch elastic terms to fixed widths before stripping content.
- **Status-card "no visible change after click" bug pattern (S052).** When a UI commits state that flips a discriminated-union back to a state visually identical to the pre-click state, users perceive the click as a no-op. Every legal choice in a multi-option control deserves its own confirmation state.
- **`visibilitychange + focus` is a 5-line stale-data antidote for SPA tabs (S052).** When a tab's data dependency is implicit ("I'll see fresh data on next mount") but mount triggers don't fire on navigation between persistent route children, subscribe to `document.visibilitychange` + `window.focus` and re-trigger your loader. Combine with realtime subs to cover both tab-return AND mid-screen mutation.
- **Back-compat alias when refactoring a component into a more general shape (S052).** When a component grows tabs/options and gets renamed (e.g. `IosInstallPrompt` → `InstallPrompt`), keep the original name as a thin wrapper — `export const IosInstallPrompt = (p) => <InstallPrompt {...p} initialTab="ios"/>` — so existing call sites stay untouched.
- **Scroll-to-top on route mount needs both `window` AND `#root` scroll (S052).** When the app shell uses `#root` as the scroll container (CSS `overflow: auto` on `#root`), `window.scrollTo(0,0)` alone doesn't reset it. Pattern: `window.scrollTo(0,0); document.getElementById('root')?.scrollTo?.(0,0)`.
- **`router.tsx` ≠ `App.tsx` — FFC routes are `createBrowserRouter` object literals (S053).** New screens add their entry as `{ path: 'awards', element: <Awards /> }` inside the `RoleLayout` children array, NOT as `<Route>` JSX. Plans referencing `<Route path=…>` should be auto-corrected when the implementer reads the actual file. The App.tsx file is just `<RouterProvider router={router} />`.
- **`tsconfig.app.json` has BOTH `noUnusedLocals: true` AND `noUnusedParameters: true` (S053).** Skeleton commits that "pre-declare state for future tasks" won't compile. Pattern when implementing a multi-task scaffold: drop the unused symbols in the skeleton commit + comment-block document what later tasks must re-add + delete the comment block when the symbols come back.
- **Defensive `as` cast in skeletons is a code smell (S053).** When `(season as SeasonRow).name` creeps in to keep an unused interface alive against `noUnusedLocals`, you're either dropping a real symbol you should keep OR you should temporarily delete the interface. Don't fight the linter with casts.
- **Per-screen brand tokens declared at scope-root (S053).** `.aw-screen` declares `--gold`, `--gold-bright`, `--gold-deep`, `--bg`, `--surface`, `--surface-2`, `--text`, `--text-muted`, `--line`, `--line-strong` — downstream rules use `var(--gold)` etc. Generalises CLAUDE.md per-screen brand convention to new screens.
- **A/B style preview as low-cost de-risk (S053).** When style-feel matters and the spec is silent, ship a 2-frame A/B thumbnail file before committing to a single direction. User picks elements from each ("mix elements from both, header from A, Wall of Fame from B"). Reusable any time a single mockup attempt could miss for unspecified style reasons.

## Realtime + Edge Functions (S030, S048, S049, S051)
- **`ALTER PUBLICATION supabase_realtime ADD TABLE`** required before `postgres_changes` fires. Verify via `pg_publication_tables`. Filter supports ONE column; secondary filters apply client-side.
- **Migration 0012 DEFAULT PRIVILEGES covers `authenticated` only, NOT `service_role`.** Any new table that needs Edge Function (service_role) DML access must emit explicit `GRANT … TO service_role` in the same migration.
- **Two-bearer Edge Function auth** — `Authorization: Bearer <legacy-jwt>` for the Functions gateway + `X-<custom>-Secret` for function-internal caller-auth. Decouples function from Supabase's `sb_secret_*` vs legacy-JWT key model.
- **Supabase auth-key dual model trap** — `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` returns `sb_secret_*` (length 41); the Functions gateway and `supabase-js` `createClient` both need legacy JWT. Maintain a separate `LEGACY_SERVICE_ROLE_JWT` env var.
- **Vault paste-artifact verification** — `SELECT length, substring(., 1, 5) FROM vault.decrypted_secrets WHERE name = ?` after every secret create/update.
- **Two-layer defense for SECURITY DEFINER RPCs** — Layer 1: helper-function NULL safety (`COALESCE(is_admin(), false)`). Layer 2: `REVOKE EXECUTE FROM PUBLIC` + `GRANT EXECUTE TO authenticated`. Either alone shuts out anon; both together survive helper bugs and signature regressions.
- **base64url tokens via `gen_random_bytes` + 3 `replace()` calls** (URL-safe vs Postgres `encode(..., 'base64')` which produces `+`/`/`/`=`).
- **Two-statement burn-then-mint under snapshot isolation needs `pg_advisory_xact_lock`** to serialise concurrent calls.
- **`current_profile_id()` returns NULL inside pg_cron contexts** — cron runs as the postgres superuser, no `auth.uid`. Helpers that depend on it (`is_admin`, `is_captain_of`, etc.) all return null/false. RPCs invoked from a cron job must NOT route through admin-guarded RPCs; either inline the work inside a SECURITY DEFINER function or write a service-role-only variant.
- **Idempotent unique partial index for cron retry safety** — `CREATE UNIQUE INDEX … (cols) WHERE kind = 'X'` + `INSERT … ON CONFLICT DO NOTHING` lets a 5-min cron with 10-min lookback overlap itself without inserting duplicates. The index encodes the dedupe rule alongside the data instead of in helper code.
- **Two-bearer pattern is a reusable template for trigger-driven Edge Functions** — Vault `service_role_key` + `dispatch_shared_secret`, EF env `LEGACY_SERVICE_ROLE_JWT` + `DISPATCH_SHARED_SECRET`, trigger sends `Authorization: Bearer <jwt>` (gateway) + `X-Dispatch-Secret: <secret>` (caller-auth). Reused 3× in S051 (auth-purge + signup-outcome + auto-lock infra) — each new EF is now a 30-line mod off the S048 base.

## Process / framing (S008, S015, S025, S038, S050, S051, S053)
- **Frame decisions before drafting** when multiple downstream artifacts depend on them — 15 min framing saves 30+ min re-draft.
- **Research-before-close** — when user flags cross-cutting concern mid-session, research it before closing even if otherwise done. Trades 15 min now for 60+ min cold-start research next session.
- **Swap infrastructure hygiene debt early** — anon key → publishable key, build tool versions, naming conventions. "Will work for now" is a debt trap when migration cost grows linearly with app size.
- **User-driven post-deploy feedback round** — when mockups + spec are approved, ship 90% solution + one live testing pass beats iterating speculatively. User must be available to drive in one sitting.
- **Always check `git log --oneline -10` at session start** before acting on instructions in prior session docs. Commits landing AFTER a session's close log may supersede the log's decisions.
- **Three-option (A/B/C) cleanup proposal before touching durable files.** Mass-edit operations on lessons / planning docs / indexes deserve a "what gets dropped" preview — same shape as a destructive-action confirm sheet in the app. User picks in one message.
- **Profile context-file bloat by file size FIRST, then by load frequency.** CLAUDE.md sends every prompt; INDEX.md reads once at session-start. Same byte saving in CLAUDE.md is worth ~5× the saving elsewhere. Triggering condition for re-running an audit: any context file crossing 30 KB or 1,000 lines.
- **Archive-don't-delete** preserves prose for future grep without bloating live context (`tasks/_archive/`). Reusable any time durable docs grow past their useful-context size.
- **Per-domain grouping for lessons > chronological listing** — future-me / fresh subagent wants "what's the rule for schema verification", not "what happened in S028". Grouped sections are scan-friendly.
- **For files with row patterns, programmatic reconstruction beats Edit chains.** `awk + sort + Bash heredoc` rewrite is cleaner than sequential Edit calls when restructuring an indexable list.
- **Probe-before-trace.** When a "trivial-but-blocked" task arrives, first step is environment probe (`which pdftocairo pdf2svg inkscape potrace gs magick`), not assuming a tool exists. When all probes fail, a 30-second `pip install pymupdf` pivot beat hand-tracing. Generalises: scope the toolchain before scoping the work.
- **PyMuPDF for PDF → SVG when source is genuine vector.** `page.get_drawings()` count + `page.get_images()` zero-count is the **probe**. When both indicators say "vector source", `page.get_svg_image()` is a faithful 1:1 conversion. Avoids the "complex hand-traced logo" trap. Generalises: probe a PDF before tracing a PNG of it.
- **Subagent rate-limit doesn't stop the session (S053).** When Anthropic dispatch caps trip mid-execution-chain (e.g. 12pm Asia/Dubai reset), continue in-session with `tsc -b` + `vite build` after every step as the actual quality gate. Pause only if you genuinely lack signal; isolation was a means to a quality end, not the quality itself.
- **Mockup rejected without specifics → pivot menu, not probe (S053).** When a deliverable is rejected and the user doesn't itemise what failed, asking "what didn't land?" can yield "all of it, redo from scratch". Better: offer a clean pivot menu (revise / try a different item / different style direction / abandon). User picks fastest path.
- **A/B style preview as low-cost de-risk (S053).** When style-feel matters and the spec is silent, ship a 2-frame A/B thumbnail file before committing to a single mockup direction. User picks "mix elements from both" with element-level specifics. Avoids second-mockup-rejection risk.
- **Clean revert via `git reset --hard HEAD~N` for local-only commits (S053).** Verify with `git status -sb` showing local-ahead-of-origin first. Recoverable through reflog if needed. Only safe BEFORE pushing — once `origin` has the commits, revert via `git revert` instead.
- **Schema verification queries as a discrete first step in plans (S053).** Plan Step 1.1 ran `information_schema.columns` + `pg_proc.pg_get_function_identity_arguments` queries BEFORE the migration was written. Took 30 seconds; surfaced the `match_players.profile_id` (not `user_id`) gotcha that would otherwise hit at first migration apply. Worth a dedicated step in any plan that touches DDL.

## Edge Function assets + font formats (S054)

- **Supabase EF CLI silently drops binary assets in subdirectories (S054).** `supabase functions deploy` only uploads `.ts`, `.tsx`, and `.json` files. Binary assets (TTF, OTF, WOFF, WOFF2, SVG, PNG, WASM) in function subdirectories are silently dropped — no warning, no error. The deploy succeeds and the function runs but `Deno.readFile(new URL('./<asset>', import.meta.url))` fails at module init with a 500 WORKER_ERROR on every cold start before any handler runs. **Mitigation:** load assets via HTTP fetch at module init from a versioned CDN (jsDelivr or unpkg), OR inline as base64 data URIs in the source for small files (<20 KB). Local file copies stay in the repo for `supabase functions serve` local dev.
- **Satori does NOT support WOFF2 — use TTF / OTF / WOFF only (S054).** Satori v0.10's font decoder doesn't bundle Brotli decompression (which WOFF2 requires). WOFF2 fonts silently fail to render text. Per Satori's own README: "Satori currently supports three font formats: TTF, OTF and WOFF. Note that WOFF2 is not supported at the moment." **Mitigation:** when fetching `@fontsource` fonts from jsDelivr at runtime, use the `.woff` URL not `.woff2`. Both formats are published. Pattern: `fetch('https://cdn.jsdelivr.net/npm/@fontsource/inter@5/files/inter-latin-600-normal.woff').then(r => r.arrayBuffer())`.

## Multi-collaborator workflow + React state hygiene (S054)

- **Migration number collisions are the operational cost of branch divergence (S054).** Bit FFC 3× in one day (Atomo's PRs #9, #13 with 2 migrations each conflicting with our `0047_phase3_awards` and `0049_match_card_payload_rpc`). When a branch is forked before a migration lands on `main`, both sides reuse the same number. Worse: `supabase db push --linked` from the forked branch silently no-ops because `schema_migrations` already has a row at that version — the developer thinks they applied their migration but the function/table doesn't exist. **Mitigation:** before adding a migration, run `npx supabase migration list --linked` and number new file = highest applied version + 1. Or `git fetch origin && git rebase origin/main` immediately before adding the migration. **Resolution when caught:** renumber-on-branch via `git mv` + force-push + squash-merge. Preserves git history; keeps `main` linear. Atomo's PR #14 was the first clean merge of the day after the second incident — pattern is learnable.
- **React 18 auto-batching does NOT extend across `await` boundaries (S054).** Inside an `async` function, sequential `setX()` calls after each `await` are NOT batched — each causes a re-render. Issue #12 root cause: `Poll.tsx`'s `loadAll` did `await fetchA(); setMd(m); await fetchB(); setMyVote(mv); await fetchC(); setCommitments(c)` — between `setMd` and `setMyVote`, the UI rendered with matchday loaded but vote=null, showing the vote prompt before the existing vote was known. **Mitigation:** for any multi-step async loader, gate the entire UI on a single `loading` boolean that stays `true` until ALL setters have run. Set `loading=false` once at the end (in a `finally` block to handle rejections). Skeleton stays mounted across all intermediate state. Same trap can hit anywhere with multi-fetch loaders — Matches.tsx's pattern (single `loading` boolean wrapping the load) is the right one.
- **Multi-effect race in auth providers (S054).** `AppContext` had session-restore in one `useEffect`, profile-fetch in a separate `useEffect` keyed on session. `setLoading(false)` was called when session resolved — but profile (which sets `role`) was still unset for one render cycle. Routing guards observed `loading=false, session≠null, role=null` and bounced to `/login` before profile-fetch effect re-rendered with the correct role. **Mitigation:** when an auth context has session AND profile (and possibly more), `loading` should remain `true` until ALL pieces are settled. The profile-fetch effect signals completion (via `setLoading(false)` at the end of its own resolution path); the session-restore effect only sets `loading=false` for the signed-out branch. Tested: cold-reopen on logged-in account no longer flashes login screen.
- **Verify subagent fixes when the subagent reports DONE_WITH_CONCERNS (S054).** The subagent that fixed the Supabase-binary-asset deploy gap also wrote a comment claiming "Satori accepts WOFF2" — incorrect. Caught proactively by the orchestrator re-reading the Satori README before the manual smoke test. Cost: 5 minutes. Saved: a Task 11 manual test that would have failed silently. **Generalises:** when a subagent reports a deviation from spec, treat it as a flag to verify the deviation's premise, not a checkpoint to glance at and approve.
- **Don't dispatch spec/code-reviewer subagents for mockup tasks (S054).** Mockup deliverables are HTML files reviewed by the user opening them in a browser. The reviewer subagent's job is to compare code against spec — but a mockup IS the spec at that stage, and the natural reviewer is the user picking elements / flagging bugs. Saved ~30 minutes of dispatch overhead across 3 mockup tasks today. Still applied the full reviewer cycle to migration / EF / wire-in tasks where it pays for itself.
- **OG attribution: scorer's own team with `(OG)` marker (S054).** Standard football reporting convention. The own-goal scorer is listed under THEIR OWN team's column with an `(OG)` marker; the team-level score still credits the goal correctly to the opposite side via stored `score_white`/`score_black` columns. Caught at mockup review (S054 Phase 3 share-PNG). Generalises: when a derived metric "credits" the OPPOSITE party but the actor still belongs to the original group, render the actor under their actual group with a marker, not under the recipient. Spec section 3 + plan Task 4 SQL aggregation simplified to one CTE grouping by `(event.team, profile_id, guest_id)` — no team flip. **Generalises further:** "where the data lives" (which team's column) is a separate concern from "where it counts" (which team's score). Don't conflate.

## Workflow + state hygiene (S055)

- **Plan mode + AskUserQuestion BEFORE writing code on multi-ambiguity tasks (S055).** Issue #15 had 3 distinct ambiguities (delete-scope, auto-fill replace-vs-coexist, waitlist UI placement). Resolving them up-front via single AskUserQuestion round-trip (3 questions) meant the plan was approved without revision and implementation hit on first pass. **Generalises:** ambiguity compounds — each unresolved question multiplied with implementation choices and would have produced wrong work to undo. The cost of a 3-question clarification round (one user round-trip) is far below the cost of one wrong implementation slice.
- **Mid-flight scope-shift sequencing (S055).** When a user drops new scope while you're in flight on something else: finish the current task to a checkpoint (here: ExitPlanMode for the in-flight plan) → tackle the smaller addition first if it's doc-only or independent → then resume the larger work. Two clean commits, no entanglement, no half-finished state. The wrong move would be to fork the work or try to bundle the new scope into the existing plan.
- **State updater functions must be pure (S055).** First version of `handleCancelPoolChip` mutated a closure variable inside `setWaitlist(prev => ...)` to capture the first waitlister for a downstream `setPool` call. React allows this but it's brittle — updaters can be invoked multiple times under StrictMode dev or during concurrent rendering, and the closure variable would be set N times. **Mitigation:** when one state update needs a value from current state to drive another state update, read the state directly at the top of the handler and pass the value into both updaters. `const promoted = waitlist[0] ?? null; if (promoted) setWaitlist(prev => prev.slice(1)); setPool(prev => [...filtered, promoted])`. Caught self-review before commit.
- **Edit `old_string` reproduces existing content verbatim — re-read surrounding lines IMMEDIATELY before composing the call (S055).** First V3.0 masterplan edit attempt failed because I retyped a multi-clause H2H bullet from memory and introduced a duplicated phrase. The Edit tool requires exact match; even one extra word fails. **Mitigation:** for any multi-line Edit on an existing file, do a Read of the target lines (ideally the same Read you composed `old_string` from, with no intermediate work) immediately before the Edit. Memory-reconstruction across more than 1 line is a coin flip.
- **Issue close via `Closes #N` commit trailer auto-closes on push (S055).** GitHub auto-closes issues referenced by `Closes #N` / `Fixes #N` / `Resolves #N` in the merged commit message. **Trap:** subsequently calling `gh issue close N --comment "..."` errors with "already closed" because the trailer already did the work. **Mitigation:** if you want a comment with implementation specifics, post it via `gh issue comment N --body "..."` after-the-fact. The trailer does the close; the explicit comment carries the shipping note.
- **Pure UI promotion (no RPC) when the data layer doesn't need to change (S055).** Roster Setup waitlist promotion = move from `waitlist[]` to `pool[]` array, no DB write. The promoted player is already a yes-voter — they were just past the original `LIMIT cap` rank. Submitting the roster picks them up unconditionally. **Generalises:** before designing a new RPC for a "promote" / "feature" / "include" action, ask: is the underlying data state already correct, and is the UI only restricting visibility? If yes, the change is UI-only.
- **Pool-cap is a UI restriction, not a data semantic (S055).** Original loader had `.limit(cap)` on the poll_votes query — this was a UI choice (show admin only the players guaranteed in by `committed_at` order), not a DB rule. Yes-voters past rank `cap` are still valid commitments. Removing the LIMIT and partitioning client-side `slice(0, cap)` / `slice(cap)` cleanly separates the UI choice from the data; the same data drives both pool and waitlist sections. **Generalises:** when a `LIMIT` is added "for the UI", consider whether to enforce it client-side instead — keeps the data layer honest and gives the UI flexibility for future overrides like waitlist.

## UI affordance + form sync (S056)

- **Whole-row-as-button needs an explicit affordance (S056).** A `<button>` styled to look like a row hides its action — discoverability requires a small icon (✏ pencil at right edge) even when the entire surface is clickable. Atomo's issue #16 surfaced this for AdminPlayers active rows: tap-to-edit was wired up but invisible; users only saw the 🚫 ban emoji and assumed that was the only action. **Mitigation:** when a row IS the primary action, render a small muted affordance (`opacity: 0.35`, `margin-left: auto`) inside the button to signal the action. "The row is clickable" is not discoverable.
- **Five colour-coded categories cannot share one accent (S056).** Roster pool position pills (GK / DEF / CDM / W / ST) all defaulted to `--accent` gold except GK; the position label TEXT was the only differentiator, defeating the purpose of a coloured pill. **Mitigation:** light tints scaled for the surface (periwinkle / teal / orange / rose on dark chip background) — NOT the saturated `--pos-*` solid-fill values used by leaderboard rank pills, which are designed for white text on coloured background, not coloured text on dark background. **Generalises:** colour-coded pills need ONE colour PER category, full stop.
- **Chip-with-delete pattern: outer span = visual chip, two transparent buttons inside (S056).** Nesting `<button>` inside `<button>` is invalid HTML, but a "chip with × delete" needs two distinct interactive regions inside one visual container. Canonical structure: `<span class="chip">{button.body action=A}{button.remove action=B}</span>` — span gets border / background / radius, body button is `padding:0;background:none;border:none;color:inherit` and provides label-area padding, remove button sits inside chip's flex row at the right edge. The wrapper-with-gap structure (chip-wrap span containing two siblings with `gap:4px`) places × OUTSIDE the chip visually — Atomo's issue #18 complaint. **Generalises:** any "primary surface action + secondary in-place action" UI needs a visual container holding two transparent buttons, not two adjacent buttons with whitespace between them.
- **Auto-derive dependent inputs via `useEffect([primary])`, NOT `useState(() => derive(...))` (S056).** Initial-mount-only derivation silently breaks the moment the user changes the primary input — the secondary stays at its initial computed value. CreateMatchdaySheet's poll opens / closes were derived at mount from kickoff but never re-derived; admins editing kickoff thought the system would update poll times. **Mitigation:** if Y is a deterministic function of X, wire `useEffect([X], () => setY(f(X)))` and keep Y editable for override cases (the input is still a `<input>` with `onChange={setY}` so the user can override).
- **Local datetime-local parsing trick (S056).** `new Date('YYYY-MM-DD')` parses as **UTC** (per ECMAScript spec); `new Date('YYYY-MM-DDTHH:MM')` parses as **local time** (modern browsers, RFC 3339). For day-of-week checks on a date-only string: append `T12:00` before constructing the Date (12:00 is safe across all DST and ±14h tz boundaries). For local date arithmetic: use `new Date(y, m-1, d-n)` directly — it constructs a local-time anchor — then round-trip through `.toISOString()` + `toLocalInput()` to feed a `datetime-local` input. Don't `setDate(d - n)` on a UTC-parsed Date.
- **Auto-mode + explicit "stop delegate" mid-session = drop subagent / advisor calls for the rest of the session (S056).** When the user signals "stop delegating," stop calling `advisor` / subagents and ship direct. Earlier triage decisions still stand — the user's directive is about delegation flow, not about the in-flight plan.
- **Issue triage: ship-now vs mockup-required (S056).** A multi-item GitHub issue with 7 sub-asks is rarely all the same shape. Separate trivial CSS/logic fixes (ship now) from UX redesigns (mockup first per CLAUDE.md operating rule #1) and atomic-fix-pack-commit them with all the issue numbers in the message. Atomo's issue #18 had 3 ship-now items + 4 mockup-required items; shipping the 3 immediately while flagging the 4 for the next session's mockup work is faster than blocking on the full mockup cycle for the whole bundle.


## S058 + S059 narrative patterns

Archived to [`_archive/lessons-s058-s059-narrative.md`](_archive/lessons-s058-s059-narrative.md) on 04/MAY/2026 — durable rules from those sessions are already captured in `Critical Rules`, `Schema verification`, and `React + UI` above.

## S059 sweep patterns (9-issue close)

- **Grid template column-order MUST match JSX cell-order; CSS doesn't auto-reflow on mismatch (S059 #30).** Leaderboard had `grid-template-columns` declaring Pts last but JSX placed Pts 3rd. Children fill grid cells in document order, so the template's column widths landed against the wrong content. Bug was latent since launch — each cell got SOME workable width (Pts=44, Win%=52) until the very narrow Pts slot fell to Last-5 which actually needed 140+. **How to apply:** when defining a grid table, write the JSX cell sequence as a comment above `grid-template-columns` and number them. Audit any existing grid table once.
- **Conditional JSX rendering of a grid cell breaks the column count (S059 #35).** Poll: hiding `.po-rank` when locked left grid template at 4 columns but only 3 children rendered, all packed left. Fix is a modifier class with a different `grid-template-columns` declaration. **General rule:** if you remove a grid child via `{cond && <X />}`, you MUST also adjust `grid-template-columns` via a class or use a `display: contents` placeholder.
- **PostgREST embed-ambiguity is a recurring class of bug, not a one-off (S059 #36).** Same `match_players → profiles` ambiguity bit Matches.tsx scorers in S058 #25 AND EditRosterPostSheet in S059 #36. **General rule:** any `match_players` SELECT with a profile embed MUST use `!match_players_profile_id_fkey` explicitly. Audit any new query before merge. Pattern likely applies to other tables with multiple FKs to the same target (any table with `*_by` columns referencing profiles).
- **For browser features that vary across platforms (Web Share API), always wire result handling to user-visible feedback (S059 #34).** `navigator.canShare({files})` returns false on desktop Chrome → silent download fallback. Without surfacing the fallback, the user thinks the action failed. **How to apply:** every `navigator.share` call should branch on success / fallback / error / cancel and emit user-visible state for at least the first three.
- **Issue body can override what the title implies (S059 #30).** Title was "color code wins/draws/losses" — read literally that meant colours were missing. Body revealed colours were already mostly correct (W green / L red present; D was cream not grey) and the real bug was column-width clipping. **General rule:** read issue body in full before scoping; titles are headlines, not specifications.
- **DOM eval at the dev preview is faster than re-deploying for DOM-observable fixes (S059 sweep).** Verified all 9 issue fixes via `preview_eval` queries (button widths, grid template, presence of `.mt-motm-strip` / `.mt-hat-badge`, sheet overlay, etc) before pushing each commit. Saved several Vercel deploy cycles. **How to apply:** for any DOM-observable fix, add a 1-liner DOM eval as the verification step before commit.

## S060 patterns

- **Mockups for live/approved screens MUST be verbatim copies + diffs (S060 #41).** First mockup attempt for issue #41 invented a fresh design (gold WINNER text, gold-bordered VS pill, uniform dark backgrounds, square fake logos, separate dark footer strip) — none of which matched live `matches.css`. User reaction: "this is much worse now. why are you not following the existing format from live database? you are creating a whole new design that is not approved!" Root cause: I styled from memory of the spec instead of locating the actual stylesheet. **General rule:** the moment you start a mockup for an existing/approved screen, run `grep -rn "\.<root-class>" ffc/src/styles/ ffc/src/index.css` FIRST. Copy the matching rule set verbatim into the mockup `<style>`. Then add only the new classes the diff needs. The canonical CSS for `.mt-card` / `.splitc-*` lives in `ffc/src/styles/matches.css` (NOT `ffc/src/index.css` where most other screen styles sit) — checking only one CSS file misses cross-file rule sets.
- **User-attached screenshots are the ground truth, not the codebase reading (S060 #41).** When the user pastes a screenshot of the live app + says "follow this exactly", that screenshot is the spec. Read it pixel by pixel: background asymmetry between teams, exact pill colours (BLUE not gold), WINNER label position (top of column not banner), ribbon styling, etc. Don't substitute your CSS-file reading for what the screenshot actually shows. **How to apply:** any time the user attaches a UI screenshot as reference, treat it as the constraint. Verify your mockup matches it element-by-element before claiming completion.
- **Standardise emoji rendering via inline SVG when cross-platform consistency matters (S060 #41).** ⚽ U+26BD renders blue/white on Windows Segoe UI Emoji but black/white on iOS / Android — same emoji, different glyph. User flagged this immediately. CSS-emoji is reliable for symbol shapes (🟨🟥), unreliable for stylised objects (⚽🏆). **How to apply:** for any emoji that's a stylised object (ball, trophy, badge, flag), prefer inline SVG via `<symbol id>` + `<use href>` so all platforms render identically. For abstract symbols (cards, dots, arrows) keep emoji.
- **An audit-style issue is its own deliverable; don't spawn 10 sub-issues mechanically (S059 #38).** User pasted a comprehensive UI/UX audit prompt as an issue. Posted findings as a single comment with categorisation (CRITICAL / HIGH / POLISH) — lets the user decide which to break out vs. flooding the issue tracker with 10 small items. **How to apply:** for any "review and report" ask, the report IS the deliverable; close the issue with the report as the comment.

## S063 patterns

- **Translate frontend types ↔ DB enums at the supabase.rpc boundary, not anywhere else (S063 #5).** RefEntry tracked `winner` as `'white' | 'black' | 'draw'` (drives CSS class names) and sent the bare value as `result`. The DB enum is `win_white | win_black | draw`. `(p_payload->>'result')::match_result` raised PG `22P02` invalid_text_representation; submit failed silently because the toast also swallowed the error. **General rule:** whenever a frontend type and a DB enum diverge by name, translate at the `supabase.rpc(...)` call site. Reference `Database['public']['Enums']['xxx']` from generated types so TS prevents the next drift.
- **Surface real PG errors in toasts via `extractErrMessage()` helper (S063 #5 + #7).** Bit twice within 30 minutes: `e instanceof Error ? e.message : String(e)` falls to `String(plainObject)` = `"[object Object]"` for Supabase `PostgrestError` objects (which have `.message` but aren't Error instances). **How to apply:** every `try/catch` around `supabase.rpc()` / `supabase.from()` should use a helper that probes `.message` on plain objects: `if (typeof e === 'object' && e !== null && typeof (e as { message?: unknown }).message === 'string') return (e as { message: string }).message`.
- **Cast string literals to enum types explicitly when SELECT uses DISTINCT, GROUP BY, or feeds through a CTE (S063 mig 0066).** `INSERT INTO notifications (recipient_id, kind, ...) SELECT DISTINCT mp.profile_id, 'match_entry_approved', ...` failed with `column "kind" is of type notification_kind but expression is of type text`. PG normally infers a string literal's type from the target INSERT column, but `SELECT DISTINCT` (and CTE chains feeding INSERT) lock the projection's types BEFORE the INSERT context is resolved. **General rule:** any string literal in a SELECT projection that ultimately INSERTs into an enum column gets `'val'::enum_type` defensively. The 5-second cast prevents a transaction-blocking surprise. Bonus: applies to plain `INSERT ... SELECT` too whenever the projection is non-trivial.
- **`games_seeded` offset is mandatory for any match-number derivation (S063 mig 0067).** Mid-season import seasons (Season 11 = 30 historic) break naive 1-based COUNTs. The Matches tab + MatchDetailSheet + share-PNG RPC + AdminMatches all need `seasons.games_seeded + (rank of this matchday)`. Share PNG showed "Match 2 of 40" while every other surface said "Game 32 / 40" because mig 0049 forgot the offset; mig 0067 fixed. **How to apply:** when adding any new "Game N" / "Match N" derivation, search for `games_seeded` to find the canonical pattern. If you can't find it on a surface that should have it, you've found a bug.
- **Cache versioning constant beats manual storage wipes (S063 EF).** `RENDER_VERSION = N` constant in EF, cache key `<matchId>-vN.png`. Bumping N auto-invalidates every cached file on next request. Legacy `<matchId>-v0.png` keys orphaned but harmless. **How to apply:** any Edge Function that caches output by a stable key MUST include a version segment. Bump it whenever the renderer's input → output mapping changes (layout, RPC payload shape, font set, anything user-visible).
- **Pre-fetch twemoji SVGs at module init for Satori PNG rendering (S063 EF).** Inter has no glyphs for ⭐ ⚽ 🟨 🟥. Without `graphemeImages` Satori renders them all as tofu `[]`. Module-init `Promise.all(...)` fetch (4 emojis) keeps per-request render fast. **How to apply:** for any Satori render that uses emoji, pre-fetch twemoji SVGs at module top, base64-encode as data URIs, pass via `graphemeImages` option. Add Inter 700 weight if any emoji-adjacent text needs bold.
- **Satori v0.10 demands explicit `display: flex | none` on every childful div (S063 EF).** `<></>` Fragments inside `.map` get silently rewrapped as `<div>` with no `display`, tripping the renderer's enforcement. Several text-bearing divs (titles, score digits) also need `display: 'flex'` even when they don't contain children. **How to apply:** every `<div>` in a Satori-rendered tree MUST have explicit `display`. Replace Fragments inside `.map` with explicit sibling JSX or extract to a child component.
- **Bottom-fixed sheet rows MUST grep for sibling `position: fixed; bottom: 0` elements before shipping (S063 #7).** `.mer-actions` was `position: fixed; bottom: 0` with no z-index; `.app-bottom-nav` is `position: fixed; bottom: 0; z-index: 10`. Bottom nav painted over the buttons. Admin scrolled to the end of the review and saw nothing actionable. **General rule:** any new `position: fixed; bottom: 0` element MUST `grep -rn "position: fixed" ffc/src/index.css` first. When in doubt, default to inline + use `app-main`'s `padding-bottom: safe-bottom + 72px` clearance.
- **`transform: translateZ(0)` + `will-change: transform` is the iOS bottom-nav and topbar bobbing fix (S063 #46).** Forces a GPU compositing layer; subpixel jitter from iOS toolbar transitions stops visibly affecting fixed elements. `.app-topbar-inner` also gets explicit `min-height` so the dynamic-island gutter doesn't shift between routes that load avatar/bell at different times. **How to apply:** any fixed element that lives across route changes (topbar, bottom-nav, persistent sheets) gets these two declarations. Inexpensive — single GPU layer per element — and visibly stabilises iOS UI.
- **Side-aligned mirrored columns reinforce team identity better than centered (S063 share PNG + MatchDetailSheet).** White-left / black-right (with `flex-direction: row-reverse` on the black side) on both surfaces mirrors the score-box layout, reads instantly, and matches the in-app FIFA player-table convention. **How to apply:** for any team-paired UI (rosters, scorers, formations, captain pickers), default to side-aligned mirrored columns over stacked or centered.
- **Single-line title with date subtitle reads cleaner than stacked banner + meta (S063 share PNG).** "Season 11 — Game 32 of 40" + "Thu, 30 Apr 2026" is two clean Playfair lines instead of a 3-line stack ("Season 11" / "Game N of T" / "Date"). **How to apply:** for any titled artifact (PNG share card, meeting brief, screenshot caption), prefer one info-dense title line + one subtitle line over multi-line stacks.
